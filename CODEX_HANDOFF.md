# JS부동산 Codex 인수인계

## 2026-08-31 찜목록 전체화면 중앙·좌우 확장

- 지도 오른쪽 경계를 기준으로 맞추던 찜목록 팝업을 전체 브라우저 화면의 정중앙 기준으로 변경했다. 상세가 닫힌 찜목록도 중앙에 놓이며 폭은 화면에 따라 500~620px로 넓어진다.
- 찜 매물 상세를 열면 팝업은 화면 폭의84%를 사용하면서 760~1120px 범위로 확장된다. 좌측 상세와 우측 찜목록은 각각 최소360px를 확보하고 1.08:0.92 비율로 배치해 사진·상세정보와 저장 목록을 모두 더 넓게 볼 수 있다. 모바일768px 이하의 기존 단일 열 동작은 유지한다.
- 자산 버전은 unified favorites JS/CSS `7.0.9-centered-wide`이다. JavaScript 문법 검사와 전체 회귀검사 **495/495**, 정적자산134개 빌드, Wrangler dry-run과 diff 검사를 통과했다. 찜 데이터·매물·DB·사진 원본·연락처는 변경하지 않았다. 운영 배포와 실제 화면 검수 결과는 후속 기록에 추가한다.
- 기능 커밋 **`85b4393`**을 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`faa613fc-a47a-487b-bec2-c1937030d428`**로 배포했다. 세 운영 도메인은 HTTP200이며 새 자산 버전을 제공한다. 로그인된 운영 화면을 가로1280px로 검수해 단독 찜목록 폭620px·좌우 여백330px, 상세 분할 폭1075px·좌우 여백102px로 정확히 중앙 정렬되는 것을 확인했다. 상세580px·찜목록494px와 매물/썸네일4개가 유지됐고 상세 X 후 중앙 찜목록으로 정상 복귀했다. 브라우저 오류와 운영 데이터 쓰기는 없었다.

## 2026-08-31 찜목록 우측 배치·좌측 상세카드 분할보기

- 데스크톱·가로 태블릿에서 찜목록 팝업을 지도 영역의 오른쪽 가장자리에 맞춘 폭440~540px 창으로 배치했다. 중앙을 크게 가리던 기존 배치를 없애고 화면 크기·지도 폭이 바뀌면 오른쪽 기준으로 다시 맞춘다.
- 찜 매물을 누르면 찜창을 닫던 동작을 바꿔, 같은 팝업이 왼쪽 상세카드·오른쪽 찜목록의 2열 구조로 부드럽게 확장된다. 기존 통합 상세카드 DOM을 팝업의 전용 왼쪽 영역에 옮겨 사진 드래그·상세정보·내비/로드뷰/대장/수정·원본 기능을 그대로 사용하며, 오른쪽 찜목록은 열린 상태라 다른 매물을 연속으로 선택할 수 있다.
- 상세카드 X를 누르면 왼쪽 상세 영역만 접히고 오른쪽 찜목록은 유지된다. 찜팝업 전체를 닫거나 지도 보기·임장하기로 전환하면 상세카드를 함께 안전하게 닫고 원래 위치로 돌려 다른 화면의 기존 상세 열기에도 영향을 주지 않는다. 모바일768px 이하는 종전 단일 열 배치를 유지한다.
- 자산 버전은 unified favorites JS/CSS `7.0.8-right-split-detail`이다. JS 문법검사, 찜목록 집중검사와 전체 회귀검사 **495/495**, 정적자산134개 빌드, Wrangler dry-run과 diff 검사를 통과했다. 찜 데이터·매물·DB·사진 원본·연락처는 변경하지 않았다. 운영 배포와 실제 화면 검수 결과는 후속 기록에 추가한다.
- 기능 커밋 **`cadb920`**, 지도 경계 폭 조정 **`06511f0`**, 캐시 갱신 **`57e918e`**를 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`a42fca6d-f9bc-45ad-b52e-f29f6e9b0a80`**로 최종 배포했다. 세 운영 도메인은 HTTP200이며 `7.0.8-right-split-detail`을 제공한다. 로그인된 운영 찜폴더4개 매물로 팝업 오른쪽 간격16px, 분할 폭668px가 지도 안에 유지되고, 상세326px가 왼쪽·찜목록340px가 오른쪽에 배치되며 썸네일4개가 유지되는 것을 확인했다. 상세 X 후 찜목록은 열린 채 폭440px 우측 배치로 복귀했고 브라우저 오류와 운영 데이터 쓰기는 없었다.

## 2026-08-31 찜목록 실제 대표사진·클릭 상세보기

- 메인 매물카드는 통합 원본에서 계산한 `thumbnailV8`을 표시하지만 찜목록은 과거 기본 사진 필드만 검사해, 같은 매물카드에 사진이 있어도 찜폴더에서는 `사진 없음`으로 보이던 원인을 수정했다. 찜목록도 `thumbnailV8`과 연결된 통합 원본의 대표사진·사진배열을 우선 확인한다.
- 찜폴더를 펼쳤을 때만 각 매물 대표사진 한 장을 58×48px(모바일48×44px) 지연 로딩하며, 원본 전체사진이나 상세정보를 미리 받지 않는다. 따라서 초기 웹 로딩과 평소 지도 운영에는 추가 요청이 없고 펼친 폴더의 보이는 썸네일만 브라우저 캐시를 활용해 내려받는다.
- 찜목록의 사진·매물명·주소·금액 영역을 하나의 상세보기 버튼으로 만들었다. 누르면 찜창을 닫고 기존 통합 상세카드를 열며, 전체사진·상세 원본은 이 실제 클릭 시점에만 기존 상세 API로 조회한다. 별도의 `찜 제거` 버튼은 분리해 오조작을 막고 키보드 포커스·모바일 배치도 함께 정리했다. 매매 찜은 임대 문구 대신 매매가를 표시한다.
- 자산 버전은 unified favorites JS/CSS `7.0.6-photo-detail`이다. JS 문법검사, 찜목록 집중검사와 전체 회귀검사 **495/495**, 정적자산134개 빌드, Wrangler dry-run과 diff 검사를 통과했다. 찜 데이터·매물·DB·사진 원본·연락처는 변경하지 않았다. 운영 배포와 실제 화면 검수 결과는 후속 기록에 추가한다.
- 기능 커밋 **`d496d6b`**을 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`d56c3f86-710e-424c-9ff8-2454424ac9e3`**로 배포했다. 세 운영 도메인은 HTTP200이고 새 JS/CSS 버전을 제공한다. 로그인된 운영 찜폴더의 매물4개를 펼쳐 네 썸네일 모두 실제 원본(자연폭947~1600px)이 지연 로딩되는 것을 확인했고, 첫 매물 행 클릭 시 찜창이 닫히고 기존 상세카드·사진이 정상적으로 열렸다. 브라우저 오류는 없었으며 찜 추가·제거·수정이나 운영 데이터 쓰기는 실행하지 않았다.

## 2026-08-31 전체매물 한 번에 즉시 표시 성능 개선

- 운영 새 탭에서 목록 카드가 처음 보이기까지 약 4.4초, 같은 화면 재접속도 약 2.7초가 걸리는 것을 확인했다. 대표매물 전체와 통합 원본·사진정보 전체를 모두 받은 뒤에야 한 번에 그리는 안전한 구조였지만, 새로고침마다 같은 전체 데이터를 다시 기다렸고 준비 중 `검색 결과 0개`가 잠깐 표시됐다.
- 기존 최근 80개 임시 캐시를 폐기하고 스키마3의 **완전한 전체매물 캐시**로 교체했다. 임대·매매를 포함한 전체 대표매물과 해당 통합 원본·사진·출처 요약을 함께 보관하며, 전체 건수와 저장 건수가 다르거나 통합 서명이 없으면 캐시를 사용하지 않아 일부 목록을 전체처럼 표시하지 않는다.
- 다음 새로고침부터는 직전의 완전한 전체 데이터 세트를 한 번에 즉시 복원하고, 최신 전체 데이터는 뒤에서 동시에 확인한다. 대표매물·통합정보 서명이 같으면 목록·지도 DOM을 다시 그리지 않아 깜빡임·스크롤 초기화가 없고, 변경된 경우에만 최신 전체 세트를 한 번 다시 반영한다. 목록 DOM은 기존 가상목록을 유지해 전체 데이터가 메모리에 있어도 화면에는 보이는 카드 주변만 생성한다.
- 최초 전체 준비 중에는 `전체 매물 한 번에 준비 중...`을 표시하고 빈 필터 렌더를 막아 `검색 결과 0개`가 나타나지 않는다. 업데이트 직후 첫 접속은 새 전체 캐시를 만들기 위해 서버 전체 로딩이 필요하며, 캐시가 만들어진 다음 새로고침부터 즉시 복원 경로를 사용한다.
- 자산 캐시 구분자는 `full-list-cache-v2=1`, 전체 캐시 스크립트는 `1.2.0-full-snapshot`이다. 전체 회귀검사 **495/495**, 정적자산134개 빌드, Wrangler dry-run과 diff 검사를 통과했다. 매물·DB·사진·연락처·개인 상태·자동수집 설정은 변경하지 않았다. 운영 배포와 실제 전후 속도 검수 결과는 후속 기록에 추가한다.
- 기능 커밋 **`4ec97c1`**을 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`8b2f5a8e-1429-453d-aad8-6b89517ecf5c`**로 배포했다. 세 운영 도메인은 HTTP200이고 새 전체 캐시 스키마3 자산을 제공한다. 업데이트 후 전체 캐시를 1회 만든 다음 운영 새로고침을 측정해 **2.09초에 전체13,574개가 한 번에 복원**되고 현재 상가임대 필터 결과8,347개가 표시됐으며, 최신 전체 확인은3.91초에 끝났다. 중간 소량 목록·`검색 결과 0개`는 없었고 화면 DOM은 가상목록18개로 제한됐다. 변경 전 같은 운영화면 재접속2.67초·새 탭4.45초보다 첫 전체표시가 빨라졌으며 운영 데이터 쓰기는 실행하지 않았다.

## 2026-08-31 지도 다각형 그리기 영역검색

- 데스크톱 지도 세로 도구의 `반경검색` 바로 아래에 `그리기` 버튼을 추가했다. 버튼을 누른 뒤 지도 경계점을 차례로 클릭하면 빨간 선과 꼭짓점이 표시되고, 3개 이상 지정한 뒤 `그리기` 버튼을 다시 누르면 마지막 점과 첫 점을 자동으로 연결한 반투명 다각형이 완성된다.
- 완성된 다각형 안쪽과 경계선 위에 좌표가 있는 매물만 오른쪽 목록·지도 클러스터에 함께 남는다. 활성 필터 칩에는 `그린 영역`을 표시하며, 같은 버튼 재클릭·상단 안내창 닫기·필터 칩 X·전체 초기화로 즉시 해제한다. 3점 미만이면 완료하지 않고 안내를 유지한다.
- 반경검색·거리재기·로드뷰 선택과 동시에 작동하지 않도록 공간검색 상태와 지도 오버레이를 공통 정리한다. 다각형 검색 중에는 지도 커서를 십자 모양으로 바꾸고 첫 꼭짓점을 별도 색상으로 표시한다. 기존 행정동/세계 클러스터의 안정화 경로도 다각형 검색 중에는 필터 결과 밖 매물을 다시 섞지 않는다.
- 자산 캐시 구분자는 `polygon-filter-v661=1`이다. 신규 다각형 판정·UI·목록/지도 연동 검사3개를 포함해 전체 회귀검사 **494/494**, 정적자산134개 빌드, Wrangler dry-run과 diff 검사를 통과했다. 매물·DB·사진·연락처·개인 상태·자동수집 설정은 변경하지 않았다. 운영 배포와 실제 지도 검수 결과는 후속 기록에 추가한다.
- 기능 커밋 **`ae5df6f`**을 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`c0677cfa-cac5-4456-8a4e-f0eac93628eb`**로 배포했다. 세 운영 도메인은 HTTP200이고 새 HTML·CSS·지도 도구·목록/지도 필터 자산을 제공한다. 로그인된 운영화면에서 네 경계점으로 다각형을 완성해 전체8,347개에서 영역 내부3,040개로 목록·지도 클러스터가 함께 줄고 `그린 영역` 칩과 상단 안내가 표시되는 것을 확인했다. 상단 X로 해제하자 버튼·칩·도형이 사라지고 전체8,347개로 즉시 복귀했다. 운영 데이터 쓰기는 실행하지 않았다.

## 2026-08-31 상세사진 마지막·첫 사진 순환 연결

- 상세매물카드 사진은 이미 순환 인덱스를 사용했지만, 사진을 크게 연 전체화면 갤러리는 첫 장과 마지막 장에서 인덱스를 고정하고 이전/다음 버튼을 비활성화해 연속 드래그가 끊겼다.
- `wrapPhotoIndexV8141` 공통 순환 계산을 추가해 상세카드·전체화면·인접사진 선행 로딩이 모두 같은 규칙을 사용한다. 마지막 장에서 다음/왼쪽 드래그하면 첫 장, 첫 장에서 이전/오른쪽 드래그하면 마지막 장으로 이어진다. 사진이 2장 이상이면 전체화면의 이전/다음 버튼도 끝에서 계속 활성 상태를 유지한다.
- 기존 115ms 페이드아웃+210ms 이동·페이드인 전환, 드래그 임계값42px, 클릭 확대, 키보드 방향키, 인접사진 선행 로딩은 유지한다. JS 자산 버전은 `8.1.41-loop-gallery`다.
- JS 문법검사, 사진·상세 집중검사35/35, 전체 회귀검사 **491/491**, 정적자산134개 빌드, Wrangler dry-run과 diff 검사를 통과했다. 사진 원본·매물·DB·수집기·개인 상태는 변경하지 않았다. 운영 배포와 실제 마지막↔첫 장 전환 검수 결과는 후속 기록에 추가한다.
- 기능 커밋 **`828b8b1`**을 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`031a205a-fe81-46b6-9934-8f8d70288a1c`**로 배포했다. 세 운영 도메인은 HTTP200이고 JS `8.1.41-loop-gallery`와 공통 순환 계산을 제공한다. 로그인된 운영화면에서 사진4장 매물을 열어 상세카드와 전체화면 각각 `1/4→이전→4/4`, `4/4→다음→1/4`, `4/4→왼쪽 드래그→1/4` 전환을 확인했다. 클릭 확대와 부드러운 전환도 유지됐으며 운영 데이터 쓰기는 실행하지 않았다.

## 2026-08-31 반경검색·거리재기 안내창 지도 상단 중앙 정리

- 반경검색과 거리재기 결과 안내가 지도 하단을 가리고 오른쪽의 문자 `×`가 어색하게 떠 보이던 화면을 정리했다. 두 기능이 공유하는 안내창을 오른쪽 매물목록을 제외한 **실제 지도 영역의 상단 중앙**(헤더 아래 74px)으로 옮겼다.
- 오른쪽 매물목록 폭을 런타임에 계산하는 기존 `--map-sidebar-width-v659`를 안내창 위치·최대폭에도 적용해, 목록 폭이나 화면 폭이 바뀌어도 지도 영역 가운데를 유지한다. 반경검색 결과 문구와 거리재기 경유점/누적거리 문구 모두 같은 배치를 사용한다.
- 단순 문자 `×`를 15px 선형 SVG 아이콘이 들어간 30px 원형 버튼으로 교체하고 테두리·호버·키보드 포커스 상태와 `반경 검색 또는 거리재기 해제` 접근성 이름을 추가했다. 기존 `clearMapMeasurementsV657()` 해제 동작은 그대로 유지한다.
- CSS 자산 버전은 `6.10.1-map-status-top`이다. 신규 집중검사2개 포함 전체 회귀검사 **491/491**, 정적자산134개 빌드, Wrangler dry-run과 diff 검사를 통과했다. 지도 반경/거리 계산, 매물 필터·목록·DB·사진·연락처·개인 상태·자동수집은 변경하지 않았다. 운영 배포와 실화면 검수 결과는 후속 기록에 추가한다.
- 기능 커밋 **`2172216`**을 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`f1578d5b-962d-4bc3-b2c2-64fc2ff32d39`**로 배포했다. 세 운영 도메인은 HTTP200이고 새 CSS 버전을 제공한다. 로그인된 운영화면에서 500m 반경검색을 실행해 지도 상단 중앙 안내·원형 닫기·즉시 해제를 확인했으며, 이어 거리재기 2개 지점을 찍어 경유점2개·누적3.02km 안내가 같은 위치와 디자인으로 표시되는 것도 확인했다. 오른쪽 매물목록과 겹치지 않았고 운영 데이터 쓰기는 실행하지 않았다.

## 작업 종료 필수 규칙

- 사용자가 명시적으로 `수정하지 말고 상의만` 또는 `배포하지 말라`고 한 경우를 제외하고, 코드 수정은 `테스트 → CODEX_HANDOFF.md 갱신 → 커밋 → 현재 GitHub 브랜치 푸시 → 운영 배포 → 운영 확인 → 배포 기록 추가 커밋·푸시`까지 한 작업으로 완료한다.
- 작업 시작 전에는 `git fetch --prune origin`과 `git pull --ff-only`로 다른 PC의 최신 커밋을 먼저 받는다. 로컬 변경이 있으면 바로 덮어쓰지 말고 stash 또는 별도 커밋으로 보존한 뒤 원격과 비교한다.
- 종료 전 `git status --short --branch`와 원격 추적 상태를 확인해 작업 커밋이 남지 않았고 현재 브랜치가 원격과 일치하는지 확인한다. 개인용·실험용 미추적 파일은 사용자 작업으로 간주해 임의로 커밋하거나 삭제하지 않는다.

## 2026-08-31 상세사진 실제 클릭 복구·부드러운 드래그 전환

- 운영 로그인 화면에서 목록 사진 클릭→상세창, 상세사진 프로그램 클릭→전체화면 확대와 원본 이미지1920px 정상 로드를 확인했지만, 실제 Edge 마우스 입력에서는 드래그 바인딩이 상세사진의 **부모 영역에 pointer capture**를 걸어 pointerup/click 대상이 사진 버튼에서 부모로 바뀔 수 있었다. 이 때문에 일반 클릭 또는 드래그 직후 확대 클릭이 누락되는 회귀가 발생했다.
- pointer capture를 부모가 아니라 실제로 누른 사진/버튼 요소가 직접 잡도록 변경했다. 드래그 후에는 브라우저가 자동 생성하는 합성 click 한 번만 최대180ms 안에서 차단하고, 이후 사용자의 실제 클릭은 즉시 확대창을 연다. 목록 사진→상세창, 상세사진→전체화면 확대의 기존 두 단계와 이전/다음 버튼·키보드 방향키는 유지한다.
- 사진 전환은 현재 장을115ms 동안 이동·페이드아웃한 뒤 다음 장을210ms 동안 반대편에서 이동·페이드인하는 Web Animations 전환으로 바꿨다. 드래그 중에도 이동량에 따라 투명도가 자연스럽게 변하며, 모션 줄이기 OS 설정에서는 애니메이션을 생략한다. 상세창·전체화면·이전/다음 버튼·드래그/터치 모두 같은 전환을 사용하고 인접 사진 선행 로딩은 유지한다.
- 자산 버전은 unified JS/CSS `8.1.40-smooth-photo-expand`다. JS 문법검사, 전체 회귀검사489/489, 정적자산134개 빌드, Wrangler dry-run과 diff 검사를 통과했다. 매물·원본·사진 파일·연락처·찜/메모/확인/계약 상태와 DB는 변경하지 않았다. 운영 배포 및 실제 포인터 검수 결과는 후속 기록에 추가한다.
- 기능 커밋 **`e3bf896`**을 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`4e9e60ff-43b3-46d8-b3b5-0170666b6e09`**로 배포했다. 세 운영 도메인은 HTTP200이고 HTML/JS/CSS `8.1.40-smooth-photo-expand`, 실제 누른 요소의 pointer capture와 부드러운 전환 함수를 운영 자산에서 확인했다. 로그인된 운영화면을 새 버전으로 열어 실제 좌표 마우스 입력으로 목록 사진→상세창→전체화면 확대가 열리는 것을 확인했고, 사진7장 매물에서 좌측 드래그 `1/7→2/7` 뒤 사진 클릭 시 전체화면도 `2/7`로 열렸다. 운영 데이터 쓰기는 실행하지 않았다.

## 2026-08-31 대표매물 합치기 즉시 완료 안내·백그라운드 목록 동기화

- 대표 전체 합치기 저장이 이미 성공했어도 완료 안내가 `unifiedListings` 전체 재조회와 `loadSheet(true)` 전체 목록 재조회까지 순차로 끝난 뒤 브라우저 `alert`로 표시돼, 실제 저장보다 안내창이 한참 늦게 나타나는 원인을 확인했다. 합치기 서버 요청 자체의 실시간성이 아니라 **완료 안내가 무거운 후속 재조회에 묶인 UI 지연**이었다.
- 이제 서버가 D1 저장·캐시 무효화를 성공 응답한 즉시 출발 대표카드를 현재 목록·지도 데이터에서 제거하고, 화면 상단 비차단 안내로 `대표매물 통합 완료 · 최신 목록 동기화 중`을 표시한다. 전체 통합정보와 CSV 재조회는 백그라운드에서 계속하며 완료되면 `최신 목록 반영도 끝났습니다`로 바뀐다. 저장 후 동기화만 실패한 경우 이미 성공한 합치기를 실패로 오인하지 않고 `저장은 완료됐지만 동기화 지연`으로 구분해 안내한다. 기존 확인창·합치기 대상/거래시장 안전검사·D1 저장·캐시 무효화는 유지한다.
- 자산 버전은 unified JS/CSS `8.1.39-live-merge-status`다. JS 문법검사, 대표합치기 집중검사10/10, 전체 회귀검사489/489, 정적자산134개 빌드, Wrangler dry-run과 diff 검사를 통과했다. 실제 운영 매물 합치기는 데이터 변경 작업이므로 임의 매물로 실행하지 않았으며 운영 배포와 비파괴 UI 확인 결과는 후속 기록에 추가한다.
- 기능 커밋 **`a364435`**을 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`c2556bb1-20e5-42c5-a3ec-2226b82e73f5`**로 배포했다. HTML·unified JS·CSS 3개 자산이 업로드됐고 세 운영 도메인은 HTTP200이며 HTML에서 JS/CSS `8.1.39-live-merge-status`, 운영 자산에서 즉시 완료 문구·백그라운드 동기화 호출·비차단 안내 스타일을 확인했다. 검수를 위해 실제 대표매물 합치기는 실행하지 않았으므로 운영 매물·원본·사진·연락처·고객/개인 상태의 추가 변경은 없다.

## 2026-08-31 초기 전체목록 단일 표시·사진 앞뒤 선행 로딩·드래그 전환

- 사용자가 웹을 새로고침하면 IndexedDB의 최근 매물 80개가 먼저 보이고 곧 D1 전체 매물로 바뀌는 현상을 확인했다. 동시에 CSV 대표매물이 통합 원본·사진정보보다 먼저 렌더링되고, 좌표 변환 중 일부 목록을 다시 그리는 경로도 있어 첫 화면의 카드 수와 내용이 여러 번 바뀔 수 있었다.
- 초기 목록은 D1 CSV와 통합 원본·사진 요약을 병렬 요청하되 **두 응답이 모두 준비된 뒤 전체 매물을 한 번만 렌더링**한다. 기존 80개 빠른 캐시는 더 이상 첫 화면에 표시하지 않는다. 좌표 보완 중에도 일부 카드 목록을 그리지 않고 진행 문구만 갱신한 뒤 최종 전체 지도·목록을 표시한다. 자동업데이트, 클러스터 고정 선택, 필터와 개인 상태는 기존대로 유지한다.
- 상세 사진은 현재 장 기준 **다음 장과 이전 장을 함께 선행 로딩**한다. 상세창과 전체화면 확대창의 기존 이전/다음 버튼·키보드 방향키를 유지하면서, 마우스 좌우 드래그와 모바일 터치 스와이프를 추가했다. 세로 스크롤은 유지하고 42px 이상 수평 이동만 사진 전환으로 판정하며, 드래그 직후 상세 확대 클릭이 오작동하지 않게 차단한다. 전체 매물의 모든 원본 사진을 일괄 다운로드하지 않고 사용자가 연 매물의 인접 사진만 준비해 메모리·통신량 급증을 막는다.
- 자산 버전은 map `8.2.22-full-initial-render`, unified JS `8.1.38-swipe-prefetch`, unified CSS `8.1.37-swipe-gallery`다. 전체 회귀검사 **488/488**, JS 문법검사, 정적자산134개 빌드, Wrangler dry-run 및 diff 검사를 통과했다. 수집기·예약·DB·매물 원본·사진·연락처·메모·찜·확인/계약 상태는 변경하지 않았고 재수집은 필요 없다. 운영 배포·브라우저 실검수 결과는 아래 후속 기록에 추가한다.
- 기능 커밋 **`7496768`**을 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`f4541ae7-dd36-4f39-80f9-0615a92444c9`**로 배포했다. 세 운영 도메인은 HTTP200이며 map/unified JS/CSS는 로컬 빌드본과 SHA-256이 일치한다. 운영 HTML도 새 세 자산 버전을 포함하며 Cloudflare Web Analytics beacon 자동 삽입분만 빌드 원문과 다르다.
- 로그인된 운영 화면을 새로고침해 250ms 간격으로 확인한 결과 `카드0개·전체 매물과 사진정보 로딩`에서 곧바로 `매물13,578개 불러옴·가상화 카드18개`로 한 번만 전환했고, 초기80개 캐시 표시 속성은 한 번도 생기지 않았다. 사진4장 실제 매물에서 좌측 드래그로 `1/4→2/4`, 이전 버튼으로 `2/4→1/4`를 확인했으며 돌아온 사진은 `complete=true`, naturalWidth1920으로 즉시 표시됐다. 운영 콘솔 경고/오류는0건이다. 전체 목록 수13,578은 거래시장 전체 원본수이고 화면 우측 DOM18개는 성능용 가상화 창이므로 소량 데이터가 아니다.

## 2026-08-31 자동수집 백그라운드 오판 재시도 수정

- 사용자 화면에서 오늘 11시 실행이 13시41분에도 9/10에서 끝나지 않고, 당근 중구가 `수집 화면의 상태 신호가 8분 동안 없어` 같은 저장 지점을 즉시 재시도 3/4·4/4 하며 수집 탭을 반복해서 다시 여는 현상을 확인했다. 유성구·서구는 목록 전체 확인 후 주소보류만 남은 정상적인 경고 완료였고, 이번 결함은 해당 주소보류 판정이 아니라 **백그라운드 탭의 상태 타이머가 Edge 절전 처리로 지연된 것을 실행 중단으로 오판한 감시 로직**이다.
- 자동수집 확장 **1.1.5**는 1분 상태 신호가 8분간 오지 않으면 곧바로 탭을 재시작하지 않고, 현재 탭의 콘텐츠 수집 세션에 직접 상태 확인을 보낸다. 세션이 살아 있고 진행 화면을 응답하면 마지막 신호/진행 상태만 갱신하고 같은 탭을 유지한다. 탭이 닫힘·폐기됨·응답 없음인 실제 단절은 기존처럼 체크포인트에서 복구하며, 숫자20분 정체와 지역90분 상한도 유지한다. 예약11시, 등록 지역/거래유형, 당근 주소보류 정책, 기존 매물·연락처·사진·메모·찜·확인/계약 상태와 DB는 변경하지 않았다.
- 신규 직접탭 확인 회귀검사를 포함해 집중검사 **31/31**, 전체 **490/490**, 정적자산134개 빌드, Wrangler dry-run, diff 검사를 통과했다. 설치 폴더의 `background.js`·`content.js`·`manifest.json` 3개 프로그램 파일만 검수본과 같은 해시로 동기화했고, 백업은 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-31\collector-code-before-1.1.5-135619`에 있다. 프로필/확장 저장소/로그인/예약/체크포인트는 건드리지 않았다.
- 새 코드를 메모리에 적용하기 위해 자동수집 전용 Edge만 한 번 재시작했다. 실행 중이던 당근 중구는 저장 체크포인트부터 복구되며 전체 실수집의 최종 10/10 완료 여부는 사용자 현황 화면 확인이 남아 있다. 코드 롤백은 이번 기능 커밋 revert와 위 3개 프로그램 파일 백업 복원으로 가능하고 데이터 롤백은 필요 없다.
- 기능 커밋 **`5e6e5c8`**을 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`7ec7ef7f-7767-47ab-a94c-857d4320c287`**로 배포했다. 정적자산 변경은 없어 업로드0건이었고 세 운영 도메인의 HTTP 응답과 원격 브랜치 일치를 재확인했다. 설치본은 이미1.1.5로 동기화·재시작했으며 다음 확인은 현황창에서 중구가 8분마다 새 탭을 열지 않고 완료/보류 판정 후 동구와 최종10/10으로 넘어가는지다.
- 재시작 직후 운영 D1을 읽기 전용으로 확인해 당근 중구 세션이 **13:54:43**, 동구 세션이 **13:57:08**에 `completed`로 종료된 것을 확인했다. 오늘 당근 유성·서구·대덕·중구·동구 5개 구 모두 완전목록 세션이 완료됐고, 마지막 동구는 재시작 전 체크포인트를 이어서 정상 종료했다. 현황창의 최종10/10 표시는 사용자 화면에서 확인하면 된다.

## 2026-08-28 공실박스 혼합수집 거래유형 오분류 수정

- 사용자 신고 그린하우스(중리동258-38, 원본371359)는 실제 `Subtype=1`, 상세 `Moneys=[매매75000]`이고 월세는0인데, 직전 동시저장 코드가 `Jmm=3`을 월세로 읽어 임대0/3 카드를 만들었다. **247313f의 동시저장 판독 결함**이다. 공실박스의 `Bo/Mm/Me/Jun/Jmm`에는 현재 광고와 무관한 과거 숫자가 남을 수 있으므로 양수만으로 거래시장을 추가하면 안 된다.
- 공실박스 **2.2.5**와 서버 공통 판독을 수정했다. 상세 `floorinfo.Moneys`의 실제 거래·금액을 우선하고 목록은 `Subtype`/`LndSubtype`를 사용한다:1매매/2전세/3월세/9반전세,13·31등 복수거래. 순수전세는 제외, 반전세는 `Bjbo/Bjmm` 또는 명시된 상세 월세를 사용한다. `Jun/Jmm`만으로 임대 생성 금지. 진짜 매매+월세 광고는 각각 별도 저장을 유지하며 구버전 수집기 요청도 서버에서 다시 검증한다. 임대수입/메모 숫자로 임대광고 생성 금지 규칙은 유지한다.
- 목록 지문에 거래유형·반전세 필드를 추가했고, 상세조회 후에도 실제 거래조건을 다시 판독한다. 네이버/당근·예약·로그인·지도 UI·DB 스키마는 변경하지 않는다. 종전 이 문서의 **Jun/Jmm 동시 임대 판독 설명은 이 항목으로 정정**한다.
- 운영 원본4848건 읽기전용 검수에서 실제 거래유형과 불일치90건: 오늘 새로 생성된 `::sale/::lease`78건, 과거 단일ID12건. 새78건 중 잘못 생긴 단독카드72개 비노출, 정상대표5개에 붙은 잘못된 원본5개 연결해제, 라임2630750의 잘못된 매매1건을 실제 반전세60000/315로 정정하는 복구를 준비했다. 잘못된 검증대기2건도 제외 대상. **이 항목 작성 시 운영 복구는 아직 미실행이며 아래 후속 기록을 확인한다.** 과거 단일ID12건은 거래시장 변경 이력 가능성이 있어 일괄 삭제하지 않는다.
- `tools/repair-gongsil-false-offers-2026-08-28.mjs`는 기본 읽기전용이며 private backup, 전체 이전행·출처수·사용자 변경/찜/고객참조 검사와 로컬 SQLite 전진/되돌리기·경합 차단 후에만 `--apply` 운영 쓰기를 허용한다. 실제 광고에 해당하는 반대시장 대표·원본도 전후 보존 검증한다. 원본/연락처/사진/이력은 삭제하지 않고 잘못된 시장 연결만 해제하며 단독 오생성 대표는 soft-delete한다. 개인 백업은 저장소 밖 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-28` 하위에 둔다.
- 새12개 포함 전체 **489/489** 회귀검사, 정적자산134개 빌드, Wrangler dry-run 통과. 실제 그린하우스·3층원룸·13/31복수유형·반전세·상세가격 우선·전세제외·구버전 서버요청·클라이언트/서버 동일판정·기존 찜/확인/계약보존을 포함한다. 새 전체 실수집은 임의 실행하지 않았다. 코드 롤백 기준 **5f70b01**이며 데이터는 개인백업의 별도 guarded rollback이 필요하다. 재수집 전 공실박스 페이지 새로고침 후2.2.5로 다시 실행해야 메모리에 남은2.2.4도 교체된다.
- **후속 운영 복구 완료:** 기능 커밋 **45c3d75** 푸시·배포 후 위78건을 실제 복구했다. 단독 오생성 대표72개 soft-delete, 정상 대표5개에 붙은 잘못된 원본5개 연결해제, 라임1개는 동일 대표/원본 ID를 유지해 반전세60000/315로 정정했다. 잘못된 검증대기2건도 제외했다. 원본78개·해당 연락처167행·사진48행은 삭제하지 않고 잘못된 시장 연결만 정정/해제했다. 정상 시장의 원본·대표, 혼합 대표5개와 고객/개인 상태는 전후 전체행 비교로 보존을 확인했다. 복구 이력78건을 남겼다. 과거 단일ID12건은 이력 판별 전이므로 이번 신규 오분류 복구에 포함하지 않았다.
- 복구 직전 건축물 자동 보완이1개 대표의 보조정보를 바꿔 첫 시도는 **전체행 경합 보호에 의해 쓰기 전에 차단**됐다. 금액/상태/연결 변경은 없음을 확인한 뒤 최신 전체 스냅샷으로 다시 실행했고, 보호조건을 완화하지 않은 채 성공했다. 백업/전진SQL/guarded rollback/전후행/캐시내역은 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-28\gongsil-false-offers-2026-08-28T09-56-46-105Z`에 있다. 이미 적용된 작업이므로 `--apply`를 재실행하지 않는다. 되돌리기는 사용자 후속 수정 여부 없는지 확인 후 별도 guarded rollback으로만 수행한다.
- 재생성 가능한 목록·운영현황·revision·해당 상세 **조회 캐시83개만** 제거했고 원본 사진/사용자 데이터는 삭제하지 않았다. 초기 Worker **21b3a1cb-75e6-45f2-8a65-c96dce07a5b2** 배포 후 복구·캐시 갱신 시 동일 코드를 재배포해 메모리 캐시도 교체했다. 최종 Worker **9724267f-3682-48bf-a67e-3933c85859ae**. 세 운영 도메인의 HTML/공실박스 JS 총6개 HTTP200·빌드본 SHA-256 일치·2.2.5를 확인했다. 마지막 전체 회귀검사도 **489/489** 통과했다.
- 운영 표본 재조회에서 그린하우스371359는 활성 매매75000(7억5000만원), 잘못된371359::lease는 비활성·연결없음, 잘못된 임대 대표는deleted임을 확인했다. 라임은 활성 임대60000/315, 퍼스트상가841436의 실제 매매37000 및 임대2000/150 원본은 모두 유지했다(연결된 기존 대표의 다른 원본 우선조건을 이번 작업으로 덮어쓰지 않는다).
- 설치본 `C:\Users\ITP\AppData\Local\JSMap\EdgeAutoCollector\extension\collectors\gongsil-collector.js` **프로그램 파일1개만** 백업 후2.2.5로 동기화하고 SHA-256 일치를 확인했다. 이전 파일은 저장소 밖 `gongsil-code-before-2.2.5` 폴더에 있다. 네이버/당근·확장1.1.4·예약11시·대상 설정·브라우저 프로필은 그대로다. **사용자는 웹 새로고침으로 복구 결과를 확인하며, 다음 수집 전 공실박스 페이지 새로고침 후 수집창2.2.5를 확인한다.** 이번 기존 데이터 복구를 위해 전체 재수집할 필요는 없다. 로그인된 운영 화면과 수정 후 새 제공처 실수집은 아직 직접 검증하지 않았으며, 배포/DB 검증과 구분한다.

## 2026-08-28 당근 부분완료 원인 조사·주소 보류와 실제 실패 분리

- 사용자 제공 자동수집1.1.3의 오늘11시 결과를 운영 D1 읽기전용으로 조사했다. 당근5개 구 모두 전체 목록을 확인했다: 유성2078/상세42/주소보류19, 서구3876/56/27, 대덕613/15/13, 중구641/10/5, 동구641/27/23. 합계 **목록7849건·기존동일7699건·상세처리150건·주소보류87건**이다. 기존 현황의 `42/2078` 등은 상세처리만 분자로 쓰고 기존동일 생략을 누락한 표시였으며 전체 목록이42건만 수집된 것은 아니다.
- 해당87개 collector_raw 오류는 모두 `정확한 지번주소 없음`, 원본은 모두 `isHideAddress=1`이었다. 42건은 단지/건물 도로명 정보만 있고 46건에 보이는 숫자 지번은 **중개사무소 bizProfile 주소**였다(두 집계는 중복 가능). 중개사무소 주소·동 중심/공개 좌표·복수 건물 단지 주소를 매물의 정확한 번지로 대입하지 않는다. 해당 세션의 실제 통신/저장 실패는0건이었다. **87건은 미해결 주소보류로 유지하며 지도에 복구했다고 주장하지 않는다.** 이번 작업에서 원본/대표매물/사용자 데이터·DB 스키마는 변경하지 않았다.
- 자동수집 확장 **1.1.4**: 전체 목록 증거와 기존동일+상세처리 수를 함께 검증해 표시한다. 주소 보류만 있는 완전 목록은 `완료·주소보류`, 통신/저장 실패는 별도 수치로 표시한다. 종료 요약도 `정상 / 부분·보류 / 실패`로 나눠 전부 정상처럼 보이지 않게 했다. 원본번호와 사유만 최대60건씩 보고서에 저장하고 펼쳐 볼 수 있다(재시도 중 오류 기록도 포함; 원문·연락처는 추가 저장하지 않음). 사유는 HTML escape하고 숫자 원본번호만 당근 링크를 만든다. 자동5초 갱신 때 펼친 내역은 유지한다.
- 당근 수집기 **1.5.2**는 주소보류와 조회·저장실패 통계를 분리하고 사유/원본번호를 펼쳐 보여준다. 서버는 전체 목록 완료 후 주소보류 안내를 명시한다. 기존 완료 판정/미노출 안전장치/실제 실패의 재시도 정책/다음 수집에서 오류 원본 재확인은 유지했다. 주소 비공개를 통신 오류로 간주해 같은 자료를 무한 재시도하지 않는다. 네이버·공실박스·월세/매매 대상·예약11시·대표 연결·찜/메모/사용자 상태는 변경하지 않았다.
- 구버전 보고서는 표시만 보완한다. 저장되지 않은 기존동일 수나 과거 사유 목록을 소급 생성하지 않으며 `이전 기록: 대상 / 상세 처리`로 구분한다. 새 `완료·주소보류`/원본별 사유 보고는 업데이트 후 실행 결과부터 적용된다. 이전 완료 기록을 바꾸기 위해 전체수집을 강제 재실행하지 않았다.
- 새16개 포함 전체 **477/477** 회귀검사, 자산134개 빌드, Wrangler dry-run 통과. 테스트는 구별 건수, 불일치 증거 거부, 실제 실패 재시도, 주소만 보류 시 다음 구 진행/보고서 저장, 중개사 주소·비공개 좌표 오용 차단, 구버전 호환, 진단 escape/원문 제외 등을 포함한다. `tools/preview-daangn-report.mjs`의 실제 현황 HTML/JS와 모의 Chrome API로 브라우저 펼침·주기 갱신 후 유지·가로 넘침 없음·상태 배지 한 줄을 확인했다. **이는 운영 제공처 재수집 성공 검증과는 구분하며, 업데이트 후 실제 당근 수집 결과는 추가 확인이 필요하다.**
- 롤백 기준은 **3df9835**(직전 공실박스 동시저장 완료)이다. 기능 커밋 revert/재배포 및 확장 프로그램 파일 복원으로 되돌릴 수 있고 DB 롤백은 필요 없다. 배포/설치 파일 동기화/원격 확인 기록은 아래에 추가한다. 사용자는 Edge 확장 프로그램 `다시 로드` 후1.1.4를 확인해야 메모리에 남은1.1.3도 교체된다.
- 기능 커밋 **4b756d4**를 원격 브랜치에 푸시하고 Worker **f0d59af5-738a-4183-a306-5f1a79611a0f**로 운영 배포했다. `js-map.com`, `www.js-map.com`, Workers 도메인의 HTML·당근 JS HTTP200과 빌드본 SHA-256 일치, 운영 수집기 **1.5.2**를 확인했다. `C:\Users\ITP\AppData\Local\JSMap\EdgeAutoCollector\extension`의 background/manifest/options.js/options.css 및 collectors/daangn-collector.js **5개 프로그램 파일만** 동기화하고 해시 일치를 확인했다. 설치 스크립트·브라우저 종료·프로필/설정/예약 변경은 실행하지 않았다. **설치 파일 갱신과 실제 메모리의 확장 재로드는 별개이며 사용자 다시 로드가 남아 있다.** 새 실제 수집은 아직 실행하지 않았고87건의 주소보류도 그대로다.

## 2026-08-28 공실박스 동일 광고의 임대·매매 동시 저장

- 사용자가 공실박스 한 카드에 `매`와 `월`이 함께 표시되면 양쪽에 저장해야 한다고 요청했다. 운영 D1 읽기전용 조회에서 퍼스트상가 원본 `841436`은 `Me=37000 / Bo=2000 / Mm=150`이 함께 있지만 매매 원본만 저장된 것을 확인했다. 이번에는 **수집기와 저장 경로를 수정했으며 기존 운영 원본 일괄 복구나 재수집은 실행하지 않았다.**
- 공실박스 수집기 **2.2.4**와 서버는 실제 광고의 `Me` 등 매매가, `Bo/Mm`, 월세가 있는 `Jun/Jmm`, 거래유형이 명시된 `Moneys`를 판독한다. 매매와 월세가 함께 있으면 상세·사진·연락처는 한 번 수집하고 거래조건을 두 개로 분리 저장한다. `TotBomoney/TotMmmoney`나 설명문의 월세수입만으로 임대 광고를 만들지 않으며 순수 전세도 월세로 만들지 않는다. 보증금 명시 0은 허용하고 누락·음수는 새 동시 임대조건으로 추정하지 않는다.
- DB의 기존 원본 유일키를 변경하지 않고 새 공실박스 원본은 내부적으로 `원본번호::sale` / `원본번호::lease`로 식별한다. 기존 단일 ID가 해당 거래시장에 있으면 그 ID·대표 연결을 유지하고 반대 거래조건만 추가한다. 검증대기 원본의 기존 ID도 보존한다. 제공처의 실제 원본번호는 `providerSourceId`와 원본 `Bfidx`에 유지한다. 임대료 수정이 매매를 덮어쓰거나 두 시장이 자동통합되지 않으며 기존 대표 ID에 연결된 찜·메모·확인/계약 상태를 이번 분리 때문에 이동/초기화하지 않는다. 기존 광고 재등장에 따른 상태 정책은 변경하지 않는다.
- 목록 비교는 거래조건별로 검사하되 브라우저에 원래 광고번호 한 번만 반환한다. 어느 한쪽이라도 누락되었으면 상세를 다시 받고, 양쪽이 그대로면 기존처럼 생략한다. 원본 수와 저장 거래조건 수(`offerReceived`)를 구분해 1개 광고가 2개 저장되어도 수집 진행 수가 어긋나지 않도록 했다. 같은 배치에서 임대·매매 후보 캐시가 서로 지워지던 부분도 보완했다.
- 완료 보고의 `observedOffers`로 실제 관찰한 거래조건만 광고 존재 상태를 갱신한다. 혼합수집·거래별 관찰정보가 없는 구버전 완료 보고로는 미노출 횟수를 증가시키지 않는다. 정상적인 단일 거래시장 전체수집의 미노출 판정은 유지한다. 새 ID 조회는 최대99바인딩의 인덱스 조회로 묶어 실행하며 원본 payload를 전체 다운로드하지 않는다. 네이버/당근 수집기·예약·지도·가격 표시·DB 스키마는 변경하지 않았다.
- 새11개 포함 전체 회귀검사 **461/461**, 실제 SQLite 기반 API 분리저장·재수집·조건변경·검증대기 ID·기존 상태·배치통합·거래별 미노출 검사, 자산134개 빌드 및 Wrangler dry-run 통과. 별도 과거 도구 `tools/test-gongsil-collector.cjs`는 기대 버전 `1.8.1`에 고정되어 시작 assertion에서 실패하는 기존 비호환 도구이므로 성공 검사에 포함하지 않았다. 현재 테스트 대상은 `tests/*.test.mjs tests/*.test.cjs`이며 실제 제공처 재수집 검증과 구분한다.
- **사용자 확인:** 진행 중 수집은 임의 중단하지 않는다. 공실박스에서 새 수집기 `2.2.4`를 연 뒤 매매·월세가 함께 표시된 광고 1개로 재수집 → 웹의 상가임대와 해당 매매 시장에서 각각 금액/사진/연락처 확인 → 같은 광고 재수집 시 중복 카드가 늘지 않는지 확인한다. 이미 매매만 저장된 광고의 임대조건은 재수집해야 반영된다. Edge 설치본의 수집기 파일 동기화 및 운영 배포 결과는 아래에 기록한다.
- 코드 복원 기준 **26a1de7**. 새 방식 실수집 전에는 기능 커밋 revert 후 재배포할 수 있다. 실수집 후에는 거래별 새 ID가 존재하므로 구버전 수집기를 무조건 실행하면 중복 생성 위험이 있다. 되돌릴 때는 수집을 중단하고 관련 원본/대표/검증행을 백업·대조한 뒤 호환 처리를 결정하며 새 거래조건을 임의 삭제하지 않는다.
- 기능 커밋 **247313f**를 `origin/codex/cloudflare-js-map`에 푸시한 뒤 Worker **7ffed2e4-515d-4ce8-9ee8-88d02d315743**로 배포했다. 세 운영 도메인의 HTML/공실박스 JS 총6개 응답이 HTTP200이며 로컬 빌드본 SHA-256과 일치했고 수집기 `2.2.4`를 확인했다. 운영 DB 수동 쓰기·캐시 삭제·재수집 실행은 없다. 이 사무실 PC 설치본 `C:\Users\ITP\AppData\Local\JSMap\EdgeAutoCollector\extension\collectors\gongsil-collector.js` 한 파일도 검수본과 같은 해시로 동기화했다. 다른 수집기·확장 버전 `1.1.3`·예약·브라우저 프로필은 그대로이며 실행 중인 탭의 코드까지 교체/재시작하지는 않았다. 다음 수집 전 공실박스 페이지에서 `2.2.4` 표시를 확인해야 한다. 다른 PC의 설치본은 Git pull만으로 자동 갱신되지 않으므로 해당 PC에서도 수집기 파일 갱신이 필요하다.

## 2026-08-28 매매 상세 안내문·토지 목록 가독성 개선

- 사용자 화면에서 평당가 안내문과 출처 설명에 가격용 큰 파란 글씨가 적용되는 문제를 확인했다. `.unified-detail-summary-v8 > p`와 데스크톱의 `!important` 가격 규칙이 일반 안내문에도 적용된 것이 원인이다. **매매 계산 안내문만 12px·보통 굵기·회색으로 제한**하고 가격 강조, 계산식과 주의사항 내용은 유지했다.
- 출처·제공처 확인일·기술적인 필드 경로는 기본 접힘 `출처·확인일 보기 · 제공처`로 바꿨다. 클릭/키보드로 펼치면 기존의 전체 확인일과 출처·계약 가능 여부 주의문을 볼 수 있다. 제공처 문자열은 계속 HTML 이스케이프하고 미제공 확인일을 등록일로 대체하지 않는다.
- **토지 매물리스트의 지목은 연초록, 용도지역은 연파랑 배지**로 구분하고 값은 13px 굵은 글씨로 키웠다. 긴 용도지역은 줄바꿈하며 미확인값은 회색 배지로 유지한다. 건물/임대 카드에는 새 토지 배지를 추가하지 않는다. 기존 원본 선택·지목/용도지역 값·가격·수익률·수집기·DB·사진·전화번호·찜·메모·확인/계약 상태·지도 동작은 변경하지 않는다. 재수집은 필요 없다.
- 실제 카드 생성 함수와 운영 CSS 전체를 쓰는 `/sale-cards` 검수에 **운영 상세 wrapper**를 포함했다. 이 구조로 가격은18px인 채 안내문12px/weight400을 확인했고, 360/450/600px 목록에서 배지 넘침과 버튼 겹침이 없음을 DOM 치수로 검증했다. 접힘/펼침과 390px 모바일 CSS 화면도 브라우저로 확인했다. 운영 로그인 세션의 실제 매물 클릭 검수와는 구분한다.
- 신규4개 포함 전체 **450/450** 회귀검사, JS 문법 검사, 자산134개 빌드, Wrangler dry-run 및 diff 검사 통과. 거래 CSS `1.4.2-land-readability`, workbench `1.0.2-land-readability`로 자산 버전을 갱신했다. 코드 복원 기준 **2344d2b**이며 기능 커밋 revert 후 재배포만 필요하고 데이터 복원은 없다. 배포 결과는 아래에 추가 기록한다.
- 기능 커밋 **4058a6e**를 원격에 푸시하고 Worker **8054a444-68ed-49ea-a9ee-87e8dd6cbe4d**로 배포했다. 세 운영 도메인의 HTML/거래 CSS/workbench JS 총9개 응답이 모두 HTTP200이며 로컬 빌드본 SHA-256과 일치했다. 운영 데이터/조회 캐시 삭제는 없다. 사용자는 새로고침 후 토지 목록의 초록·파랑 배지와 상세의 작은 회색 안내문, `출처·확인일 보기` 펼침을 확인하면 된다.

## 2026-08-28 토지이음·밸류맵 정확한 지번 바로 연결

- 사용자가 외부 조회 버튼을 눌러도 지번이 전달되지 않는다고 지적했다. 기존 구현은 공식 홈페이지 열기와 주소 복사만 지원했다. 이제 매매 상세의 외부 조회 창에서 **선택한 원본의 지번을 확인한 뒤 같은 필지(PNU)를 토지이음과 밸류맵에 전달**한다. 수집기, 임대/매매 분류, 가격, 지도 좌표, 연락처, 사진, 메모, 찜, 사용자 확인/계약상태, DB는 변경하지 않는다. 재수집은 필요 없다.
- 새 `js/parcel-external-links-v1.js`는 기존 카카오 주소검색 SDK의 EXACT 검색으로 주소명·법정동코드·산 여부·본번/부번이 일치하는 단일 필지를 확인한다. PNU 19자리와 두 사이트 고정 호스트만 허용한다. 동 이름만 있는 주소·층/호실만 있는 주소·도로명·여러 필지 표기에는 잘못된 바로가기를 만들지 않고 정확한 지번 입력 또는 주소 복사/홈페이지 직접 검색을 안내한다. 좌표 역지오코딩·건물명 검색·다른 대표매물 주소로 대체하지 않는다.
- 기존 JS 매물의 시 생략 주소는 **동구/중구/서구/유성구/대덕구에 한해서 대전** 기준으로 명시적으로 정규화하며 조회창에 안내한다. 전체 주소가 있으면 해당 시·도를 보존한다. 사용자가 조회용 주소를 고칠 수 있지만 저장된 매물 주소를 바꾸지는 않는다. 입력 변경·창 닫기·매물 전환 시 이전 비동기 응답/링크는 적용하지 않는다. 8초 제한, 실패 재시도, 중복 조회 합치기, 성공 10분 메모리 캐시(최대100개)를 사용하고 개인 저장소/DB에는 쓰지 않는다.
- 토지이음은 `https://www.eum.go.kr/web/ar/lu/luLandDet.jsp?selGbn=umd&isNoScr=script&s_type=1&mode=search&add=land&pnu=...`, 밸류맵은 `https://www.valueupmap.com/properties/lands/...`를 사용한다. 두 사이트의 실제 검색 UI에서 도마동49-38(PNU3017010300100490038)을 찾은 뒤 독립 새 탭으로 동일 필지 열기를 확인했다. 외부 자료 자동 동기화가 아니며 외부 사이트 로그인/유료 기능/추후 URL 변경은 별도다.
- 집중 회귀검사 **11/11**, 전체 **446/446**, JS 문법 검사, 자산134개 빌드, Wrangler dry-run 통과. 실제 조회창의 정상 링크·주소 수정 시 해제·조회 실패·지번 누락은 로컬 `/parcel-links` 모의 SDK로 확인했다. 실제 외부 사이트의 해당 필지 열기는 별도 브라우저로 확인했다. `/parcel-links?live=1`은 운영과 같은 공개 카카오 SDK를 사용하는 선택적 검수 모드이나 로컬에서는 SDK 준비가 되지 않아 실제 SDK 통합 검증은 완료하지 못했다. 운영 로그인 세션이 없어 로그인 후 조회창의 전체 실동작은 사용자 최종 확인이 필요하다. 이를 모의 테스트/외부 URL 확인과 혼동하지 말 것.
- 자산 버전: parcel links `1.0.0`, workbench `1.0.1-parcel-links`, 거래 CSS `1.4.1-parcel-links`. 인증된 스크립트의 의존성 순서를 보장한다. 코드 복원 기준 **b66ec74**이며 기능 커밋 revert 후 재배포만 필요하고 데이터 복구 작업은 없다.
- 기능 커밋 **e55678d**를 원격에 푸시하고 Worker **597c842c-fd2e-4698-8071-6cdbc78c47b7**로 배포했다. 세 운영 도메인의 HTML/새 parcel JS/workbench JS/거래 CSS, 총12개 응답이 모두 HTTP200이며 로컬 빌드본 SHA-256과 일치했다. DB 쓰기·수집 실행·캐시/사용자 데이터 삭제는 없다. 사용자는 새로고침 후 매매 상세 → `토지이음 · 밸류맵`에서 확인된 지번과 `해당 필지` 링크를 확인하면 된다.

## 2026-08-27 매매 검토 도구·필터 확장 및 공실박스 지목/용도 누락 복구

- 사용자 요청으로 매매가를 억/만원으로 표시하고, 건물매매에 대지·연면적·단순 연 수익률·건물 구분 필터, 토지매매에 면적·평당가·지목·용도지역 필터를 추가했다. 토지의 기존 수익률 자리에는 광고면적 기준 평당가를 표시한다. 선택 조건 칩의 개별 해제, 시장 변경 시 숨겨지는 조건 초기화와 임대 복귀를 지원한다. 공실박스 지목의 `임/과/종` 등 약칭은 필터에서 정식 명칭과 동일하게 비교하되 수집 원본값은 보존한다. 임대 카드/클러스터 로직은 유지한다.
- 매매 상세에 토지이음·밸류맵 공식 사이트 열기와 주소 복사를 추가했다. **확인되지 않은 필지 딥링크·좌표를 만들거나 외부 자료를 자동 수집하지 않는다.** 외부 사이트에서 복사한 주소로 검색하며 로그인/유료 서비스는 해당 사이트 정책을 따른다.
- 수익 계산·검토 도구는 대출/이자율/공실률/운영비/취득비용 가정을 별도로 입력한다. 연 현금흐름 ÷ (매매가−임대보증금−대출+취득비용)이며 원금상환·소득세·양도세는 제외한다. 원본의 월수입·광고 수익률과 메인 카드 단순 연 수익률은 덮어쓰지 않는다. 계정별 `saleWorksheetV1` cloud_state에 검토 메모와 최대30개 필지(지번/면적)를 저장하며 버전 비교로 다른 PC의 최신 내용을 덮어쓰지 못하게 한다. 기존 조회 실패 시 저장을 잠그고, 검토 창 전환 시 늦은 응답은 적용하지 않는다. 복수 필지는 일괄 매매 검토용 합계·평당가만 계산하며 **매물 자동합치기·지도 좌표 변경은 하지 않는다.**
- 매매 카드 2~4개 체크 후 선택 작업줄에서 비교표를 연다. 카드에 이미 있는 요약을 사용하므로 추가 전체목록/상세 일괄 조회는 하지 않는다. 목록 요약에 없는 건물 용도지역 등은 비교표에서 미확인이며 각 매물 열기로 원본 상세를 확인한다. 원본 상세에는 제공처와 확인일, 공실박스 지목/용도지역 필드 경로를 표시한다. 등록일을 확인일로 오인하지 않는다.
- **누락 원인:** 공실박스 토지 원본 `2618535/2619186` 등의 `detail.getlands` 값은 비어 있지만 `list.JiMok`(대문자 M), `list.YongdoAddr`, `detail.bilinfo.jimok/yongdoaddr`에는 값이 있었다. 기존 로직은 잘못된 `Jimok` 표기와 지적정보만 읽어 실제 제공처 정보를 놓쳤다. 서버가 해당 경로를 보완하고 공실박스 수집기 **2.2.3**도 목록 필드를 보존한다. 구버전 매매 스냅샷은 다음 수집에서 상세를 한 번 재확인하도록 metadataVersion=2를 사용하며 검증대기 별칭에도 버전을 보존한다. 기존 네이버/당근 설명문 수집과 임대 수집/예약/완전수집 안전장치는 변경하지 않았다.
- 운영 Gongsil 매매 원본2,120개 중 **2,118개**의 저장된 상세 메타데이터만 백업·기존 스냅샷/수정시각 비교 후 보완했다(전후 정확일치2,118, 경합 변경0). 신규 누락 복구는 전체 중 지목355/용도지역341이고, 토지95개 중 누락42개의 지목/용도지역을 모두 복구했다. 최종 읽기전용 집계 **토지95/95 지목 있음, 95/95 용도지역 있음**. 나머지 보완은 주로 지적공부 면적·토지형상 등이다. 대표정보·매매가·광고면적·사진·연락처·메모·찜·사용자 확인/계약상태·통합연결·raw_json은 수정/삭제하지 않았다.
- 광고면적과 지적공부 면적이 다른 원본(예:2480845,2452014)은 광고면적을 덮어쓰지 않고 별도 지적공부 면적과 차이 안내를 표시한다. 일부 지분/복수 필지 여부는 사람이 확인해야 하며 평당가는 광고면적 기준이다.
- 복구 도구 `tools/repair-gongsil-sale-metadata-2026-08-27.mjs`는 기본 읽기전용이고 외부 개인 백업 경로와 `--apply`를 명시해야 쓰기한다. 정방향·롤백·경합 보호를 SQLite로 검사한 뒤 적용했다. 실제 백업/롤백 SQL은 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-27\gongsil-metadata-2026-08-27T14-03-07-545Z`이다. 이미 적용했으므로 무조건 재실행하지 않는다. 코드 복원 기준 **f39d7ca**이며 코드 revert/재배포와 이 메타데이터 guarded rollback은 별도다. 검토 데이터 scope는 복원 시 임의 삭제하지 않는다.
- 전체 회귀검사 **435/435**, 자산133개 빌드, Wrangler dry-run 통과. 로컬 실제 UI 검수 페이지에서 시장별 필터, 임대 복귀, 토지 평당가/지목/용도지역, 외부 조회, 수익 계산과 개인 검토 저장/재열기(모의 계정 API), 복수 필지 합계, 비교표를 확인했다. 실제 운영 DB에는 검토용 가짜 매물을 만들지 않았다. 상세 캐시를 `unified-detail-v5-sale-metadata`로 갱신해 복구 전 캐시가 남지 않게 했다.
- **실수집 미완료/다음 확인:** 연결 가능한 브라우저는 in-app뿐이며 공실박스 미로그인 상태였다. 사용자 Edge 수집 브라우저는 연결되지 않아 새 전체/소량 수집을 실행하지 못했다. 이를 사용자에게 알렸으며 로그인 연결 후 공실박스 **2.2.3** 소량 수집과 제공처 상세/D1/화면 대조가 필요하다. 기존 토지95개의 메타데이터는 재수집 없이 복구했으나 과거 제외35건의 개별 사유는 구버전이 기록하지 않아 이번 데이터로 확정하지 않는다. 새 수집기의 제외 사유 기록을 확인해야 한다. 배포 완료와 실제 제공처 전체수집 완료를 혼동하지 말 것.

- 기능 커밋 **299c838**을 원격에 푸시하고 Worker **58e4eb39-0661-4229-92ca-4396bfd5d3d1**로 배포했다. 세 운영 도메인 모두 HTML/변경 JS5개/CSS의 HTTP200 및 로컬 빌드본 SHA-256 일치를 확인했다. 복구된 목록 요약을 재생성하도록 `api-cache/unified-listings-v5-source-aware-review.json` **조회 캐시 1개만** 제거했다. 상세는 새 캐시 namespace를 사용한다. 원본사진/사용자 데이터 삭제는 없다. 새로고침 후 건물/토지매매 필터·상세 검토 도구·2~4개 비교를 확인하면 된다. 수집기는 다음 실행 시 **공실박스2.2.3**을 확인하되 이미 진행 중인 수집을 강제 초기화하지 않는다. 로그인한 운영 UI/실제 새 제공처 수집은 위 제한에 따라 별도 확인이 남아 있다.

## 2026-08-27 토지 매물카드 지목·용도지역 표시

- 사용자 요청에 따라 토지매매 목록 카드의 가격·면적 아래에 **지목 / 용도지역** 한 줄을 추가했다. 예: `지목 대 · 용도지역 제2종일반주거지역`. 저장된 값만 표시하며 미제공 항목은 `미확인`으로 남긴다. 건축물 용도를 토지의 용도지역으로 추정하지 않는다. 기존 임대·건물매매 카드의 항목은 유지한다.
- 통합목록의 토지 `saleSummary`에 `landUse`와 `zoning` 문자열만 추가했다(공백 정리·최대160자). 기존에 읽는 스냅샷을 사용하므로 카드마다 상세 API를 더 호출하지 않는다. 출처/대표 링크/가격에 맞는 원본 정보를 사용하고 복수 후보의 지목·용도지역이 충돌하면 임의로 섞지 않는다. DB·스키마·수집기·사진·연락처·찜·메모·사용자 상태·예약은 변경하지 않았다.
- 전체 회귀검사 **425/425**, 자산132개 빌드, Wrangler dry-run, diff 검사를 통과했다. 실제 카드 렌더러를 사용하는 로컬 `/sale-cards` 화면에서 600/450/360px의 정상·정보누락·긴 용도지역 토지 카드9개 모두 별도 정보줄, 넘침 없음, 버튼 아래 배치를 확인했다. 운영 D1 읽기전용 표본에서도 `전/제2종일반주거지역`, `대/제2종일반주거지역`, `임야/개발제한구역`과 실제 누락값을 확인했다(쓰기0). 운영 로그인 화면 검수와는 구분한다.
- 자산 버전은 거래 CSS `1.3.0-land-info`, 거래 UI `1.4.0-land-info`, 공통 JS `sale-metrics-v1=2`이다. 새 자료 수집 없이 기존 저장값으로 표시된다. 코드 복원 기준은 **`72b379b`**이며 기능 커밋 revert·재배포 후 통합목록 조회 캐시를 갱신한다.
- 기능 커밋 **`ca6a381`**을 원격에 푸시하고 Worker **`b3157586-19d4-4902-8e68-a86316c837bd`**로 배포했다. 세 운영 도메인 모두 HTML·변경 JS2개·CSS의 HTTP200과 로컬 빌드본 SHA-256 일치를 확인했다. 새 요약이 재생성되도록 통합목록 조회 캐시 `api-cache/unified-listings-v5-source-aware-review.json` **1개만** 제거했으며 원본 사진·매물·사용자 데이터는 삭제하지 않았다. 사용자는 새로고침 후 `토지매매`에서 지목·용도지역을 확인하면 된다. 운영 로그인 화면은 사용자 확인이 남아 있으며 재수집은 필요 없다.

## 2026-08-27 매매 설명문 정보 보완·공실박스 완료/제외 사유 표시

- 네이버 수집기 **6.0.3**은 매매 상세의 긴 설명문을 보존하고, 서버는 네이버·당근의 기본값을 우선하여 설명문의 명시적인 매매가·융자·임대보증금·월 임대수입·이자 차감 후 월수익·실투자금·광고 연 수익률·대지/연면적·총층/세대수를 보완한다. 단위 없는 금액·범위·충돌값은 추정하지 않는다. 기본정보와 설명이 다르면 기본값을 유지하면서 설명의 값과 경고도 표시한다. 원문은 상세에서 펼쳐 확인할 수 있고 외부 연락처는 기존 수집 정책에 따라 제거한다.
- **이자 차감 후 월수익은 일반 월세수입이 아니다.** 이를 단순 연 수익률 계산에 넣지 않는다. 계산할 자료가 없고 광고 수익률만 있으면 카드에 `광고 연 6.53%`처럼 명확히 구분한다. 기존 단순 수익률 공식은 유지한다. 네이버·당근의 기존 매매 원본은 설명 추출 버전이 없을 때 다음 수집에서 한 번 상세를 다시 확인한다. 검증대기 원본/별칭도 버전을 기억해 매번 재수집하지 않는다. 임대와 공실박스의 기존 비교/연락처 갱신 규칙은 유지한다.
- 당근 **4084712**의 원본은 TWO_ROOM/3층이지만 설명에 대지183.1㎡·연299.88㎡·3층·18세대와 수익형 다가구가 명시되어 있었다. 명확한 복수 조건일 때만 건물 전체/다가구로 보완하고 추론 표시를 남긴다. 해당 단일 매물을 버전/기존값 보호 조건 및 SQL 복원 검증 후 실제 보완했다: 매매85000만원 유지, 대지183.1㎡·연299.88㎡, 보증금17880만원, 융자30000만원, 이자 차감 월수익202만원, 실투자37120만원, 광고 연6.53%. 사진3개·메모·연락처·찜·고객연결·사용자 상태는 변경되지 않았음을 전후 비교했다.
- 해당 원본 `O-ba39f279-bdd9-41f1-9be9-04463773a267`, 대표 `M-04be49f5-54dd-4134-af29-fa791e987d98`. 개인 백업/정방향/롤백 SQL은 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-27\daangn-sale-description-2026-08-27T10-55-34-408Z`에 있으며 저장소에는 개인정보 백업을 넣지 않는다. `tools/repair-daangn-sale-description-2026-08-27.mjs` 기본 실행은 읽기 전용이고 `--apply`는 이 단일 초기 버전만 허용한다. 이미 적용했으므로 다시 실행하지 않는다.
- 공실박스 수집기 **2.2.2**: 처리 종료를 `수집 완료 · 제외/실패 N건`으로 명확히 표시하고 원본번호·단계·사유를 최대200건 기록/표시한다. 서버/운영현황에도 민감정보를 제거한 진단을 전달한다. 모두 필수정보 제외인 경우도 종료 결과를 남기되 빈 목록을 이전 체크포인트와 혼동하지 않는다. 불완전 수집을 완전수집으로 승격하거나 기존 광고 미노출을 계산하지 않는다.
- 기존 아파트797건 세션 `GONGSIL-1787824235250-eip4fz7n`은 처리가 끝난 부분결과였다(신규699/통합46/검증39/중복2/필수제외10/실패1). **구버전은 제외된 개별 사유를 서버에 남기지 않았으므로 이번 개선으로 과거 사유가 소급 생성되지는 않는다.** 토지130건 중35건 제외는 후속 조사 대상이다. 해당 조사 전 임의 재수집/삭제/별도등록은 하지 않는다.
- 신규13개 포함 전체 **420/420** 회귀검사, 정적132개 빌드, Wrangler dry-run, diff 검사 통과. 실제 HTML 함수로 만든 로컬 검수 화면에서 설명충돌·4084712 재무/범위·기존 공실박스 건물/토지 표시와 제외사유 펼침을 확인했고 콘솔 오류/경고는 없었다. 이 검사는 수정 후 전 제공처 실수집 완료를 의미하지 않는다. 현재 운영 네이버 매매 원본은0건이므로 실제 네이버 매매 상세 수집은 소량 표본 확인이 필요하다.
- 코드 복원 기준은 `544ba3d`이다. 기능 revert/재배포와 위 단일 데이터 롤백은 구분한다. 수집 확장1.1.3·당근 클라이언트1.5.1·예약·대상·브라우저 프로필은 변경하지 않았다. 이미 열린 공실박스2.2.1은 완료 후 수집창을 새로 실행해2.2.2를 확인한다. 배포/원격 일치 확인은 다음 기록을 참조한다.

- 기능 커밋 **`aeaa9e5`**를 원격 브랜치에 푸시하고 Worker **`80ed9216-6b93-4cb8-9af8-7412e62942c5`**로 배포했다. 세 운영 도메인 HTTP200 및 공실박스/네이버/매매UI/운영현황 자산 SHA-256 일치를 확인했다. 단일 복구 반영을 위해 재생성 가능한 목록·운영현황·revision·해당 상세 캐시6개만 제거했으며 원본 사진/데이터는 삭제하지 않았다. 실제 로그인한 운영 UI와 새 제공처 실수집은 사용자 확인이 남아 있다.

## 2026-08-27 당근 자동수집 구 단위 표시 화면 고정

- 사용자 화면에서 구 단위로 등록한 당근 자동수집이 실제로는 구 전체를 수집하면서 지도에는 동 단위 클러스터가 표시되는 혼선을 확인했다. 당근에서 구 클러스터를 누르면 `cluster_id`는 구 단위로 저장되지만 URL의 지도 확대 단계는 동 화면으로 바뀌는 것이 원인이었다. 운영 D1 읽기 전용 검사에서 수정본 `1.5.1`이 구 ID `REGION:1154/1130/1166/1112/1095`로 유성구 2,074개·서구 3,880개·대덕구 618개·중구 642개·동구 645개를 확인해 실제 범위 누락은 없음을 별도로 검증했다.
- 자동수집 확장 **1.1.3**은 당근의 이름 있는 구 대상만 실행 URL의 지도 확대 단계를 구 화면 `11`로 정규화한다. 구 클러스터 ID·구 이름·월세/매매 필터·등록 대상 키는 유지하므로 수집 결과의 범위와 개수는 바뀌지 않는다. 네이버·공실박스, 이름 없는 개별 당근 클러스터와 수동 수집 화면은 건드리지 않는다.
- 새 회귀검사는 구 ID와 필터가 보존되면서 표시 확대 단계만 바뀌는지, 다른 제공처와 개별 클러스터의 URL이 그대로인지 확인한다. 전체 테스트 **407/407**, 정적 자산 132개 빌드, Wrangler dry-run과 `git diff --check`를 통과했다. D1/R2·매물·연락처·사진·메모·찜·사용자 상태·오전 11시 예약과 등록 대상은 변경하지 않았다.
- 설치 폴더 `C:\Users\ITP\AppData\Local\JSMap\EdgeAutoCollector\extension`의 `background.js`·`manifest.json`을 백업 후 갱신하고 검수본과 해시 일치를 확인했다. 백업은 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-27\collector-code-before-1.1.3-181000`이다. 브라우저 프로필·쿠키·확장 저장소는 읽거나 변경하지 않았고 Edge를 강제 종료하지 않았다. 적용하려면 확장 화면에서 `다시 로드` 후 **1.1.3**을 확인한다.
- 기능 커밋 **`1c1bccf`**를 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`627bde15-3c82-454c-b0a5-4cb1ebdf181b`**로 배포했다. 세 운영 도메인은 모두 HTTP 200이며 기능 커밋과 원격 브랜치가 일치한다. 확장 프로그램 변경은 설치 폴더까지 반영했지만 현재 열린 Edge 작업자에는 사용자가 확장 화면에서 `다시 로드`해야 적용된다.

## 2026-08-27 당근 월세/매매 오판·반복 재시도 수정

- 사용자 재시작 후 네이버 서구 **8,506개(16:46)**·동구 **1,701개(16:48)** 완료가 사용자 화면과 D1 읽기전용 검사로 확인됐다. 앞 작업의 네이버 재개 확인은 완료됐지만 당근은 별도 결함이었다. 당근 유성구 세션 `DAANGN-1787816914788-608b03fb`는 33개, 서구 `DAANGN-1787817219731-bda3cf55`는 45개를 처리하고 부분수집으로 반복 재시도했다. 멈춰 있는 화면만의 문제가 아니었다.
- 당근 `1.5.0`의 거래 판독이 전체 JSON에서 `sale` 문자열을 찾아 **분류 항목명 `salesTypes`까지 매매로 오인**했다. 실제 작업에 `propertyFilter.tradeTypes=["MONTH"]`인데 `tradeType=sale`가 저장된 것과 함수 재현 결과를 확인했다. 월세 목록에서 매매 조건을 선택해 적은 수만 처리되고 100개 미만 완전수집 보호장치에 걸렸다. 유성구에는 정확한 지번 없는 원본 2개도 있었다.
- 당근 수집기 **1.5.1**은 JSON의 `tradeTypes` 값 `MONTH/BUY`만 판독하고 혼합·미지원·손상 필터를 임의로 월세/매매로 바꾸지 않는다. 등록된 대상의 URL/분류 필터를 현재 페이지 URL로 덮어쓰지 않고, 자동 실행 중 지도 클릭·늦은 네트워크 응답·재열기가 선택을 바꾸지 못하게 했다. 명시된 임대/매매 대상과 URL이 다르면 안내 후 차단하며, 매매 확장 이전 거래값 없는 예약은 기존 월세 대상으로 유지한다.
- 서버는 시작·이어가기·매 묶음 저장 전에 URL·거래유형·저장 체크포인트를 재검증한다. 구버전의 잘못된 작업은 더 저장하지 못하며 새 수집기는 임대/매매 분리 작업 ID를 사용한다. 한 광고에 매매·월세 조건이 함께 있는 경우 상세 월세값이 매매 우선 조건 때문에 목록의 과거 월세로 되돌아가지 않도록 같은 거래 조건을 기준으로 판독한다.
- 자동수집 확장 **1.1.2**: 실제 목록 끝 도달, 발견/목록/처리 건수 일치, 주소 외 실패 없음이 증명된 경우만 `100건 미만`·주소 누락을 확인사항이 있는 부분완료로 표시하고 다음 구로 진행한다. 페이지 잘림·미처리·통신/저장 실패는 계속 복구 대상이며, 거래 설정 충돌은 무의미하게 재시도하지 않고 사유를 남긴다. **100건 미만/불완전 수집에서 기존 광고 미노출을 계산하지 않는 서버 보호 규칙은 그대로다.** 부분완료는 완전수집으로 둔갑시키지 않는다.
- 전체 테스트 **405/405**, 새 당근 회귀검사 27개와 실행기 추가 검사 2개, 정적 자산 132개 빌드·Wrangler dry-run·diff 검사를 통과했다. 실제 필터 형태, 혼합 거래의 금액, 기존 임대 예약, 매매 아파트 제외, 지연 선택 응답, 구버전 작업 저장 전 차단, 작은 목록의 다음 구 전환, 설정 오류 반복 차단을 검증했다. 이는 모의/회귀검사이며 수정 후 당근 5개 구의 실수집 완료는 확장 재로드 이후 별도 확인이 필요하다.
- D1 스키마·기존 매물/원본·연락처·사진·메모·찜·확인/계약 상태를 직접 수정하거나 삭제하지 않았다. 오전 11시 예약과 대상 등록도 변경하지 않았다. 잘못된 실행으로 수집된 매매 원본을 이번 수정에서 임의 삭제/월세 변환하지 않는다. 코드 되돌리기 기준은 `dc707c8`이며 기능 커밋 revert 후 재배포한다. 설치 파일 갱신과 배포 결과는 아래에 기록한다.
- 기능 커밋 **`c246f10`**을 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`04a08c96-fdad-4cc9-b92e-0952c3a4c9e8`**로 배포했다. 세 운영 도메인 HTTP 200·당근 자산 `1.5.1` 및 검수본과 SHA-256 일치를 확인했다. 조회 캐시/운영 데이터 삭제는 없다. 설치 폴더 `C:\Users\ITP\AppData\Local\JSMap\EdgeAutoCollector\extension`의 프로그램 13개를 백업 후 갱신하고 모두 해시 일치를 확인했다. 백업은 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-27\collector-code-before-1.1.2-173325`이다. 브라우저 프로필·쿠키·설정 저장소에 접근하지 않았고 Edge를 종료/강제 재시작하지 않았다.
- **사용자 적용 단계:** Edge 확장 화면에서 `JS 부동산 자동수집 → 다시 로드` 후 **1.1.2**, 당근 수집창 **1.5.1**을 확인한다. 기존 미완료 실행 복구를 먼저 확인하고 중복 전체 실행은 누르지 않는다. 재로드 후 당근 임대 `MONTH/lease` 작업의 발견 수·저장 수·부분완료 사유·5개 구 완료를 실수집/D1로 확인하는 것이 남아 있다. 코드 배포·설치 파일 갱신 완료를 실행 중인 브라우저 코드 적용·당근 수집 완료와 혼동하지 않는다.

## 2026-08-27 네이버 서구 자동수집 정체 복구 보완

- 사용자 화면의 자동수집 `1.0.38`은 네이버 유성구·대덕구·중구 3개 완료 후 **11:33:44 서구 실행 중(4/10)**에서 멈춰 있고 동구·당근 5개 구는 대기 상태였다. 네이버 화면은 `5.9.3`, 목록 8,378개 확인 100%이나 처리 0개/수동 저장 버튼 상태였다. 운영 D1 읽기전용 확인에서도 앞의 3개 구 완료는 확인됐지만 해당 서구 저장 세션 시작은 확인되지 않았다. Windows 예약 실행 성공(0)은 저장 완료를 뜻하지 않는다. 화면/코드로 정체 경로를 조사했으며 실행 중인 Edge 내부 상태를 직접 확보하지 못했으므로 이번 실정체의 단일 원인을 확정했다고 표현하지 않는다.
- 네이버 수집기 **6.0.2**: 기존 목록 요청 60초 제한이 응답 헤더 수신 직후 해제되던 결함을 수정해 **응답 본문 읽기·JSON 해석까지 제한을 유지**한다. 자동 목록 탐색/저장 중 뒤늦게 도착한 제공처 지도 응답이 선택·준비 상태와 진행창을 덮어쓰지 못하도록 보호하며 종료/실패 시 보호 상태를 해제한다. 수동 클러스터 선택과 기존 목록 확인→중복 비교→저장 방식은 유지한다.
- 자동수집 확장 **1.1.1**: 시작/재시도/구 전환/구버전의 알 수 없는 단계가 3분 이상 연결되지 않으면 현재 미완료 구부터 복구한다. 구버전 시간 정보가 비어 있으면 구별 실행표의 시작 시간을 사용한다. 알람뿐 아니라 현황창·진행 신호·지금 전체 실행에서도 정체를 점검하고, 제어 오류를 최근 기록에 남긴다. 완료 응답과 이전 구의 진행 신호가 동시에 상태를 덮어쓰지 않도록 상태 변경을 직렬화했고 수집 시작 메시지에도 40초 응답 제한을 추가했다. 지역 실행이 90분에 도달한 실패는 즉시 같은 구를 반복하지 않고 미완료 재시도 대기열로 보내 뒤의 지역을 진행한다.
- 현황창에는 단계 연결 상태·진행 갱신 경과를 표시하고 실제 실행기 코드 버전이 설치 파일과 다르면 전용 브라우저 재시작 안내를 표시한다. 완료된 구·저장된 설정·체크포인트를 지우는 초기화는 하지 않는다. **오전 11시 예약, 등록된 수집 대상, 매물/사진/연락처/메모/찜/확인 상태, 매매 분류, D1 스키마와 운영 데이터는 변경하지 않았다.**
- 신규 실행 모의검사 18개, 기존 포함 집중검사 **27/27**, 전체 **376/376**, 정적 자산 132개 빌드·Wrangler dry-run·diff 검사를 통과했다. 검사에는 서구 장기 정체 후 동구 진행, 완료 3개 구 보존, 구버전 단계 복구, 정상 수집/예약 대기 유지, 중복 완료·지연 진행신호 경합, 시작 메시지 무응답, 본문 지연, 자동 목록 확인 후 저장 진입이 포함된다. 이는 재현/회귀검사이며 실제 서구·당근 수집 완료를 확인했다는 뜻은 아니다.
- 사무실 PC `C:\Users\ITP\AppData\Local\JSMap\EdgeAutoCollector\extension`의 **프로그램 파일 13개만** 검수본으로 갱신하고 해시 일치를 확인했다. 이전 프로그램 백업은 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-27\collector-code-before-1.1.1-162614`이다. 브라우저 프로필·로그인·확장 저장소는 읽거나 변경하지 않았고 브라우저 강제 종료/자동 실행도 하지 않았다. **현재 열린 구버전 Edge에는 재시작이 필요하며 실제 새 실행기 로딩/수집 재개는 아직 미확인**이다. 사용자에게 전용 수집 Edge만 닫았다가 기존 바로가기로 다시 열어 달라고 안내했다. 다음 확인은 현황창 `1.1.1`, 네이버 `6.0.2`, 미완료 구의 진행·새 저장 세션 및 최종 당근 완료 여부다.
- 코드 복원 기준은 **`5c8de32`**이다. 이번 기능 커밋 revert 후 재배포하고 필요 시 위 프로그램 백업을 복원한다. 운영 DB 쓰기가 없어 데이터 복원은 필요 없다.
- 기능 커밋 **`53dcf57`**을 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`cf80c41e-81eb-4694-9ce3-7faebe7907b7`**로 배포했다. `js-map.com`, `www.js-map.com`, Workers 도메인 모두 HTTP 200이며 세 도메인의 네이버 수집기 **6.0.2** 응답 해시가 로컬 검수본과 일치했다. DB/R2 데이터 정리나 캐시 삭제는 하지 않았다. **웹 배포 및 설치 파일 갱신 완료와 현재 Edge 실행기 재시작/실수집 재개 확인은 구분**하며 후자는 사용자 재시작 이후 확인이 남아 있다.

## 2026-08-27 매매 카드 수익률·대지/연면적 표시

- 사용자 요청에 따라 **매매 카드의 기존 평당 배지를 수익률로 교체**했다. 임대 카드의 평당 임대료 표시는 그대로 유지한다. 계산은 `월 임대수입 × 12 ÷ (매매가 − 기존 임대보증금 합계) × 100`이며, 카드 툴팁과 상세에는 보증금 차감 기준 단순 연 수익률이고 대출이자·취득비용·세금·공실·운영비 미반영임을 명시한다. 실제 순수익률이나 대출 반영 수익률로 표현하지 않는다.
- 매매가·보증금·월수입 중 필요한 값이 없거나 분모가 0 이하이면 `수익률 확인 필요`로 표시한다. 명시적인 보증금 0·월수입 0과 미제공값을 구분한다. 현재 확인된 수집값을 사용한 예시는 대원아르떼 **5.67%**, 베스트 **9.25%**이다. 서로 다른 출처·매매가·매매유형의 보증금/수입을 섞지 않으며, 같은 조건의 복수 원본이 서로 다른 수익자료를 가진 경우 임의 선택하지 않는다.
- 매매 카드와 원본 조건줄은 모호한 `평123.9` 대신 **대지 62평 · 연 123.9평**으로 표시한다. 토지는 토지면적만, 전용면적이 제공된 개별 공간은 전용도 표시한다. 미제공 대지·연면적은 `미확인`이며 기존 범용 `area`를 근거 없이 대지나 연면적으로 바꾸지 않는다. 상세의 ㎡/평 병기와 나머지 매매 정보는 유지한다.
- D1 통합목록 응답에 허용된 5개 숫자와 매매 범위만 담는 `saleSummary`를 추가했다. 기존 목록 쿼리에서 이미 읽은 원본 스냅샷을 활용하므로 카드마다 상세 API를 추가 호출하지 않는다. `saleDetails`와 구분해 목록 요약 때문에 상세 로딩이 생략되지 않도록 했다. D1 스키마·매물 데이터·연락처·사용자 상태·수집기·자동수집 예약은 변경하지 않았다.
- 전체 테스트 **358/358**, 새 집중검사 **7/7**, 자산 빌드·Wrangler dry-run·diff 검사를 통과했다. 로컬 `/sale-cards`에서 운영 카드 렌더러와 CSS를 사용해 600/450/360px 각각 4종 매매 카드의 수익률·면적 잘림과 작업 버튼 겹침을 검사했다. 좁은 폭에서는 매매 카드만 높이와 버튼 줄바꿈을 허용하며 임대 레이아웃은 유지한다. 이 화면 검사는 예시 데이터 기반이며 운영 로그인 후 실제 데이터 화면은 별도로 확인해야 한다.
- 자산 버전은 거래 UI/CSS `1.2.0-sale-yield`, 공통/원본 JS에는 `sale-metrics-v1=1` 캐시 갱신 인자를 추가했다. 코드 복원 기준은 **`f9874b2`**이며 기능 커밋을 revert 후 재배포하면 된다. 운영 DB를 수정하지 않아 데이터 되돌리기는 필요 없다.
- 기능 커밋 **`e9643f9`**를 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`2ba02718-95e5-4599-b69e-cb6c1446a759`**로 배포했다. 세 운영 도메인 HTTP 200·새 버전 인자와 변경된 HTML/JS/CSS 5개가 로컬 검수본과 일치함을 확인했다. 기존 통합목록 R2 조회 캐시 `api-cache/unified-listings-v5-source-aware-review.json` **1개만** 제거해 새 `saleSummary`가 재생성되도록 했고 원본사진·사용자 데이터는 삭제하지 않았다. 실제 두 원본의 매매가·보증금·월수입·면적도 운영 D1 읽기 전용으로 재확인했다(쓰기 0). 검수 서버는 종료했다. 사용자는 웹 새로고침 후 건물매매에서 수익률 배지·대지/연면적·상세 계산 안내를 확인하면 되며 재수집은 필요 없다.

## 2026-08-27 매매 제공처 필드 보완·공실박스 시험수집 복구

- 실제 공실박스 시험수집 세션 `GONGSIL-1787803990762-ilkfs9e6`은 25개 중 신규 2개·기존 동일 1개·필수정보 제외 22개였다. 목록의 실제 매매가 필드 `Me`를 읽지 못해 건물매매에 딸린 `Bo/Mm`를 임대조건으로 오인했다. 제외 22개는 원본이 D1에 저장되지 않아 개별 사유를 전부 확인한 것은 아니다. `100%`는 처리 종료이지 전부 정상 등록이라는 뜻이 아니며 제외/실패가 있으면 완료 제목에도 확인 안내를 표시하도록 보완했다.
- 공실박스 수집기 `2.2.1`은 `Me`를 우선 판독하고 건물통·토지·아파트·빌라·오피스텔·주택 등 제공처 분류를 보존한다. 서버에서도 원본 `Me`를 재확인해 구버전 수집기가 잘못 전달한 월세를 그대로 저장하지 않는다. 매매가가 없으면 임대보증금을 대신 매매가로 사용하지 않고 제외/오류로 남긴다. 순수 전세를 월세로 바꾸지 않는다.
- 매매 상세 `saleDetails`를 원본 스냅샷에 추가해 대지·연면적·건축·전용면적, 건물 전체/개별 공간/토지 범위, 기존 보증금 합계·월 임대수입, 확인된 지목·용도지역·도로접면·용도·승인일 등을 각각 보존한다. 공실박스 목록 `LandArea/YunArea`는 평이고 네이버/당근 구조화 면적은 ㎡이므로 단위를 분리했다. 미제공 면적·관리비는 0으로 지어내지 않는다. 토지의 용도가 창고시설이어도 토지매매로 남고, 토지에는 층이 필요 없으며 건물통의 총층수를 개별 호실로 취급하지 않는다. 실제 사진 없는 공실박스는 계속 사진 없음이다.
- 네이버 수집기 `6.0.1`은 `priceInfo.dealPrice`를 매매가로 사용하고 입주 임대보증금 `warrantyPrice`와 분리한다. 매매 상세 원본 응답을 보존하며 단독/다가구/빌라/상가주택/사무실 등을 별도 분류한다. 당근 북마클릿은 `1.5.0` 그대로이고 **실제 수집을 수행하는 서버**의 매매 유형·가격·면적·요청 거래유형 일치 검사를 수정했다. 매매 스냅샷 변경은 임대용 느슨한 동일조건 비교로 생략하지 않는다. 임대/매매 및 서로 다른 매매 유형의 자동통합은 차단한다.
- 수집 범위는 최신 사용자 화면 기준이다. 공실박스는 일반 아파트를 포함한 모든 매매, 당근은 일반 아파트를 제외한 9개 유형, 네이버는 일반 아파트를 제외하되 사진에서 선택된 재건축·아파트분양권·오피스텔분양권 등은 별도 유형으로 허용한다. 네이버/당근의 일반 아파트는 서버 저장에서도 제외한다. 제공처가 알 수 없는 유형을 보내면 임의의 주택/상가로 단정하지 않고 기타로 보존한다. 기존 오전 11시 예약·등록된 임대 구역·자동수집 대상은 변경하지 않았다.
- 공실박스 `678545`(월평동 893 대원아르떼)는 **19억7천/건물전체/연면적123.9평**, `2613327`(어은동111-4 베스트)는 **19억5천/건물전체/연면적124.3평**으로 기존 대표/원본 ID를 유지해 복구했다. 대표·원본 각각 2건의 거래/면적 필드와 이력 2행만 변경했다. 최종 D1 대표는 **임대11,331 / 매매2 / 합계11,333**이며 연락처·사진·고객매칭·관련 cloud_state·사용자 메모·상태는 전후 비교로 보존을 확인했다. D1 스키마 변경·매물 삭제는 없다.
- 복구 도구 `tools/repair-gongsil-sale-sample-2026-08-27.mjs`는 기본 읽기전용이며 `--apply`를 명시해야 운영 쓰기를 한다. 정확한 ID·이전 전체 행·단독 원본·버전을 검사하고 중간 변경 시 중단한다. 같은 SQL의 복구/되돌리기/경합 차단을 독립 SQLite에서 검증했다. 운영 백업·복구 SQL·되돌리기 SQL·전후 JSON은 개인 폴더 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-27\gongsil-sale-2026-08-27T05-09-40-894Z`에 있으며 개인정보 포함 파일은 Git에 넣지 않았다. 이후 새 편집/수집이 발생하면 되돌리기 SQL도 안전검사에서 중단하므로 무조건 재실행하지 말고 비교해야 한다.
- 전체 테스트 **351/351**, Cloudflare 전용 **190/190**, 자산 빌드·Wrangler dry-run·diff 검사를 통과했다. 테스트는 실제 공실박스 `Me/Bo/Mm` 모양, 제공처별 아파트 허용, 토지/창고 분리, 단위·통건물·누락값·가격 오인 방지·매매 스냅샷 재수집·HTML escaping을 포함한다. 로컬 브라우저 `/sale` 검수 화면에서 실제 렌더러로 4종 매매 예시를 표시해 항목 표시 및 360px 카드 넘침 0건을 확인했다. 자산은 `listing-trade-v1.css`/`listing-trade-ui-v1.js` `1.1.0-sale-details`, `unified-listings-v8.js` `8.1.37-sale-details`이다.
- **다음 실수집 검증:** 공실박스 페이지를 새로고침하고 수집기를 다시 열어 `2.2.1` 확인 → 기존 시험 25건을 다시 수집 → 신규/기존/제외 사유와 건물매매·토지매매 노출 대조. 이번에는 전체수집을 임의 실행하지 않았다. 네이버/당근 매매도 사용자 지정 소량 구역에서 실제 응답과 대조해야 한다. 네이버 공개 상세 진단 호출은 429 응답으로 중단했으므로 새 매매 유형의 모든 실서비스 응답 형식을 이미 검증했다고 주장하지 않는다. 화면 예시 및 회귀검사 통과와 실제 전 유형 수집 완료는 구분한다.
- 기능 커밋 `9eead94`를 `origin/codex/cloudflare-js-map`에 푸시하고 운영 Worker **`7d6345f2-760d-42a3-8836-bbde54ddc516`**로 배포했다. 세 운영 도메인 HTTP 200 및 새 HTML 자산 버전을 확인했고 공실박스·네이버·거래 UI·상세 JS·거래 CSS의 실제 운영 응답을 로컬 검수본과 비교해 일치했다. 대표 CSV·통합목록·운영현황·리비전 2개·복구 대상 상세 2개, 총 7개 R2 조회 캐시만 제거해 D1에서 재생성하도록 했다(원본사진·개인 데이터 삭제 없음). 로컬 시각검사 서버와 탭도 종료했다. 코드 되돌리기는 기준 커밋 `623410f` 대비 기능 커밋을 검토해 revert 후 재배포하며, 데이터는 위 별도 guarded rollback을 사용한다.

## 2026-08-27 거래시장 선택기·검색창 간격 보완

- 사용자 피드백에 따라 선택기와 검색 입력창 사이에 8px 여백을 추가하고 입력창 왼쪽 모서리를 10px로 둥글게 했다. 입력창 너비에서 같은 8px를 빼므로 다른 상단 메뉴 위치는 그대로이며 입력창과 검색 버튼은 계속 붙어 있다. CSS 버전은 `1.0.3-selector-spacing`이다.
- 브라우저 검수에서 1920·1366·1200·1024·900·820·769px 모두 선택기 간격 8px, 검색 버튼 간격 0px, 버튼 겹침·잘림 0건을 확인했다. 전체 테스트 `339/339`, 자산 빌드, Wrangler dry-run 및 `git diff --check`를 통과했다. 기능·수집기·운영 데이터에는 변경이 없다.
- 커밋 `fea613f`를 푸시하고 운영 Worker `7570c4e1-19c5-4fad-b5e1-a1089a7f0934`로 배포했다. 세 운영 도메인 HTTP 200 및 새 CSS 버전, 운영 CSS와 로컬 검수본 일치를 확인했다. 검수 서버·브라우저를 종료하고 화면 크기 재정의도 해제했다.

## 2026-08-27 거래시장 선택기 상단 배치 수정

- 새 거래시장 선택기가 검색·필터·초기화의 고정 그리드 바깥에 추가돼 지도 위 두 번째 줄로 밀리고, 정의되지 않은 `sr-only` 클래스 때문에 `매물 시장`과 중복 `상가임대` 문구가 보이던 문제를 수정했다. 선택기를 기존 검색행 내부의 첫 칸으로 옮기고 접근성 이름은 `aria-label`로 유지하며 중복 문구는 제거했다.
- 선택기·검색어·검색 버튼을 명시적 3열로 고정했다. PC와 가로 태블릿(900px 이상)은 기존 한 줄 헤더를 유지하고 769~899px의 좁은 태블릿에서는 검색·필터행과 운영 메뉴행을 두 줄로 정돈한다. 선택기 폭은 화면에 따라 88~112px이고 모바일 전용 헤더의 기존 표시 정책은 변경하지 않는다. CSS 버전을 `1.0.2-inline-header`로 갱신했다.
- 실제 운영 HTML과 로컬 CSS를 같은 순서로 읽는 `tools/preview-listing-header.mjs` 시각검사 서버를 추가했다. 로컬 127.0.0.1에만 열리며 인증·운영 API·실매물 데이터에 접근하지 않고 배포 자산에도 포함하지 않는다. 로그인 화면까지만 확인하고 헤더를 검증하지 못했던 이전 검사의 빈틈을 보완한다.
- 브라우저에서 1920·1540·1366·1200·1024·1000·900·899·820·769px의 선택기·검색·필터·초기화·운영 메뉴 전체 경계를 검사해 겹침 및 화면 밖 잘림 0건을 확인했다. 세 거래시장 선택값과 문서 모드 변경도 확인했고 390px에서는 기존 데스크톱 헤더가 숨겨짐을 확인했다. 전체 테스트 `339/339`, Cloudflare 전용 `178/178`, 자산 빌드, Wrangler dry-run과 `git diff --check`를 통과했다.
- 이번 수정은 헤더 HTML/CSS와 테스트·검수 도구만 변경한다. 거래시장 분리·매물/원본 데이터·수집기·자동수집 예약은 건드리지 않으며 실제 매매 소량수집은 사용자 화면 확인과 지역 지정 후 진행한다.
- 수정 커밋 `e9af0ae`를 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `cf422c02-3285-4658-9264-cafeabaf74e4`로 배포했다. 세 운영 도메인 HTTP 200, 검색행 내부 선택기·새 CSS 버전·중복 문구 제거를 실제 응답에서 확인했고 운영 CSS는 로컬 검수본과 일치한다. 로컬 검수 서버·브라우저는 종료하고 화면 크기 재정의도 해제했다.

## 2026-08-27 상가임대·건물매매·토지매매 분리 구현

- 메인 상단 검색창 왼쪽에 작은 거래시장 선택기를 추가했다. `상가임대 / 건물매매 / 토지매매` 중 하나만 선택하며 첫 접속과 전체 초기화는 항상 `상가임대`로 돌아간다. 거래시장을 바꾸면 이전 시장의 구분·보증금·월세·권리금 조건을 제거해 임대 필터가 매매 목록을 가리거나 반대로 섞이는 현상을 막는다.
- 목록·지도·클러스터·검색·필터는 선택한 거래시장만 사용한다. 매매에서는 보증금 입력란을 매매가 최소·최대로 바꾸고 월세·권리금·중개보수 필터를 숨기며, 건물매매에는 상가·다가구·주택·공장/창고 등 건물 성격을, 토지매매에는 토지만 표시한다. 카드와 상세 원본에도 매매가가 보이고 임대와 매매는 동일 주소·층이어도 하나의 대표매물로 합쳐지지 않는다.
- 기존 마이그레이션 `0019_listing_trade_foundation.sql`의 `trade_type`, `sale_category`, `sale_price`를 웹 조회·CSV·변경분·상세·대표정보 갱신·전체합치기·분리에 연결했다. 기존 운영행은 전부 `lease` 기본값이므로 현재 임대 매물·찜·계약완료·확인매물·고객매칭은 그대로 유지된다.
- 공실박스 `2.2.0`, 네이버 `6.0.0`, 당근 `1.5.0` 수집기는 제공처의 거래유형과 매매가를 읽어 임대·매매를 분리 저장한다. 네이버와 당근 자동수집 대상 키·체크포인트도 거래유형을 포함하므로 현재 등록된 오전 11시 임대 대상은 그대로 임대로 실행되고, 사용자가 나중에 지정할 매매 지역은 별도 대상으로 등록한다. 공실박스 한 목록에 임대와 매매가 섞여도 각 원본은 올바른 거래유형으로 저장하되 혼합 결과로 다른 시장의 기존 원본을 미노출 처리하지 않는 안전장치를 넣었다.
- 매매 자동수집 운영 대상은 아직 등록하거나 실행하지 않았다. 다음 실사용 순서는 **운영 웹에서 세 거래시장 선택기 확인 → 사용자가 네이버·당근 매매 최초 클러스터/지역 지정 → 적은 구역을 소량 수집 → 웹의 건물매매·토지매매 분류와 원본 수 검수 → 자동수집기를 수동 1회 실행**이다. 소량 검수가 끝나기 전에는 매매 전체수집이나 기존 오전 11시 예약 대상 변경을 하지 않는다.
- Cloudflare 전용 테스트 `177/177`, 전체 테스트 `338/338`, 변경 JavaScript 문법 검사, 정적 자산 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 로컬 비로그인 화면은 인증창까지만 열리므로 거래시장 UI의 실제 운영 화면 검증은 배포 후 로그인된 사용자가 확인하는 위 첫 단계에서 진행한다.
- 기능 커밋 `92c6b9e`를 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `9c183d82-6b8c-4c54-99c1-db580fbf4310`으로 배포했다. `js-map.com`, `www.js-map.com`, Workers 도메인은 모두 HTTP 200이고 운영 자산에서 거래시장 세 항목·기본 임대 모드·공실박스 `2.2.0`·네이버 `6.0.0`·당근 `1.5.0`과 혼합거래 안전장치를 확인했다. 운영 D1은 읽기 전용으로 확인했으며 대표매물 `11,331건`이 모두 기존 `lease`, 매매 `0건`, 쓰기 `0건`이다. 따라서 배포만으로 기존 데이터를 바꾸지 않았고 사용자가 매매 대상 지역을 지정한 뒤 첫 소량 수집부터 매매 데이터가 생긴다.

## 2026-08-26 거래확인 후보 카드·지도 강조

- 메인 매물목록에 보이는 `거래확인 후보`를 작은 배지만으로 구분하지 않는다. 모든 연결 원본이 광고 미노출인 후보 카드는 옅은 주황색 배경, 굵은 주황색 왼쪽 띠, 주황 테두리와 그림자를 사용하고 사진 오른쪽 아래에 `광고 미노출 / 확인 필요` 리본을 표시한다. 마지막 정상 사진이 없는 경우에도 같은 리본과 카드 강조가 유지된다.
- 기존의 비활성 `미노출` 버튼은 주황색 `계약완료` 버튼으로 바꿨다. 이 버튼을 누르면 메인지도에서 위치만 찾는 흐름이 아니라 `운영현황`을 열고 해당 거래확인 후보의 인라인 매물카드를 정확히 펼친 뒤 화면 안으로 스크롤한다. 후보가 그 사이 해제됐으면 안전하게 안내만 표시하며, 버튼 문구와 관계없이 사용자가 운영현황의 최종 확인창을 승인하기 전에는 계약완료 상태를 자동 변경하지 않는다.
- 사용자 피드백에 따라 거래확인 후보가 포함된 지도 클러스터의 진한 주황색 외곽선은 제거했다. 동·구 행정구역과 주소·공간 단위 근거리 클러스터 모두 기존 기본색, 공실 임장확인색, 완료매물색과 파란 선택 강조만 사용한다. 거래확인 후보의 강한 시각 구분은 메인 매물카드에만 적용하므로 지도 가독성과 기존 클러스터 디자인을 유지한다.
- 거래확인 여부는 이미 D1 통합원본 응답에 있는 `sourceUnavailable`만 사용하므로 D1 데이터·스키마·수집기에는 변경이 없다. 기존 `운영현황 → 거래확인 후보`의 마지막 정상 사진 보존, 인라인 확인 및 사용자가 직접 누르는 `거래완료 처리` 경계도 그대로 유지한다.
- 거래확인 후보 집중검사, JavaScript 문법 검사, 전체 테스트 `335/335`, Cloudflare 전용 테스트 `174/174`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 최초 강조 기능은 커밋 `f4ba472`였고 사용자 피드백 반영 커밋 `76cee25`, CSS 강제갱신 커밋 `b5172cd`를 원격 `codex/cloudflare-js-map`에 푸시한 뒤 최종 운영 Worker `9411a3d3-57d9-429d-b335-f40566321622`로 배포했다. 세 운영 도메인은 모두 HTTP 200이며 실제 운영 응답에서 `계약완료` 버튼, 새 JS/CSS 버전과 지도·CSS의 거래확인 후보 링 코드 제거를 확인했다. D1 데이터·스키마와 R2 데이터 캐시는 변경하지 않았다.

## 2026-08-26 거래확인 후보 창내 확인·마지막 사진 보존

- `운영현황 → 거래확인 후보`의 `매물 확인`은 더 이상 운영현황을 닫거나 메인지도로 이동하지 않는다. 현재 후보 행 아래에 마지막 정상 사진, 주소·층/호실, 보증금·월세·관리비·권리금·평수, 원본 출처·개수, 미노출 횟수, 메모를 담은 매물카드를 펼치며 같은 버튼으로 다시 접을 수 있다.
- 펼친 카드의 `거래완료 처리`는 기존 D1 `toggleDone` 저장 경계를 그대로 사용한다. 확인창을 한 번 더 거친 뒤 계약완료 상태와 날짜 표식을 저장하고 성공한 후보만 현재 목록에서 제거하며 운영현황 건수와 메인 목록을 다시 읽는다. 운영 검증에서는 실제 사용자의 후보를 임의로 거래완료 처리하지 않는다.
- 모든 연결 원본이 완전수집에서 3회 이상 미노출된 매물도 마지막으로 수집된 **실제 매물 사진**을 유지한다. 업소 프로필·`/avatars/` 이미지는 제외하고 카드에 `현재 광고 미노출 · 마지막 정상 사진`을 명시하므로 광고가 살아 있는 매물로 오해하지 않는다. 이 읽기용 보완은 원본이나 대표매물을 재활성화하지 않으며, 실제 광고 재등장은 기존 `sourceReappeared` 경계에서만 자동복구된다.
- 메인 통합목록에도 해당 매물을 읽기용 보존 원본으로 포함해 사진과 상세를 열 수 있게 했고, 원본 링크 버튼은 `미노출`로 비활성화한다. 현재 운영 거래확인 후보는 1,007건이므로 후보 API 상한을 500건에서 2,000건으로 올려 운영현황에서 전부 확인할 수 있게 했다.
- 전체 테스트 `335/335`, Cloudflare 전용 테스트 `174/174`, 후보·고아 상세 집중검사 `4/4`, JavaScript 문법 검사, 운영 D1 읽기 전용 후보 집계, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 운영 D1 데이터·스키마는 변경하지 않았다.
- 기능 커밋 `5f605c0`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `f24a4efb-a146-4573-9016-334e7baadd01`로 배포했다. 통합목록·운영현황·목록/운영 리비전 R2 캐시 4개를 제거했고 `js-map.com`, `www.js-map.com`, Workers 도메인과 새 JS/CSS 자산은 모두 HTTP 200이며 배포 버전 문자열과 새 카드 선택자가 운영 응답에 포함됨을 확인했다. 로그인되지 않은 독립 브라우저에서는 인증 화면과 정적 자산까지만 확인했으며 실제 사용자의 거래완료 버튼은 데이터 보호를 위해 누르지 않았다.

## 2026-08-26 재노출 광고의 계약완료 매물 자동복구

- 원본 광고가 3회 연속 완전수집에서 보이지 않으면 원본만 `active=0`으로 바뀌고 아직 계약완료되지 않은 대표매물은 `거래확인 후보`에 표시된다. 자동으로 계약완료시키는 기능은 없으며 계약완료는 계속 사용자가 직접 판단한다.
- 사용자가 계약완료로 둔 매물의 기존 광고가 계속 노출되는 동안에는 반복 수집만으로 상태를 풀지 않는다. 반면 3회 미노출로 비활성화된 원본이 실제로 다시 관찰되거나 계약완료 대표에 새로운 원본 광고가 연결되면 대표 상태를 `active`로 자동복구하고 메모 끝의 `[거래완료 YYYY-MM-DD]` 표식만 제거한다. 모든 복구는 `sourceReappeared` 이력으로 남고 고객매칭도 다시 계산한다.
- 운영 이력을 대조해 계약완료 이후 실제 미노출 원본이 재노출된 것이 확정된 5건(`서구 탄방동 616 4층`, `중구 대흥동 451-3 1층`, `서구 갈마동 377-40 1층`, `서구 용문동 282-21 1층`, `서구 탄방동 58-1 1층`)만 복구했다. 단순히 활성 원본을 가진 계약완료 매물은 건드리지 않았다.
- 운영 적용 전 D1 전체를 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-26\js-map-primary-pre-source-reappeared-reactivation.sql`(619,802,713바이트)로 내보냈다. 복구 SQL `tools/reactivate-reappeared-completed-listings-2026-08-26.sql`은 대표·원본 ID, 재관찰 시각, 이전 `toggleDone` 이력을 모두 확인하는 조건부·재실행 안전 방식이며 운영 적용 후 5건 모두 활성, 복구 이력 각 1건, 기존 메모 보존과 거래완료 표식 제거를 확인했다.
- 목록·통합원본·운영현황·리비전 및 5건의 상세 R2 캐시를 제거해 복구 데이터가 즉시 재생성되게 했다. 재노출 경계 집중검사 `4/4`, 전체 테스트 `334/334`, Cloudflare 전용 테스트 `173/173`, 복구 SQL 로컬 D1 10문장 실행, JavaScript 문법 검사, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다.
- 운영 데이터 복구 북마크는 `00000361-0000014e-000050d3-8a48db793e5bfe03eb1ac3a12eefcde5`이다. 기능 커밋 `c4748bc`를 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `7778fa5d-982b-4e54-bfe0-cc66fc01469d`로 배포했다. `js-map.com`, `www.js-map.com`, Workers 도메인과 인증 설정 API는 모두 HTTP 200이다.

## 2026-08-26 과거 네이버 동명주소의 전체 정확지번 복구

- 과거 네이버 수집기가 `중구 용두동`처럼 동까지만 저장한 원본을 한 정확지번 대표매물에 몰아 붙인 문제를 대전 전 구·전체 활성 원본으로 확대 조사했다. 정확지번 대표에 연결된 동명주소 네이버 원본은 2,509건이었고, 이 중 원본 자체 좌표가 남은 2,430건은 대표 좌표가 아니라 **각 원본 좌표**를 카카오 역지오코딩해 구·동 일치까지 검증했다. 실제 대표와 같은 지번은 1,652건, 다른 지번은 778건이었으며 이미 정확지번이 저장됐지만 다른 대표에 붙은 9건도 별도로 포함해 총 787건을 복구 대상으로 확정했다.
- 운영 D1 전체를 복구 전에 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-26\js-map-primary-pre-full-address-recovery.sql`(618,280,934바이트)로 내보냈다. 복구 도구 `tools/recover-legacy-naver-addresses.mjs`는 좌표가 없거나 구·동이 불일치하면 절대 추측하지 않고 보류하며, 같은 정확지번·거래유형·보증금·월세·층/호실과 허용 면적차까지 모두 맞는 기존 대표가 하나일 때만 이동한다. 그 외에는 원본 조건의 엄격한 서명별로 별도 대표를 만든다. 실행 근거와 대상은 `tools/repair-legacy-naver-addresses-manifest.json`, 적용 SQL은 `tools/repair-legacy-naver-addresses-generated.sql`에 보존했다.
- 운영 적용 결과 다른 지번 원본 382건은 조건이 강하게 일치하는 기존 대표로 이동했고, 405건은 375개의 새 대표매물로 분리됐다. 원본·사진·연락처를 함께 옮기고 양쪽 `listing_history`에 분리·연결 이력 각각 787건을 남겼다. 원본이 비어 삭제된 과거 대표는 15개이며 고객매칭·고객활동·찜을 보호한 뒤 삭제했다. 실제 찜 1건은 `동구 중동 77-12`의 잘못된 대표에서 복구된 `동구 중동 77-24` 새 대표로 이전돼 과거 ID 참조 0건, 새 ID 참조 1건이다.
- 정확지번 원본 하나만 가진 채 대표주소가 `유성구 전민동`으로 남은 1건도 후속 SQL `tools/repair-legacy-naver-addresses-followup.sql`로 `유성구 전민동 337-9`로 완성했다. 최종 운영 검증에서 정확지번 원본-대표 주소 불일치 0건, 정확지번 원본이 동명 대표에 연결된 건 0건, 이동 원본의 사진·연락처 소속 불일치 각 0건, 복구 임시테이블 0건, 새 활성 대표 375개를 확인했다. 활성 원본이 비활성 대표를 가리키는 48건은 모두 사용자가 보존한 `계약완료` 매물 또는 기존 고아 2건으로 이번 복구가 만든 데이터 손상이 아니다.
- 좌표가 과거 원본에도 전혀 남지 않은 네이버 동명주소 79건은 안전하게 지번을 확정할 근거가 없으므로 기존 대표에 그대로 보류했다. 이들은 다음 정상 재수집에서 정확지번과 원본 좌표가 들어오면 이미 배포된 주소 경계 로직이 자동으로 분리·재판정한다. 삭제하거나 임의 지번을 부여하지 않았다.
- 운영 적용 북마크는 본 복구 `00000361-0000007a-000050d3-3e6608ce24bdc4ba854c0692ebd52bad`, 후속 주소완성 `00000361-00000098-000050d3-86163a92fce5c3ae505cb9bfcf8a5a66`이다. 목록·운영현황·리비전 R2 캐시 5개와 실제 존재하던 영향 대표 상세 캐시 38개를 제거해 웹이 복구된 D1을 즉시 읽도록 했다.
- 복구 SQL은 신규 임시 로컬 D1에 전체 마이그레이션 19개를 적용한 뒤 25문장·2,439행으로 실행 검증했다. 전체 테스트 `334/334`, Cloudflare 전용 테스트 `173/173`, 복구 도구 JavaScript 문법 검사, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다.
- 복구 근거·도구 커밋 `629425c`를 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `f623917c-e0d4-4c24-8caf-4ff40411820e`로 배포했다. `js-map.com`, `www.js-map.com`, Workers 도메인과 인증 설정 API는 모두 HTTP 200이며, 운영 DB 최종 교차검사에서도 새 대표 375개·분리/연결 이력 각 787건·주소 불일치 0건·찜 이전 정상 상태가 유지된다.

## 2026-08-26 번지 미확인 원본의 잘못된 대표매물 연결 복구

- 운영 `중구 용두동 33-9` 대표 `M-b421c174-81f1-4359-855e-f8a5628a9f84`의 활성 원본 23개를 전수 대조했다. 정확한 33-9 원본은 4개였고, 과거 네이버 수집 당시 `중구 용두동`까지만 저장된 원본 19개가 같은 대표에 연결돼 있었다. 좌표와 현재 정확지번 매물을 교차 확인해 이들 중 다수가 113-10·29-11·23-9·39·19-4·34-33·111-30·119-7·33-15·145-3·29-10 등 다른 번지임을 확인했다.
- 수집 서버는 앞으로 동명만 있거나 `용두동 1층`처럼 층수가 번지로 오인될 수 있는 주소를 저장·통합하지 않고 `정확한 지번주소 없음`으로 재수집 대상으로 남긴다. 이미 연결된 원본에서 새 수집으로 정확한 번지가 확인됐는데 현재 대표 번지와 다르면 기존 대표에서 원본·사진·연락처를 먼저 안전 분리한 뒤 새 번지의 기존 후보를 다시 비교해 통합·별도등록·매물검증 중 하나로 처리한다.
- 운영 복구용 SQL `tools/repair-yongdu-33-9-address-mix-2026-08-26.sql`은 원본을 삭제하지 않는다. 같은 33-9로 확인된 2개는 주소만 보정하고, 다른 번지가 확인된 15개는 정확한 번지의 기존 대표 7개로 이동하거나 별도 대표 8개로 복구하며, 번지를 확정할 수 없는 2개는 사진·연락처를 보존한 채 매물검증으로 되돌린다. 모든 변경은 원래 대표 ID와 원본 ID를 함께 조건으로 사용하고 양쪽 대표 이력에 근거를 남긴다.
- 적용 전 운영 D1 전체를 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-26\js-map-primary-pre-yongdu-address-repair.sql`(616,228,260바이트)로 내보냈다. SQL은 빈 최신 스키마에서 전체 마이그레이션과 함께 문법 검증했고, 주소 경계 집중검사 16개, 전체 테스트 `334/334`, Cloudflare 전용 테스트 `173/173`, JavaScript 문법 검사, 정적 자산 130개 빌드, Wrangler dry-run과 `git diff --check`를 통과했다.
- 운영 복구 적용 후 33-9 대표의 활성 원본은 `23개 → 6개`가 됐고 6개 모두 주소 `중구 용두동 33-9`, 조건 `4,000/130`으로 일치한다. 과거 동명만 저장된 19개는 33-9 유지·보정 2개, 정확지번 기존 대표 이동 7개, 정확지번 별도 대표 생성 8개, 지번 재확인 검증대기 2개로 전부 보존해 합계가 일치한다. 새 대표 8개와 이동 원본의 사진 연결 불일치 0건, 임시 생성 대표 잔존 0건도 운영 D1에서 확인했다.
- 주소를 확정할 수 없는 2개는 검증 버전 7의 `review`로 고정해 자동분류기가 다시 동명 대표에 붙이지 않게 했고 원본·사진은 삭제하지 않았다. `api-cache/d1-sheet.csv`, 통합목록, 목록 리비전과 영향받은 상세 캐시 15개를 포함한 R2 캐시 18개를 제거해 웹이 복구된 D1을 즉시 다시 읽도록 했다.
- 기능 커밋 `f992fe8`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `4077075f-71b9-4337-bc9b-2cd50b7af1d3`로 배포했다. 운영 데이터 복구 이후 전체 테스트 `334/334`, Cloudflare 전용 `173/173`, 로컬 최신 D1의 복구 SQL 15문장 실행을 다시 통과했다.

## 2026-08-24 자동수집 지역 장기점유 방지

- 오전 11시 자동수집은 네이버 유성구·대덕구·중구를 11시 15분까지 정상 완료했지만, 서구가 상세저장 세션을 시작하기 전 목록 확인 단계에서 약 6시간 40분을 사용했다. 운영 D1에서 서구 세션은 17시 55분 시작 후 18시 21분 정상 완료했고 이어 동구와 당근 수집으로 넘어간 것을 확인했다. 따라서 7시간 내내 저장이 멈춘 것은 아니지만 한 지역이 전체 실행을 과도하게 점유한 실제 결함이었다.
- 네이버 목록 요청 하나가 60초 동안 응답하지 않으면 `AbortController`로 중단하고 재시도한다. 제공처 요청이 무기한 대기해 다음 페이지·지역으로 못 넘어가는 경로를 차단하며, 기존 페이지·지역 체크포인트와 D1 중복방지는 그대로 사용한다. 네이버 수집기 버전은 `5.9.3`이다.
- 자동수집 감시기는 `123초 경과`처럼 시간만 변하는 문구를 실제 진행으로 계산하지 않는다. 페이지·처리건수·진행률이 20분간 변하지 않을 때 기존 자동복구가 실제로 작동하며, 화면 신호 단절 8분·로딩 3분·시작확인 2분 기준은 유지한다.
- 한 지역이 진행 신호를 내더라도 총 90분을 넘으면 그 지역을 미완료 재시도 대기열로 보내고 다음 지역을 먼저 처리한다. 같은 지역을 즉시 90분씩 반복하지 않도록 즉시재시도 대상에서도 제외했으며, 미완료 지역은 전체 대상 처리 후 저장 지점부터 다시 시도한다. 확장 프로그램 버전은 `1.0.38`이다.
- 자동수집 전용 테스트 `8/8`, 전체 테스트 `333/333`, Cloudflare 전용 테스트 `173/173`, 변경 JavaScript 문법 검사, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 매물·원본·검증·찜·확인 상태·고객 데이터와 D1 스키마는 변경하지 않았다.
- 기능 커밋 `cb53e23`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `e82fa239-8144-4c79-a399-868bf5903c79`로 배포했다. 세 운영 도메인은 HTTP 200이고 운영 네이버 수집기 응답은 `5.9.3`이다. 전용 Edge 확장 프로그램 `1.0.38`도 같은 해시로 설치했으며 Windows 예약은 매일 11시, 다음 실행은 2026-08-25 11시, 직전 결과는 성공(0)이다.
- 설치를 위해 전용 Edge만 재시작한 뒤 진행 중 당근 수집이 체크포인트에서 정상 이어지는지 운영 D1로 확인했다. 재시작 전 서구, 재시작 직후 대덕구·중구가 `completed`로 끝났고 동구 세션이 새로 시작해 갱신 중이므로 일반 브라우저와 운영 데이터 손실 없이 새 감시기가 적용됐다.

## 2026-08-24 상가임대·매매 분리 1단계 저장 기반

- 미구현 계획의 첫 단계로 대표매물 `listings`, 원본매물 `listing_sources`, 검증·수집대기 `collector_raw`에 공통 거래유형(`lease/sale`), 매매 세부 구분, 매매가 저장 칸을 추가하는 D1 마이그레이션 `0019_listing_trade_foundation.sql`을 마련했다. 기존 모든 행은 명시적으로 `lease` 기본값을 사용하며 매매가는 `NULL`이므로 현재 임대 목록·금액·클러스터·검증 결과는 바뀌지 않는다.
- 공용 거래유형 모듈은 월세·임대와 매매를 내부 표준값으로 변환하고 상가·다가구·주택·건물·토지·공장/창고 매매 구분을 정규화한다. 알 수 없는 거래유형은 임대로 추측하지 않으며, 임대와 매매가 같은 대표매물 경계를 공유할 수 없다는 기준을 회귀검사로 고정했다.
- 이번 단계는 저장 기반만 준비한다. `상가임대 / 매매` 화면 전환, 매매 카드·클러스터·필터, 공실박스 매매 실제 수집은 아직 활성화하지 않았고 기존 네이버·당근·공실박스 월세 수집 경로도 변경하지 않았다.
- 신규 집중검사 4개, 전체 테스트 `333/333`, Cloudflare 전용 테스트 `173/173`, JavaScript 문법 검사, 신규 임시 로컬 D1에 19개 전체 마이그레이션 적용, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다.
- 운영 적용 전 D1 전체를 `C:\Users\ITP\Documents\JS부동산 백업\2026-08-24\js-map-primary-pre-trade-foundation.sql`(573,738,074바이트)로 내보내 복구본을 보존했다. 적용 전 대표 10,697건·원본 39,657건·수집/검증 14,635건을 기록하고 운영 D1에 마이그레이션 `0019`를 적용했다. 적용 후 세 건수가 모두 동일하고 세 테이블의 모든 기존 행이 `lease`, `sale` 및 매매가 보유 행은 0건이며 잘못 분류된 행도 0건임을 확인했다.
- 기능 커밋 `197f328`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `0fefbb4a-336e-413d-abf7-ce76a4393e0d`로 배포했다. 운영 D1에는 미적용 마이그레이션이 없고 `js-map.com`, `www.js-map.com`, Workers 도메인은 모두 HTTP 200이다. 다음 단계는 기능 플래그가 있는 `상가임대 / 매매` 목록·검색·클러스터 격리이며, 사용자의 추가 화면 구상을 확인한 뒤 진행한다.

## 미구현 계획: 상가임대·매매 분리 확장

> 이 항목은 2026-08-21 사용자와 상의한 향후 구현안이다. 2026-08-24에 1단계 저장 기반만 운영 D1에 적용했으며 화면·수집기·운영 매물 데이터는 아직 임대 전용으로 유지한다. 이후 단계는 사용자가 `상가임대·매매 분리 작업을 이어서 시작해줘`라고 요청할 때 진행한다.

### 목표 화면과 사용자 흐름

- 최상위 거래 모드는 `상가임대 / 매매` 두 가지로 나눈다. 최초 접속과 `초기화`의 기본값은 항상 기존 `상가임대`이며, 기존 임대 지도·클러스터·목록·검색·고객매칭 동작을 그대로 보존한다.
- `매매` 모드에서는 상가·다가구·주택·건물·토지·공장/창고 매매를 하나의 지도와 하나의 클러스터 체계에 함께 표시한다. 임대 클러스터와 매매 클러스터는 한 화면에서 섞지 않는다.
- 매매 모드의 `구분`은 `전체 매매`, `상가매매`, `다가구매매`, `주택매매`, `건물매매`, `토지매매`, `공장/창고매매`를 기본안으로 한다. `전체 매매`는 모든 매매 매물을 같은 클러스터에 합산하고, 특정 구분을 고르면 지도·클러스터·목록·검색 결과를 해당 구분으로만 다시 계산한다.
- 매매 카드에는 세부 구분 배지를 표시한다. 매매 전용 주요 정보는 매매가를 중심으로 하며 필요하면 평당가·대지면적·연면적·현재 보증금 총액·월 임대수익·수익률·공실·직접사용 가능 여부를 카드 또는 상세창에 단계적으로 추가한다.

### 수집과 저장 설계

- 공실박스 수집은 한 번의 전체 수집에서 임대와 매매 원본을 함께 가져온 뒤 원본의 거래조건을 판별해 분리 저장한다. 수집기를 두 번 돌리거나 DB를 완전히 둘로 나누는 방식은 사용하지 않는다.
- 월세는 `상가임대`, 매매는 `매매`로 저장하고 매매는 원본의 매물 성격에 따라 위 세부 구분을 함께 저장한다. 전세·단기임대 등 아직 지원하지 않는 거래유형과 거래유형이 불명확한 원본은 임의 변환하지 말고 제외 또는 전용 검토 대상으로 보낸다.
- 기존 원본·대표매물 구조를 확장하는 방향을 우선 검토한다. 최소 데이터 항목은 거래유형(`lease/sale`), 매매 세부 구분, 매매가이며, 매매가를 기존 보증금이나 월세 필드에 넣지 않는다. 기존 모든 대표·원본은 마이그레이션 시 명시적으로 `상가임대` 기본값을 받아 현재 결과가 달라지지 않게 한다.
- 같은 원본 매물번호가 다시 수집되면 같은 거래유형 안에서만 갱신한다. 원본의 거래유형이 임대에서 매매 또는 반대로 바뀐 경우에는 기존 대표를 즉시 덮어쓰거나 이동하지 말고 별도 변경 검증을 거친다.
- 공실박스의 전화번호·Tell 검색·사진·메모·주소·좌표·로드뷰·대장·원본 링크·원본 매물번호는 기존 로직을 재사용한다. 찜·임장확인·계약완료·고객매칭·사용자 메모 등 사람이 만든 상태는 재수집·분리·통합 과정에서도 보존한다.
- 네이버와 당근의 현재 수집기는 월세 전용 안전규칙을 그대로 유지한다. 매매 수집을 추가할 때는 기존 월세 수집 조건을 느슨하게 풀지 말고 출처별 매매 수집 경로와 판별 경계를 별도로 추가한다.

### 동일매물·검증 안전규칙

- 임대 원본과 매매 원본은 주소·층·호실·면적이 같아도 서로 동일매물로 자동 통합하지 않는다. 대표매물·검증대기·중복판정의 첫 번째 경계에서 거래유형이 다르면 반드시 분리한다.
- 매매 중복판정은 매매가·주소·층·호실·면적·세부 구분을 사용하고, 기존 임대의 보증금·월세 비교 규칙과 분리한다. 거래유형 또는 매매 세부 구분이 불명확하면 자동합치기보다 사람 확인을 우선한다.
- 임대와 매매 사이에서 대표정보·사진·연락처·찜·확인 상태가 잘못 이전되지 않는 회귀검사를 마련한다. 기존 임대 대표값과 당근 우선·사진 우선 정책은 매매 추가만으로 바뀌지 않아야 한다.

### 권장 구현 순서와 복구 원칙

1. **완료(2026-08-24)** D1 백업·스키마 확장안·기존 임대 기본값 마이그레이션을 먼저 만들고 로컬 D1에서 기존 임대 건수와 대표값이 전후 동일한지 확인한다.
2. 기능 플래그를 둔 `상가임대 / 매매` 전환과 거래유형별 목록·검색·클러스터 격리를 구현한다. 플래그를 끄면 기존 상가임대 화면만 즉시 사용하도록 복구 가능하게 한다.
3. 매매 빠른등록, 매매 카드·상세창·구분 필터를 구현하고 임대용 입력·표시와 값이 섞이지 않는지 검사한다.
4. 공실박스 한 번 수집에서 임대·매매를 분리 저장하되 소량 시험수집과 읽기 전용 비교를 먼저 수행한다. 신규·갱신·거래유형 변경·재수집 상태 보존을 검증한 뒤 전체 수집으로 확대한다.
5. 필요할 때 네이버 매매, 당근 매매 수집을 출처별로 추가한다. 기존 월세 전용 수집의 수집량·제외 규칙·대표값 회귀검사를 반드시 함께 실행한다.
6. 마지막으로 매매 전용 고객매칭과 수익률 등 부가 필터를 추가한다. 기존 임대 고객조건을 매매가나 매매 수익률에 임의 대응시키지 않는다.
- 각 단계는 별도 커밋으로 남기고 전체 임대 회귀검사, 거래유형별 수집·저장·중복판정 검사, 지도·클러스터·찜·메모·Tell·확인상태 검사를 통과한 뒤에만 다음 단계와 운영 배포로 진행한다.
- 실제 구현을 시작할 때는 사용자의 추가 구상을 먼저 반영하고, 이 계획을 고정 사양으로 간주하지 않는다. 사용자가 요청한 시작 문구는 `CODEX_HANDOFF.md와 최신 GitHub 변경사항을 확인하고 상가임대·매매 분리 작업을 이어서 시작해줘`이다.

## 2026-08-21 최신 GitHub 통합본 인수·회귀검사

- `git fetch --prune origin`과 `git pull --ff-only`로 원격 `codex/cloudflare-js-map`의 `52db499` 이후 최신 5개 커밋을 안전하게 fast-forward해 HEAD `1d9b5ca`로 맞췄다. 반영 내용은 최신 통합본 검증 기록과 PC 상단 로그아웃 버튼을 빠른등록과 같은 크기로 확대한 CSS·캐시 버전·배포 기록이다. 사용자 개인 강의 도구 미추적 파일은 건드리지 않았다.
- 인수인계 상단의 완료 기록과 과거 `다음 작업` 항목을 대조했다. 과거 데이터 접근 전환 후보는 후속 기록에서 모두 완료됐고 현재 별도로 명시된 미완료 기능이 없어, 이번 이어가기 범위는 최신 통합본의 전체 회귀검사와 운영 배포 일치 확인으로 정했다.
- 로그아웃·인증 집중검사 `9/9`, 전체 테스트 `329/329`, Cloudflare 전용 테스트 `169/169`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 운영 `js-map.com`, `www.js-map.com`, Workers 도메인은 모두 HTTP 200이고 운영 HTML이 `header-professional-v1.css?v=1.1.2-logout-size`를 연결하며 CSS에 확대된 로그아웃 규칙이 포함됨을 확인했다.
- 최신 기능은 이미 Worker `ba39cd59-d759-4f03-a2f7-1621be9d49c4`로 운영 배포·확인까지 완료된 상태라 코드·D1·R2를 불필요하게 다시 변경하거나 재배포하지 않았다. 이번에는 최신 인수와 회귀검사 결과만 HANDOFF에 기록해 커밋·푸시한다.

## 2026-08-20 상단 로그아웃 버튼 크기 확대

- PC 상단의 로그아웃 버튼을 빠른등록과 같은 크기로 확대했다. 900~1199px에서는 두 버튼이 각각 `84×42px`, 1200~1539px에서는 `98×42px`, 1540px 이상에서는 `108×42px`로 동일하며, 1200px 이상에서는 로그아웃 글자와 아이콘도 빠른등록과 같은 크기·굵기로 맞췄다. 로그아웃의 중립 회색과 위험 동작을 알리는 붉은 hover 색은 유지해 빠른등록과 기능 위계가 혼동되지 않는다.
- 넓어진 로그아웃 버튼만큼 900~1539px 검색 영역의 가변 폭을 정확히 줄여 전체 헤더가 화면 밖으로 밀리거나 기존 운영현황·고객매칭·Tell·AI임장·빠른등록과 겹치지 않게 했다. 769~899px 아이콘 전용 규칙과 768px 이하 모바일 앱 상단 도구 숨김 규칙은 유지했다.
- 헤더 집중검사와 로그인 집중검사 9개, 전체 테스트 `329/329`, Cloudflare 전용 테스트 `169/169`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. dry-run 첫 실행에서 OneDrive의 일시적인 `print-v2.css` 파일 잠금이 한 번 발생했으나 같은 생성물을 재검사해 정상 통과했다. 매물·고객·찜·로그인 세션·D1 스키마·운영 데이터는 변경하지 않았고 CSS 자산 버전만 `header-professional-v1.css?v=1.1.2-logout-size`로 갱신했다.
- 기능 커밋 `af741bc`, 글자 굵기 선택자 보완 `528b795`, 브라우저 캐시 갱신 `a42296f`를 원격 `codex/cloudflare-js-map`에 푸시하고 최종 운영 Worker `ba39cd59-d759-4f03-a2f7-1621be9d49c4`로 배포했다. 운영 자산과 메인은 HTTP 200이며, 로그인된 실제 화면 측정에서 1280px는 빠른등록·로그아웃 모두 `98×42px`·13px/900, 1600px는 모두 `108×42px`·14px/900으로 일치했다. 두 화면 모두 문서 폭이 뷰포트 폭과 같아 가로 넘침이 없다.

## 2026-08-20 집 노트북 최신 통합본 인수·전수 회귀검사

- `codex/cloudflare-js-map`에서 `git fetch --prune origin`과 `git pull --ff-only`를 실행해 사무실 작업 33개 커밋·77개 파일을 `079091d`에서 `52db499`까지 안전하게 fast-forward로 받았다. 동기화 전후 로컬 작업트리는 깨끗했고 사용자 변경을 덮어쓰거나 유실하지 않았다.
- 최신 인수인계의 상단 완료 기록과 과거 `다음 작업` 항목을 함께 점검했다. 이전에 남아 있던 데이터 접근 전환 항목은 후속 커밋에서 모두 완료됐고, 현재 별도로 미완료 표시된 기능 작업은 없어 이번 작업 범위를 최신 통합본 전체의 회귀검사와 배포 가능 상태 확인으로 정했다.
- 전체 테스트 `329/329`와 Cloudflare 전용 테스트 `169/169`를 통과했다. 최신 변경 JavaScript 11개 파일의 `node --check`, 정적 자산 130개 빌드, Wrangler 4.124.0 배포 dry-run, `git diff --check`도 모두 정상이다.
- 인증·90일 세션·친구 계정 연결·수집기·검증매물 재판정·방문확인 보존·즐겨찾기 격리·목록 및 사진 성능 경로를 포함한 최신 통합본에서 회귀 오류를 발견하지 않았다. `js-map.com`과 `www.js-map.com`도 실제 GET 요청에서 모두 HTTP 200이다. 따라서 기능 코드·D1 스키마·운영 데이터는 추가로 변경하지 않았으며, 이미 배포된 최신 운영본을 불필요하게 재배포하지 않고 이번 인수·검증 기록만 커밋·푸시한다.

## 2026-08-20 같은 층·상이한 임대조건 검증매물 전수 재판정

- `서구 둔산동 1072`의 네이버 신규원본 `2644275014`(`8층`, 보 1,000/월 65, 18.2평)이 기존 당근 대표 `M-62b66f56-e2d5-4750-87d6-4f6168a2c292`(`8층`, 보 3,000/월 160, 49.9평)과 같은 층이라는 이유로 매물검증에 남은 사례를 운영 D1에서 확인했다. 두 조건은 최신 공간 판정 함수상 이미 `같은 층이지만 보증금·월세가 모두 크게 다름`에 해당해 별도 대표매물로 생성돼야 한다.
- 원인은 자동분리 기준을 보완한 뒤 과거 검증행의 `autoDecisionVersion`을 올리지 않아, 운영 검증대기 2,426건 전부가 버전 6으로 고정된 채 최신 기준 재검사에서 제외된 것이었다. 검증 판정 버전을 7로 올려 과거 검증행을 전수 재판정 대상으로 만들었다. 새로 수집되는 원본은 수집 시점에 같은 기준을 즉시 적용하며, 동일 호실·동일 조건은 기존처럼 합치고 작은 임대료 조정이나 공간 식별이 애매한 경우는 계속 사람 확인으로 남긴다.
- 둔산동 실제 수치 회귀검사를 추가하고 JavaScript 문법 검사, 집중검사 16개, 전체 테스트 `329/329`, Cloudflare 전용 테스트 `169/169`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 대표정보·확인매물·찜·고객·메모·계약완료 상태와 D1 스키마는 변경하지 않았다.
- 운영 검증대기 2,426건을 버전 7로 전수 재판정해 명백한 다른매물 239건을 별도 대표로 생성하고 정확한 동일매물 14건은 기존 대표정보를 유지한 채 원본만 연결했다. 나머지 2,173건은 작은 임대조건 변경·면적 차이만 있거나 정확한 공간 식별이 부족해 사람 확인으로 보존했다. 둔산동 네이버 원본은 새 대표 `M-d326d021-1df2-485a-9668-0385a2767c23`(`8층`, 보 1,000/월 65, 18.2평)으로 생성됐다. 재판정 도중 일시 실패 후 직접확인으로 정상 복귀한 행에 과거 `error_text`가 남는 표시 결함도 수정해, 성공한 직접확인 판정은 이전 오류문구를 지운다.
- 기능 커밋 `7a914fe`, 오류표시 보완 커밋 `9aeef43`을 원격 `codex/cloudflare-js-map`에 푸시하고 최종 운영 Worker `724a3faf-5e4a-407f-8caf-247191d20fed`로 배포했다. 운영 D1 재조회에서 버전 6 이하 검증행 0건, 오류가 남은 검증행 0건, 버전 7 결과가 다른매물 239건·동일매물 14건·직접확인 2,173건으로 정확히 합계 2,426건임을 확인했다. 로그인된 운영 목록 강제 새로고침 API에서 둔산동 새 대표 ID와 주소가 실제 CSV에 포함됨을 확인했고 세 운영 도메인은 모두 HTTP 200이다.

## 2026-08-20 상단 오른쪽 로그아웃 버튼

- PC·가로 태블릿 상단 메뉴의 `빠른등록` 오른쪽 끝에 전용 `로그아웃` 버튼을 추가했다. 기존 전역 인증 종료 함수 `jsSecureLogout`을 그대로 사용하므로 Google 로그인과 발급 아이디 로그인 모두 같은 방식으로 서버 세션 쿠키와 기기 초기 매물 캐시를 정리한 뒤 로그인 화면으로 돌아간다.
- 버튼을 누르면 즉시 비활성화하고 `종료 중`으로 바꿔 중복 요청을 막는다. 900px 이상 상단 한 줄 구조는 버튼 너비만큼 검색 영역을 안전하게 줄여 기존 운영현황·고객매칭·Tell·AI임장·빠른등록과 겹치지 않게 했고, 769~899px에서는 아이콘 전용 버튼으로 표시한다. 모바일 앱의 기존 상단 도구 숨김 규칙은 유지한다.
- 인증·헤더 집중검사 12개, JavaScript 문법 검사, 전체 테스트 `328/328`, Cloudflare 전용 테스트 `169/169`, 정적 자산 130개 빌드, Wrangler dry-run과 `git diff --check`를 통과했다. D1 스키마·로그인 방식·90일 세션 기간·매물·찜·고객·수집 데이터는 변경하지 않았다. 자산 버전은 `auth-gate-v1.js?v=1.3.1-header-logout`, `header-professional-v1.css?v=1.1.0-header-logout`이다.
- 기능 커밋 `d3c6c43`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `33f6c91d-381d-45d5-bdc0-d5f56480731e`로 배포했다. 운영 1280px 화면에서 버튼은 빠른등록 오른쪽 6px 간격의 최우측에 72×42px로 표시되고 60px 상단 헤더 안에 들어감을 확인했다. 실제 발급 아이디 세션으로 로그인 HTTP 200, 로그아웃 HTTP 200, 로그아웃 직후 세션 재확인 HTTP 401을 확인해 서버 세션이 종료됨을 검증했다. `js-map.com`, `www.js-map.com`, Workers 도메인과 두 버전 자산은 모두 HTTP 200이며 운영 DOM에도 `로그아웃` 버튼과 `jsSecureLogout` 연결이 반영됐다.

## 2026-08-20 당근 매매·월세 복수조건의 월세 우선 수집

- 중구 태평동 254-4 당근 원본 `4239920`은 제공처 원본에 `매매 1억5,000`이 대표조건이고 `월세 2,000/50`이 보조조건으로 함께 들어 있었다. 당근 월세 필터는 월세 조건이 하나라도 있는 게시글을 반환하지만 기존 수집기는 `preferred=true`인 매매조건을 먼저 선택해 JS 매물 `M-aee1021b-2dd3-4968-9b9f-3fa3c5c19dd5`을 `보 15,000/월 0`으로 잘못 생성했다. 운영 D1 전수검사에서 같은 구조의 활성 당근 원본은 12건이고 모두 실제 MONTH 조건도 함께 보유함을 확인했다.
- 당근 정규화는 여러 거래조건 중 MONTH를 제공처 대표조건보다 먼저 선택한다. 목록 단계와 D1 저장 경계에서 월세조건 여부를 이중 검사해 월세조건이 전혀 없는 매매·전세 원본은 목록·대표매물·매물검증 어디에도 새로 저장하지 않는다. 이미 정규화된 레코드도 원본 `trades`로 다시 검증해 이전 수집기나 재개 작업이 매매 금액을 보증금으로 저장하지 못하게 했다.
- D1 마이그레이션 `0018_daangn_monthly_trade_priority.sql`은 기존 활성 12개 원본 스냅샷을 월세조건으로 정리하고, 그중 대표매물이 실제로 `월 0`인 9건만 월세 보증금·월세로 수정한다. 이미 다른 월세 대표조건이 표시되는 3건은 대표정보를 건드리지 않으며 변경 전후를 `repairDaangnMonthlyTrade` 이력으로 남긴다. 검증대기의 매매 우선 복수조건 6건은 월세 조건으로 보정해 재판정하고, 월세 조건이 전혀 없는 순수 매매 2건은 원본 감사행만 보존한 채 검증목록에서 제외한다. 집중검사 `33/33`, 전체 테스트 `328/328`, Cloudflare 전용 테스트 `169/169`, 로컬 전체 마이그레이션 18개, 정적 자산 130개 빌드, Wrangler dry-run과 `git diff --check`를 통과했다.
- 기능 커밋 `5d075a5`를 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `975b98a0-9dfb-4776-bec8-00f26b609fbd`로 배포한 뒤 운영 D1에 `0018`을 적용했다. 대표 9건과 기존 원본 스냅샷 12건이 보정됐고, 검증대기 6건은 월세 조건으로 재판정되어 자동생성 1건·자동통합 1건·중복대기 연결 2건·사람 확인 유지 2건으로 정리됐다. 순수 매매 2건은 `excludeNonMonthlyTrade` 처리돼 검증목록과 웹 매물에서 제외됐다.
- 목록·통합목록·운영현황 및 영향받은 대표 9건의 상세 캐시 총 12개를 삭제하고 목록·운영 리비전을 전체 새로고침으로 갱신했다. 운영 D1, 로그인된 목록 CSV와 통합 상세 API에서 `푸른뫼 / 중구 태평동 254-4 / 당근`이 모두 `보 2,000/월 50·월세`로 일치함을 확인했다. `js-map.com`, `www.js-map.com`, Workers 도메인은 모두 HTTP 200이고 운영 발급 아이디 로그인도 정상이다.

## 2026-08-20 친구 Google·발급 아이디 단일 사용자 연결

- 친구의 기존 Google 계정과 마스터가 발급하는 아이디·비밀번호를 하나의 내부 사용자로 연결한다. 두 로그인 방식 모두 세션의 사용자 이메일을 기존 Google 이메일로 통일하므로 지금까지 그 Google 계정으로 저장한 찜목록·임장목록·AI임장 상태·변경 담당자 식별을 별도 복사 없이 그대로 사용한다. Google 로그인과 아이디 로그인을 동시에 허용하며 어느 쪽으로 접속해도 권한과 사용 허용 상태가 같다.
- `운영현황 → 사용자 관리 → 발급 아이디`에서 사용 허용된 친구 Google 계정을 선택한 뒤 아이디·표시 이름·비밀번호를 발급한다. 한 Google 계정에는 발급 아이디 하나만 연결할 수 있고 소유자 계정만 연결 대상에서 제외한다. 연결된 Google 계정을 중지하면 Google 로그인과 아이디 로그인 모두 즉시 차단되며, Google 계정 권한을 바꾸면 아이디 로그인에도 같은 권한이 적용된다. 친구 Google 계정이 관리자라면 아이디 로그인도 같은 관리자 권한을 사용한다.
- D1 마이그레이션 `0017_link_local_google_identity.sql`은 기존 `local_accounts`에 `linked_email`과 일대일 고유 인덱스만 추가한다. 매물·원본·고객·메모·찜·임장 데이터 테이블은 수정하거나 복사하지 않는다. 마이그레이션 적용 당시 운영 발급 계정은 0건이어서 연결 누락 계정은 없었다.
- 연결 신원·비밀번호 해시·90일 세션·즉시 해제 집중검사와 JavaScript 문법 검사를 통과했고 전체 테스트 `327/327`, Cloudflare 전용 테스트 `168/168`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다.
- 사용자가 요청한 발급 아이디 `jinwong87`, 표시 이름 `웅`을 기존 친구 Google 계정 `ggjin8877@gmail.com`에 연결해 운영 D1에 발급했다. 비밀번호 평문은 저장하지 않았고 16바이트 무작위 salt와 PBKDF2-SHA256 해시만 저장했다. Cloudflare Worker에서 310,000회 PBKDF2 검증이 실행 한도를 넘어 로그인이 거절되는 문제를 실제 운영 로그인으로 발견해 100,000회로 조정했으며, 5회 실패 시 10분 잠금과 90일 보안 쿠키는 그대로 유지한다.
- 정식 운영 Worker `46f90383-3fb7-480c-b329-dbef1eff7696` 배포 후 발급 아이디 로그인 HTTP 200, 세션 재확인 HTTP 200을 확인했다. 세션은 기존 Google 이메일, 표시 이름 `웅`, 기존 관리자 권한으로 생성되며 D1의 기존 친구 계정 찜·삭제기록·임장목록·AI임장 상태 4개 범위가 같은 이메일에 그대로 남아 있음을 확인했다. 세 도메인과 인증 설정 API도 모두 HTTP 200이다. 기능 커밋 `3fdfd3a`, 관리자 친구 연결 보완 `3c12471`, Worker 비밀번호 검증 한도 보완 `9991f93`을 원격 `codex/cloudflare-js-map`에 푸시했다.

## 2026-08-20 90일 Google 로그인·마스터 발급 친구 계정

- 사이트 로그인 세션을 기존 5일에서 90일로 연장했다. 본인은 기존 승인된 Google 계정으로 로그인하며, 배포 전에 발급된 정상 5일 세션도 다음 접속 때 한 번만 90일 쿠키로 자동 승격하므로 다시 Google 인증할 필요가 없다. 세션은 IP·와이파이에 묶지 않아 태블릿이 와이파이를 바꾸거나 엘리베이터에서 일시적으로 연결이 끊겨도 로그아웃되지 않는다. 초기 세션 확인 중 네트워크가 끊기면 로그인 화면으로 잘못 전환하지 않고 `인터넷 연결을 기다리는 중입니다`를 표시하며 3초마다 자동 복구한다.
- 공개 회원가입 없이 마스터 소유자만 친구용 아이디를 발급할 수 있는 로컬 계정 방식을 Google 로그인과 함께 추가했다. `운영현황 → 사용자 관리`에는 기존 Google 승인 계정과 별도로 `발급 아이디` 영역이 표시되며, 영문·숫자 기반 아이디, 표시 이름, 10자 이상 비밀번호, 편집자/조회자 권한을 지정할 수 있다. 친구 계정은 로그인 화면의 `아이디로 로그인`을 사용한다. 이번 작업에서 운영 친구 계정을 임의로 생성하지 않았으므로 마스터가 배포 후 직접 발급해야 한다.
- 친구 비밀번호는 원문이나 복호화 가능한 값으로 저장하지 않고 사용자별 16바이트 무작위 salt와 Cloudflare Worker에서 안정적으로 검증되는 PBKDF2-SHA256 100,000회 결과만 D1 `local_accounts`에 저장한다. 동일 아이디 5회 실패 시 10분 잠금하며, 비밀번호·권한·사용 허용을 저장할 때 세션 버전을 올려 기존 기기 로그인을 즉시 해제한다. 사용 중지 계정도 다음 API 요청에서 바로 차단된다. 발급 아이디에 별도 권한을 부여하지 않고 연결된 Google 계정의 현재 권한을 그대로 사용하며 소유자 계정은 연결하지 않는다.
- Google 승인계정 방식과 기존 이메일 허용목록, 소유자 권한, 로그아웃, 모든 매물·수집·찜·고객 데이터는 변경하지 않았다. 로컬 계정은 내부 고유 사용자 주소를 가져 개인 찜·AI임장 상태와 변경 담당자 기록도 친구별로 분리된다.
- JavaScript 문법 검사, 로컬 비밀번호 해시·기존 세션 자동승격·90일 만료·로그인·즉시 세션해제·잠금 집중검사, 전체 테스트 `326/326`, Cloudflare 전용 테스트 `167/167`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. D1 마이그레이션 `0016_local_accounts.sql`은 운영 DB에 적용했으며 운영 계정은 마스터가 화면에서 직접 발급한다.
- 기능 커밋 `9dc4ba5`와 기존 Google 세션 자동승격 보완 커밋 `a4f63b8`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `5f4ade6b-bbb9-41df-b9bc-69e1cd9a107d`로 최종 배포했다. 운영 세 도메인과 인증 설정 API는 HTTP 200이며 Google·발급 아이디 로그인이 모두 활성 상태다. 기존 Google 로그인 상태로 운영 메인 화면과 매물 10,300개 표시가 정상적으로 열렸다. 운영 D1에는 미적용 마이그레이션이 없고 `local_accounts` 0건, `listings` 10,361건을 확인해 친구 계정을 임의 생성하거나 매물 데이터를 변경하지 않았음을 검증했다.

## 2026-08-20 당근 자동수집·매물검증 실데이터 확인 및 현황탭 복구

- 감시알람 수정 후 이어진 당근 5개 구 운영 세션을 D1에서 전수 대조했다. 유성구 1,969건·서구 7,594건·대덕구 613건·중구 661건·동구 646건, 총 11,483건이 모두 `completed`와 완전수집 검증 통과로 끝났다. 신규·변경 상세 판정 221건은 신규 17건·기존통합 74건·조건갱신 25건·매물검증 6건·상세중복 8건·지번주소 미공개 제외 91건으로 정확히 합계가 일치한다. 제외 91건은 전부 제공처가 지번주소를 주지 않은 원본이며 기존 JS매물을 숨기거나 잘못 합치지 않는다.
- 새 매물검증 6건 중 서구 원본 `4278458`은 수집 직후 예약 자동정리에서 앞서 적용한 `같은 층이지만 보증금·월세가 모두 크게 다름` 규칙으로 별도 대표매물 `M-c90d6e59-b7fe-4999-8237-a7d5545971cb`을 생성했다. 나머지 당근 5건은 유성구 신성동 126-1, 서구 괴정동 127-47·탄방동 590, 동구 용전동 48-11·160-15의 실제 `processing_state='review'` 행으로 운영 매물검증에 남아 있다. 즉 신규·통합·조건갱신·자동분리·사람 검증대기까지 전체 판정 경로가 실제 운영 데이터에서 정상 작동했다.
- 전용 Edge 자동수집이 끝난 뒤 빈 탭만 남고 사용자가 보던 현황창이 사라진 원인은 `autorun.html` 임시 신호탭이 성공 1.2초 뒤 닫히는 반면, 구별 진행표가 있는 `options.html`은 실행기에서 열지 않았기 때문이다. Windows 실행기는 두 번의 안전한 시작 신호를 보낸 뒤 `JS 자동수집 현황` 페이지를 항상 열어 둔다. 프로필 복원으로 같은 현황탭이 이미 있으면 새 탭을 중복 생성하지 않으며, 현황 페이지는 기존처럼 5초마다 진행 상태·신규·변경·검증·오류 건수를 갱신한다.
- PowerShell 구문 검사, 자동수집 집중 테스트 `8/8`, 전체 테스트 `320/320`, Cloudflare 전용 테스트 `161/161`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 웹 매물·검증원본·대표 연결·고객·확인 상태와 D1 스키마는 수정하지 않았다.
- 기능 커밋 `93e9b5e`를 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `f82e4d37-64b5-47f9-b5a6-22aeb66df5a0`로 배포했다. 이 PC의 설치 실행기 해시가 저장소와 일치하고 확장 버전 `1.0.37`, 예약 2026-08-21 11:00, 최근 작업 결과 0임을 확인했다. 설치 후 수집을 다시 실행하지 않고 현황 페이지만 열어 `준비됨`, `10/10개 완료`, 진행 0·대기 0·실패 0과 각 구별 실제 수집 결과가 5초 갱신으로 표시되는 것을 확인했다. 완료된 과거 당근 탭은 닫고 전용 Edge에는 현황탭 하나만 남겨 사용자에게 표시했다. 운영 세 도메인은 HTTP 200이다.

## 2026-08-20 네이버 자동수집 감시 알람 상시 보장

- 운영 D1에서 당일 네이버 자동수집 세션을 전수 확인했다. 중구·대덕구·유성구는 정상 완료됐지만 서구 세션 `S-NAVER-1787192077280-s2qgngy2`는 11:14:39에 시작해 목록 7,500건을 기존 동일 7,162건·변경 3건·상세확인 필요 335건으로 분류한 뒤 11:15:18부터 4시간 이상 갱신되지 않았다. 첫 상세 묶음이 저장되기 전이라 해당 세션의 `collector_raw`는 0건이며, 화면 지연이 아니라 실제 중단으로 판정했다.
- 확장프로그램에는 5분 주기 감시와 진행 숫자 20분 정체·상태 신호 8분 단절 자동복구 코드가 이미 있었지만, 감시 알람을 설치·Edge 시작·설정 저장 때만 만들었다. 전용 Edge 프로필이 비정상 종료됐다가 같은 백그라운드 프로세스에서 다시 실행되는 경우 시작 이벤트를 놓치면 이번 실행 전체에 감시 알람이 없을 수 있었다.
- 확장프로그램 `1.0.37`은 자동수집 신규 시작, 진행 중 실행 재개, 매분 진행 신호를 받을 때마다 감시 알람의 실제 존재를 확인하고, 없으면 즉시 다시 만든다. 설정 저장 때는 존재하는 감시 알람을 불필요하게 지웠다가 만드는 순간 공백도 제거했다. 따라서 일시적인 Edge/확장 서비스워커 재기동 뒤에도 진행 숫자가 20분 정체되거나 신호가 8분 끊기면 현재 구를 마지막 저장 지점부터 자동 재시도한다.
- 자동수집 집중 테스트에 시작·재개·진행신호 세 경로의 감시 알람 보장 회귀검사를 추가했다. 집중 테스트 `8/8`, 전체 테스트 `320/320`, Cloudflare 전용 테스트 `161/161`, 정적 자산 130개 빌드와 Wrangler 4.124.0 dry-run을 통과했다. D1 스키마·운영 매물·대표 연결·고객·확인 상태는 변경하지 않았다.
- 기능 커밋 `0d742ec`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `8f80a580-fa9b-4230-99cb-188e1647b451`로 배포했다. 이 PC의 전용 Edge 확장을 `1.0.37`로 교체하고 매일 11시 작업을 다시 등록했으며 다음 실행은 2026-08-21 11:00, 최근 작업 결과는 0이다. 일반 Edge 프로필과 사용자가 연 브라우저 창은 변경하지 않았다.
- 중단된 서구를 강제 재개한 새 세션 `S-NAVER-1787207478810-emxayzt1`은 목록 2,500건에서 8,347건까지 계속 증가한 뒤 15:38:28에 완전수집 완료됐다. 신규 14건·기존통합 449건·조건갱신 8건·검증대기 27건·상세중복 47건·실패 0건이며, 이어서 동구 1,679건도 완료되고 당근 유성구 수집으로 자동 전환되는 것을 확인했다. 저장 원본이 0건이던 과거 중단 서구 세션만 매물 미노출 판정 없이 `partial`로 종료 기록을 남겼다. 운영 세 도메인은 HTTP 200이고 설치본·저장소 확장 버전은 모두 `1.0.37`이다.

## 2026-08-20 임대조건이 크게 다른 공실박스 신규매물 자동분리

- `동구 용전동 39-26`의 공실박스 `1799255`(`102호`, 보 500/월 60, 12평)와 `2762276`(`1층`, 보 1,500/월 100, 12평)이 같은 주소의 기존 `1층` 매물과 면적이 비슷하다는 이유로 매물검증에 머물렀다. 두 검증원본을 운영 매물검증 화면에서 각각 `다른매물`로 처리해 `M-76e6f615-2ec0-473e-bcb4-1eb24a262f05`, `M-727cafc2-727b-49cf-927a-0bfc53ac58a5` 별도 대표매물로 생성했다. 검증대기에는 두 원본이 0건으로 남았고 운영 검색 `용전동 39-26`에는 기존 2건과 신규 2건, 총 4건이 표시된다.
- 같은 층에서 한쪽만 호실이 구체적인 경우 보증금 차이 200만원·25% 이상과 월세 차이 10만원·15% 이상이 동시에 충족되면 별도 매물로 자동 생성한다. 양쪽 모두 `1층`처럼 일반 층 표기인 경우에는 보증금 500만원·50% 이상과 월세 20만원·30% 이상이 동시에 충족될 때만 자동 분리한다. 따라서 이번처럼 임대조건이 모두 크게 다른 신규매물은 검증에 묶이지 않지만, 한두 조건만 바뀌거나 차이가 작은 임대조건 변경은 계속 검증대기로 보내 사람이 확인한다. 양쪽 호실이 정확히 같은 매물은 가격이 바뀌어도 기존 매물 갱신 규칙을 유지한다.
- 공실박스 수집기에서 `Ho` 값이 이미 `1층`·`지하1층`처럼 층 표기인데도 무조건 `호`를 붙여 `1층호`로 만들던 오류를 수정하고 버전을 `2.1.7`로 올렸다. 숫자 `102`는 계속 `102호`로 정규화한다. 운영의 `2762276` 대표와 원본 스냅샷도 `1층`으로 바로잡았고, 공실박스 원본의 빈 관리비·권리금은 임의의 0이 아니라 원래의 미입력 상태로 보존했다. 다른 운영 매물·대표 연결·사진·연락처·메모·확인매물 상태는 변경하지 않았다.
- 용전동 두 사례, 작은 임대료 조정의 검증 유지, `1층/지하1층/102호` 표기 회귀검사를 추가했다. 전체 테스트 `320/320`, Cloudflare 전용 테스트 `161/161`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run, JavaScript 문법 검사와 `git diff --check`를 통과했다.
- 기능 커밋 `2f5dc21`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `8ce994c2-46f3-4a75-b780-2fd6bf7e71f4`로 배포했다. Workers 도메인·`js-map.com`·공실박스 수집기 자산은 모두 HTTP 200이며 운영 자산에서 `2.1.7`과 층 표기 보존 코드를 확인했다. 로그인된 운영 웹을 새로고침해 `용전동 39-26`을 검색했을 때 총 4건이 표시되고 신규 `1층 보 1,500/월 100`과 `102호 보 500/월 60`이 서로 다른 카드로 노출됐다. 첫 신규 매물의 관리비·권리금은 `-`로, 상세 원본 호실은 `1층`으로 표시됐다.

## 2026-08-20 매물검증 통합 대상의 다른 층 후보 제외

- 매물검증 작업공간은 신규 검증원본을 주소·층호실별로 묶으면서도 왼쪽 `통합 대상`에는 같은 주소의 모든 기존 대표매물을 표시했다. 따라서 검증원본이 모두 `4층`이어도 실제 동일매물이 될 수 없는 `8층·11층·12층` 등이 함께 노출돼 사람이 불필요한 후보를 다시 판별해야 했다.
- 검증원본 묶음에서 확인되는 층을 기준으로 기존 통합 대상을 서버에서 걸러낸다. `4층` 검증원본에는 `4층`, `401호`처럼 4층으로 해석되는 매물과 층·호실 정보가 아예 없는 매물만 표시하며, 명확히 다른 층은 응답과 화면에서 제외한다. 검증원본 자체에 층 정보가 없으면 오판을 막기 위해 같은 주소의 기존 후보를 종전처럼 모두 보여준다.
- 운영 D1을 읽기 전용으로 대조해 사용자가 본 사례가 `중구 오류동 188-15`임을 확인했다. 해당 주소의 검증원본은 `4층` 3건이고 기존 대표는 `2·4·5·8·10·11·12·13층` 8건이었다. 새 기준에서는 4층 대표만 통합 대상으로 남으며 다른 층 데이터나 대표 연결은 삭제·변경하지 않는다.
- `4층·401호·층 미확인`은 유지하고 `8층·11층·1201호`는 제외하는 API 회귀검사를 추가했다. 전체 테스트 `318/318`, Cloudflare 전용 테스트 `161/161`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. D1 스키마·운영 매물·검증원본·대표 연결·확인매물 상태는 변경하지 않았다.
- 기능 커밋 `67b1627`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `fdd31b58-3888-4c76-8c25-ece7264645da`로 배포했다. 로그인된 운영 매물검증 화면에서 `오류동 188-15`를 검색해 `4층` 검증원본 3건과 통합 대상 `4층` 대표 1건만 표시되고, 같은 주소의 다른 층 대표는 제외되는 것을 확인했다. 운영 화면 콘솔 오류는 0건이었다.

## 2026-08-20 사용자 확인매물 상태 영구 보호·과거 52건 복구

- 운영 D1의 전체 매물 이력을 전수 대조했다. 사용자가 직접 `(확인매물)`로 바꾼 이력이 있는 고유 매물은 202건이며 그중 4건은 현재 삭제 상태였다. 나머지 활성 198건 중 146건은 확인 상태를 유지했지만 52건(26.3%)은 다시 `(임장가자)`로 바뀌어 있었다. 현재 원본 기준으로 당근 50건·네이버 2건이며, 43건은 수집 원본 메모와 현재 메모가 정확히 같아 대표 원본 갱신이 사용자 확인 표식을 덮어쓴 것이 주원인이었다.
- `(확인매물)`을 수집 원본 메모가 아니라 사용자가 지정한 보호 상태로 분리해 처리한다. 새 네이버·당근·공실박스 원본이 들어오거나 대표 원본과 대표 임대조건이 갱신돼도 현재 확인 표식과 사용자가 남긴 메모 본문을 그대로 보존한다. 과거 당근 대표 일괄 승격 SQL을 다시 실행해도 확인 상태를 덮어쓰지 않는다.
- 대표매물 합치기는 어느 쪽이든 확인 상태이면 최종 생존 대표가 확인 상태를 이어받는다. 원본을 기존 대표로 옮기거나 대표 전체를 합칠 때도 대상 대표에 확인 상태를 전달하며 별도 이력을 남긴다. 원본을 새 매물로 분리하면 새 대표도 확인 상태를 이어받고, 기존 대표의 확인 상태도 임의로 해제하지 않는다. 따라서 사용자가 다시 직접 미확인으로 바꾸기 전에는 새 원본 유입·합치기·분리·대표 변경으로 확인 상태가 사라지지 않는다.
- 복구 SQL은 삭제 매물이나 계약완료 상태를 되살리지 않고, 과거 사용자 확인 이력이 명확하며 현재 활성·미확인인 52건의 메모 표식만 `(확인매물)`로 복원했다. `restoreConfirmedVisitMarker` 이력 52건을 남겼고 운영 재조회 결과 과거 확인 이력이 있는데 미확인인 활성 매물 0건, 현재 확인매물 202건, 확인/미확인 표식 중복 0건이다. `괴정동 92-15`, `탄방동 80-8`, `갈마동 1387`, `대흥동 492-1` 표본도 모두 정상 복구됐다.
- 목록 CSV와 통합 목록 R2 캐시를 정확히 삭제하고 목록 데이터 리비전을 `restoreConfirmedVisitMarker` 전체 새로고침으로 갱신해 열린 웹도 새 데이터를 다시 읽게 했다. 운영 D1 스키마·삭제 상태·계약완료 상태·대표 임대조건·원본 연결은 복구 과정에서 변경하지 않았다.
- 확인상태 집중 테스트와 합치기·분리 회귀검사를 포함해 전체 테스트 `317/317`, Cloudflare 전용 테스트 `160/160`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 운영 Worker `fe077f8e-8c74-444b-817a-29f3fc66045b`로 배포했고 `https://js-map.com`은 HTTP 200을 반환한다.

## 2026-08-20 공실박스 수동수집 누락 방지·웹 즉시 동기화

- 최근 수동 공실박스 세션 `GONGSIL-1787190180564-00owk7qw`를 운영 D1에서 읽기 전용으로 추적했다. 목록 2,621건 중 변경 상세 4건·신규 상세 5건은 서버에 모두 저장됐지만, 상세 연락처 역할 확인 실패가 매물 전체 변환 실패로 처리돼 239건이 제외될 수 있었고, 완료 후 Worker 캐시 삭제가 백그라운드 작업이라 웹이 잠시 이전 R2 목록을 다시 읽을 수 있었다.
- 공실박스 수집기를 `2.1.6`으로 올렸다. 지번주소 또는 임대조건처럼 매물 생성에 반드시 필요한 정보가 없는 경우만 제외한다. 상세창 인증·상세조회·연락처 역할 확인이 실패해도 주소와 임대조건이 확인된 매물은 정상 저장하며, `preserveContacts` 계약으로 이미 저장된 임대인·세입자 번호를 빈 목록으로 지우지 않는다. 새 매물은 연락처 없이 먼저 노출하고 실패 원인을 `연락처 보류`로 따로 표시한다.
- 연락처 보류 원본은 주간 상세 갱신을 기다리지 않고 다음 공실박스 수집에서 다시 상세 확인한다. 이후 연락처 확인에 성공하면 보류 상태를 해제하고 원본 연락처를 정상 보강한다. 수집 세션에는 필수정보 제외 수, 연락처 보류 수, 원인별 경고와 상세 연결 경고를 분리 저장해 과거처럼 모든 실패가 `주소변환 제외`로 보이지 않게 했다.
- 공실박스 목록에서 제공되는 정확 좌표와 도로명주소도 상세 연결 성공 여부와 무관하게 함께 저장한다. 기존 사진 정책은 그대로 유지해 실제 `Photos/photos`만 매물 사진으로 인정하며, 공실박스 프로필·대표 대체 이미지는 다시 사용하지 않는다.
- 수집 완료 API는 목록 CSV·통합 원본·운영현황·좌표 R2 캐시 삭제와 데이터 리비전 갱신을 모두 끝낸 뒤에만 완료 응답을 반환한다. 웹의 리비전 확인 주기는 5분에서 1분으로 줄였고, 변경을 감지하면 ETag 과거 응답을 재사용하지 않고 D1 목록과 통합 원본을 강제로 다시 읽는다. 따라서 열린 웹도 새로고침 없이 최대 1분 안에 신규 매물을 반영한다.
- JavaScript 문법 검사와 공실박스·연락처·캐시 동기화 집중 검사 `40/40`, 전체 테스트 `313/313`, Cloudflare 전용 테스트 `156/156`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run 및 `git diff --check`를 통과했다. 지도·클러스터·대표 우선순위·찜목록·고객매칭·메모·거래완료 데이터와 D1 스키마는 변경하지 않았다. 지도 자산은 `map.js?v=8.2.19-collector-fresh-sync`이다.
- 기능 커밋 `b91a294`를 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `14305dab-a1c8-4e1c-acf0-f45e738e3861`로 배포했다. Workers 도메인·`js-map.com`·`www.js-map.com`은 모두 HTTP 200과 새 지도 자산을 반환했고, 운영 수집기 응답에서 `2.1.6`·연락처 보존·보류 집계, 지도 응답에서 1분 확인·리비전 변경 후 D1 강제 조회 코드를 확인했다. 로그인된 운영 화면은 카드·지도·필터·주요 메뉴가 정상 로딩됐으며 콘솔 오류는 0건이었다. 위치 권한을 허용하지 않은 브라우저의 기존 `GeolocationPositionError` 경고 1건만 남았다.

## 2026-08-20 찜목록 동일 주소·층 별도 매물 오표시 분리

- `서구 괴정동 423-4 5층`의 서로 다른 대표매물 2개가 이름·주소·층·구분으로 만든 과거 `item.key`를 공유했다. 사용자가 첫 번째 카드만 찜했어도 찜목록 지도 필터가 저장된 `property:<대표매물ID>`와 함께 공통 `item.key`까지 확장해 두 번째 카드도 찜한 것처럼 표시하던 원인을 확인했다.
- 매물카드의 찜 버튼은 대표매물 ID가 있으면 항상 `property:<대표매물ID>`를 전달하고, ID가 없는 구형 데이터에서만 과거 키를 사용한다. 찜목록 `지도 보기`도 저장된 고유 property 참조만 필터에 넘기며 공통 주소 키를 다시 추가하지 않는다. 따라서 같은 주소·같은 층·같은 종류여도 실제로 찜한 대표매물 한 건만 목록과 지도에 표시된다.
- 기존 찜 폴더에 이미 저장된 property 참조와 구형 참조의 해석·정규화, 폴더 추가·삭제·동기화, 찜하지 않은 매물 데이터와 대표/원본 연결은 변경하지 않았다. D1 스키마와 운영 매물·고객·찜 데이터도 직접 변경하지 않았다.
- 동일한 과거 키를 공유하는 `M-first`, `M-second` 두 매물을 VM으로 재현해 `property:M-first`만 저장된 폴더의 지도 필터 결과가 정확히 한 건인지 동적으로 검증했다. JavaScript 문법 검사, 전체 테스트 `310/310`, Cloudflare 전용 테스트 `153/153`, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 자산 버전은 `script.js?v=6.10.8-favorite-property-id`, `unified-favorites-v7.js?v=7.0.5-property-id-only`이다.
- 기능 커밋 `36ab1d9`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `6f5c0afd-0f91-47f6-9a95-b6e7c3ce7899`로 배포했다. Workers 도메인·`js-map.com`·`www.js-map.com`과 새 JavaScript 2개는 모두 HTTP 200이며, 카드의 property 참조와 공통 키 확장 제거 코드가 실제 응답에 포함됐다. 로그인된 운영 화면에서 `서구 괴정동 423-4` 검색 결과의 서로 다른 5층 대표매물들을 확인한 뒤 사용자의 `1111` 찜폴더(저장 1개)를 `지도 보기`로 열었다. 찜한 `중소형사무실 보 1,000/월 50·15.1평` 한 건만 남고, 함께 보이던 다른 5층 매물은 제외됐으며 지도 집계와 검색 결과도 모두 1개였다. 운영 찜·매물 데이터는 변경하지 않았다.

## 2026-08-20 연결 원본 기준 동일매물 자동통합·명확한 호실 자동분리

- `서구 괴정동 423-4 5층` 대표카드는 과거 조건 `1,000/45`를 유지하지만 연결된 네이버 원본은 `1,000/40·20.1㎡`로 갱신돼 있었다. 새 사진 원본 `2642474405`도 `1,000/40·20.1㎡`로 정확히 같았지만 수집기가 대표행 `1,000/45`만 비교해 `공간 식별정보가 부족함` 검증대기로 보낸 원인을 확인했다.
- 신규 원본의 동일 공간 판정은 이제 대표행뿐 아니라 해당 대표에 연결된 모든 활성 원본의 층·호실·보증금·월세·면적 스냅샷도 함께 비교한다. 대표 조건이 오래됐더라도 활성 원본과 주소·층·조건·면적이 정확히 일치하면 같은 대표에 자동 연결한다.
- `501호`와 `502호`, `B01호`와 `B02호`처럼 양쪽 호실이 명확하고 서로 다르면 검증대기로 보내지 않고 별도 매물로 자동 생성한다. 대표가 `5층`처럼 일반 표기여도 연결 원본이 `501호`로 확인된 상태에서 신규 원본이 `502호`이면 서로 다른 공간으로 처리한다. 호실이 없거나 모호하고 조건만으로 확정할 수 없는 경우에만 기존 검증 안전장치를 유지한다.
- 검증 자동판정 버전을 6으로 올려 기존 검증대기 건도 새 기준으로 다시 평가한다. 대표 검증행과 동일하다고 먼저 묶여 있던 네이버·당근 별칭 원본도 대표행이 자동 통합될 때 함께 연결하고 사진·원본 링크가 남지 않게 처리한다. 당근 원본이 포함된 경우 기존 당근 대표 우선순위는 그대로 적용한다.
- 자동정리 전에 사용자가 검증화면에서 대표 검증행을 직접 `동일매물`로 처리하더라도, 그 검증행을 가리키는 반복등록 별칭 원본을 같은 대표에 함께 연결한다. 개별 별칭 저장 실패는 오류를 기록하고 성공 건수와 실패 건수를 응답에서 분리해 대표 검증행 처리 결과를 숨기지 않는다.
- 수동 `동일매물` 처리는 대상 대표 ID를 `affectedListingIds`에 항상 포함한다. 따라서 새 원본·사진을 연결한 직후 해당 대표의 상세 R2 캐시까지 정확히 무효화된다. 이전 구현은 임대조건 변경 통합에만 대상 ID를 반환해 일반 동일매물 통합 후 최대 1시간 동안 상세창이 과거 `원본 1개·사진 없음` 캐시를 보여줄 수 있었다. 기존 잔존 캐시를 즉시 우회하도록 목록 캐시는 `unified-listings-v5-source-aware-review`, 상세 캐시는 `unified-detail-v3-source-aware-review` 세대로 올렸다.
- 상세 API는 제공처 원본 JSON 위에 수집기가 정규화한 목록 스냅샷을 덮어쓴다. 당근 원본의 제곱미터 `66.49`가 정규화된 `20.1평`을 다시 덮어 상세 대표 면적만 `66.49평`으로 보이던 단위 혼동을 제거했다. 이 보정 캐시 세대는 최종 `unified-detail-v4-normalized-snapshot`이며 카드·상세가 동일한 평수와 임대조건을 사용한다.
- 괴정동 재현, 오래된 대표 조건 복구, 일반 층 아래 명확한 다른 호실 분리, 영문 지하 호실 분리를 포함한 집중 테스트 `13/13`, 전체 테스트 `310/310`, Cloudflare 전용 테스트 `153/153`, JavaScript 문법 검사, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 배포 전 읽기 전용 D1 재검사에서 대상 대표 원본 1개, 검증대기 네이버 사진 원본 1개와 그 별칭 네이버·당근 사진 원본 2개를 확인했으며 이 단계에서는 운영 데이터를 변경하지 않았다.
- 기능·보완 커밋 `10ca918`, `2616211`, `542169d`, `d6d61c2`를 원격 `codex/cloudflare-js-map`에 푸시하고 최종 운영 Worker `b2a11a68-cae0-4b4b-9d98-2aa9c1ff3336`으로 배포했다. 운영 검증화면에서 대상 대표 `M-408ec911-8f6c-482a-ba13-b95b1a023cf8`에 네이버 검증행 `2642474405`를 동일매물로 처리했으며 동일 검증행의 네이버 별칭 `2642115751`과 당근 별칭 `4158260`도 함께 연결됐다. 기존 네이버 `2640046222`까지 활성 원본은 총 4건, 연결 사진은 18장이다. 운영 웹을 새로고침해 `서구 괴정동 423-4`를 검색했을 때 카드와 상세 모두 `보 1,000/월 40·20.1평`, 당근 대표, `동일 공간 원본 4개`와 당근 1건·네이버 3건, 사진 갤러리가 표시됐고 콘솔 오류는 0건이었다.

## 2026-08-20 매물카드 마우스·메모 입력 반응 최적화

- 로그인된 운영 화면과 코드를 함께 점검해 카드 위 포인터가 지나갈 때마다 상세사진과 공실박스 연락처 선행조회가 즉시 시작되고, 메모 입력은 글자마다 `scrollHeight`를 읽어 강제 레이아웃을 만들며, 메모 저장 성공 시 같은 카드를 연속 두 번 다시 생성하던 병목을 확인했다. 운영 화면은 카드 18개·클러스터 62개·DOM 요소 약 5,375개였고 앱 오류는 없었으며 위치 권한 경고만 있었다.
- 카드 상세·연락처 선행조회는 포인터가 카드에 180ms 이상 머물 때만 실행한다. 포인터가 떠나거나 카드를 누르면 예약을 취소해 목록을 빠르게 훑을 때 불필요한 API 요청과 사진 디코딩이 연속 발생하지 않게 했다. 실제 클릭 시 상세창이 직접 조회하는 기존 흐름, 전화 버튼의 명시적 연락처 조회, 카드 더블클릭·선택·지도 연결은 변경하지 않았다.
- 메모 자동 높이 계산을 브라우저 프레임당 한 번으로 합쳐 연속 입력 중 강제 레이아웃을 반복하지 않는다. 편집창 최초 열기에는 즉시 한 번 계산해 기존 높이를 유지한다. 저장 성공 표시는 `showMemoStatusV655()` 한 경로로만 렌더링해 성공 직후 중복 카드 재생성을 제거했으며, D1 메모·연락처 검증 저장과 실패 시 입력 복원은 그대로 유지한다.
- 지도 위 고정 도구 버튼은 배치·색상·hover·선택 상태를 유지하면서 실시간 `backdrop-filter` 흐림만 제거하고 불투명도 90%의 흰 배경으로 바꿨다. 카카오 지도와 클러스터가 다시 그려질 때마다 뒤 배경을 재합성하는 그래픽 비용을 줄였다.
- 새 성능 회귀 테스트는 연속 메모 입력 두 번이 한 프레임 예약과 한 번의 `scrollHeight` 읽기로 합쳐지는지 동적으로 검증하고, 카드 180ms 지연·이탈/클릭 취소·중복 저장완료 렌더링 방지·새 자산 버전을 검사한다. JavaScript 문법 검사, 전체 테스트 `308/308`, Cloudflare 전용 테스트 `153/153`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 자산 버전은 `script.js?v=6.10.7-interaction-smooth`, `header-professional-v1.css?v=1.0.7-interaction-smooth`이다. D1 스키마·운영 매물·메모·고객·원본 연결·수집기는 직접 변경하지 않았다.
- 기능 커밋 `602c543`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `88ba083e-2580-4938-b547-185ab93ce061`로 배포했다. Workers 도메인·`js-map.com`·`www.js-map.com`과 새 JavaScript/CSS는 모두 HTTP 200이며 새 버전, 180ms 카드 지연, 프레임 단위 메모 높이 계산, 흐림 제거 스타일을 반환한다. 로그인된 운영 화면에서 카드 18개·클러스터 62개·DOM 요소 5,327개가 정상 렌더링됐고 새 자산과 `backdrop-filter: none`, 흰 배경 90%가 실제 적용됐다. 기존 메모를 열어 편집 중 임시문자 입력·되돌리기·취소·닫기를 실행했을 때 원문과 높이가 유지되고 편집창이 정상 제거됐으며, 카드 본문 클릭 시 선택 카드와 지도 클러스터가 각각 1개만 강조되고 상세창 닫기도 정상 동작했다. 운영 데이터는 변경하지 않았다.

## 2026-08-19 네이버 로드뷰 디자인 1~4번 정돈

- 사용자와 합의한 디자인 개선 1~4번만 적용했다. 상단의 네이버맵·과거사진·카카오맵·지도 버튼 묶음과 매물명·주소 사이에 14px 여백과 얇은 세로 구분선을 두고, 긴 매물 정보는 헤더를 밀어내지 않도록 말줄임 처리했다. 모바일에서는 좁은 폭을 보호하기 위해 구분선만 숨기며 기존 탭·닫기 버튼·주소 정보는 유지한다.
- `과거사진 보기`는 선택된 네이버맵 탭과 경쟁하지 않도록 흰색·회색 테두리의 보조 버튼으로 바꾸고 히스토리 아이콘을 추가했다. 네이버 계열 초록색은 hover·focus에만 사용하며, 파노라마 준비 전 비활성화와 공식 네이버 거리뷰 직행 로직은 변경하지 않았다.
- 연동 지도 오른쪽 위에 실제 마커 색과 일치하는 `파랑=매물 위치`, `초록=현재 거리뷰` 범례를 추가했다. 거리뷰 2/3·지도 1/3 비율과 파란 도로 이동 도움말은 그대로 유지했다. 거리뷰와 지도 경계는 1px 회색선과 약한 그림자로 분리했고, 네이버 지도 인증 실패로 지도 패널을 숨길 때는 경계도 함께 제거한다.
- 새 디자인 회귀 테스트를 추가하고 관련 로드뷰·네이버 지도 인증 테스트 `4/4`, 전체 테스트 `307/307`, Cloudflare 전용 테스트 `153/153`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 최초 dry-run에서 OneDrive의 순간 파일 잠금으로 `EBUSY`가 한 번 발생했으나 동일 생성 자산을 즉시 재검사해 정상 통과했으며 코드·자산 오류는 없었다. CSS 자산 버전은 `unified-listings-v8.css?v=8.0.46-roadview-polish`이다. JavaScript·D1·로드뷰 연결·매물 데이터는 변경하지 않았다.
- 기능 커밋 `d9098ff`를 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `c398aa9c-b0d6-4473-9536-ed77d20c2355`로 배포했다. Workers 도메인·`js-map.com`·`www.js-map.com` 모두 HTTP 200과 새 HTML 마크업을 반환하고 새 CSS도 HTTP 200과 구분선·중립 버튼·범례·패널 경계 스타일을 반환한다. 로그인된 운영 화면에서 1280px 데스크톱의 거리뷰:지도 폭이 정확히 `2:1`이고 범례 두 항목, 24px 구분선, 활성화된 과거사진 보조 버튼이 표시되는 것을 확인했다. 네이버→카카오→지도→네이버 전환 시 선택 패널과 과거사진 활성 상태가 정상이며, 390px 모바일에서는 가로 넘침 없이 탭·닫기 버튼이 보이고 구분선과 연동 지도만 반응형 규칙대로 숨겨졌다. 전체 검수 중 콘솔 오류는 0건이었다.

## 2026-08-19 네이버 공식 거리뷰 과거사진 직행 버튼

- 로드뷰 모달 상단의 `네이버맵`과 `카카오맵` 사이에 `과거사진 보기 ↗` 버튼을 추가했다. 네이버 파노라마가 실제로 준비된 뒤에만 버튼이 활성화되며, 로딩 중·조회 실패·카카오맵/지도 탭·모달 종료 상태에서는 비활성화돼 이전 매물의 파노라마를 잘못 열지 않는다.
- 버튼을 누르면 현재 네이버 Panorama SDK의 `panoId`와 사용자가 보고 있던 `pan/tilt/fov`를 네이버 지도 웹의 전체 거리뷰 공유 주소에 전달한다. 지도 검색 화면을 거치지 않고 같은 위치·방향의 공식 네이버 거리뷰를 새 탭으로 열며, 사용자는 공식 화면의 촬영 날짜 메뉴에서 과거사진을 선택한다. 새 창은 `noopener,noreferrer`로 열고 파노라마 ID는 URL 인코딩한다.
- 브라우저에서 네이버 지도 웹이 현재 생성하는 `p=<panoId>,<pan>,<tilt>,<fov>,Float` 주소를 직접 재확인했다. 해당 주소를 새 탭에서 다시 열었을 때 전체 파노라마 뷰어와 최신 촬영 날짜 버튼이 바로 표시됐다. 이 주소 형식은 네이버가 공개 API 문서로 보장하는 스킴은 아니므로, 네이버 지도 웹의 공유 주소 규칙이 바뀌면 링크 생성부만 보정해야 한다.
- 데스크톱·모바일·태블릿 가로모드에서 기존 세 탭과 닫기 버튼을 밀어내지 않도록 전용 버튼 폭·글자 크기·비활성 상태를 반응형으로 구성했다. 기존 네이버/카카오 거리뷰 로딩, 연동 지도, 촬영일 표시, 매물·D1 데이터와 스키마는 변경하지 않았다.
- 자산 버전은 `script.js?v=6.10.6-naver-history-deeplink`, `unified-listings-v8.css?v=8.0.45-naver-history-button`이다. JavaScript 문법 검사, 새 직행 링크 동적 테스트를 포함한 전체 테스트 `306/306`, Cloudflare 전용 테스트 `153/153`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다.
- 기능 커밋 `f4deec5`를 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `3a901380-7662-43cc-8092-a6ad35d30ad8`로 배포했다. 운영 홈·새 JavaScript·새 CSS는 모두 HTTP 200이며 버튼 마크업·직행 링크 생성 함수·반응형 스타일을 반환한다. 로그인된 운영 화면에서 첫 매물 로드뷰를 열어 `과거사진 보기 ↗` 버튼이 네이버맵 바로 옆에 표시되고 촬영일 `2026년 04월`과 함께 정상 활성화되는 것을 확인했다. 버튼 클릭 후 공식 네이버 전체 거리뷰 `p=PhOlx5dUVz-XqkzpJHr_Pg,0,-7,80,Float`가 새 탭으로 열렸고, 파노라마 뷰어와 `2026년 04월(최신)` 날짜 선택 버튼이 표시됐다. JS부동산과 네이버 탭의 콘솔 오류는 없었다.

## 2026-08-19 동일 주소·동일 층 매물/지도 선택 강조 완전 분리

- 최신 `적용 필터 칩·매물/지도 선택 강조`를 후속 점검한 결과, 체크박스 작업 선택은 이미 `propertyId + D1 행`으로 분리돼 있었지만 새 파란 선택 강조는 주소·층·종류 기반 `item.key`를 다시 사용하고 있었다. 같은 주소·같은 1층의 별도 대표매물들은 이 표시 키가 같을 수 있어 한 카드를 눌러도 여러 카드가 파랗게 보이고 첫 번째 동일 키 매물의 클러스터를 잡을 수 있는 회귀 가능성을 수정했다.
- 선택 강조 전용 ID `selectedListCardIdV845`를 추가하고 기존 체크박스와 같은 `propertyId + D1 행` 카드 인스턴스 키를 사용한다. 카드 본문·상세카드·스마트 AI 카드에서 매물을 선택하면 정확한 카드 한 장과 그 매물이 실제 포함된 지도 클러스터 하나만 강조된다. 기존 `selectedItemKey`는 AI 분석·메모 등 과거 기능 계약을 위해 그대로 유지한다.
- 자동 목록 갱신도 주소 키 목록만 저장하지 않고 고유 카드 ID 순서를 함께 저장한다. 같은 표시 키의 별도 매물이 두 개 이상 있어도 갱신 후 서로 바뀌거나 같은 매물이 중복 복원되지 않으며 원래 카드 순서와 선택을 유지한다.
- 필터 초기화·임장필터·계약완료 보기·다중클러스터 해제·클러스터 직접 선택·매물 삭제·대표 전체 합치기에서 주소 키와 고유 카드 ID를 함께 해제한다. 매물 수정으로 표시 키가 바뀌면 같은 카드 ID로 선택을 이어가며, 기존 체크박스·분리/합치기·대표값·필터 칩·지도 클러스터 집계 로직은 변경하지 않았다.
- 동적 회귀검사에서 같은 `item.key`를 가진 `M-first/21행`, `M-second/22행` 두 매물을 재현했다. 두 번째 카드를 선택했을 때 두 번째 카드와 두 번째 클러스터만 선택되고, 자동갱신 뒤에도 `M-first → M-second` 순서로 각각 복원되는 것을 확인했다.
- 자산 버전은 `script.js?v=6.10.5-unique-linked-selection`, `map.js?v=8.2.18-unique-linked-selection`, `unified-listings-v8.js?v=8.1.33-unique-linked-selection`, `analysis.js?v=6.3.41-unique-linked-selection`, `property-edit-v648.js?v=1.0.1-linked-selection`이다. D1 스키마·운영 매물·통합 원본 연결·고객·수집 데이터는 수정하지 않았다.
- 변경 JavaScript 5개 문법 검사, 선택 집중 테스트 `9/9`, 전체 테스트 `305/305`, Cloudflare 전용 테스트 `153/153`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다.
- 기능 커밋 `e90b605`를 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `ed1edf3e-cf67-4076-a8ca-e0d7f7284b36`으로 배포했다. 운영 홈과 새 script/map/unified 자산은 모두 HTTP 200이며 새 버전과 고유 카드 ID 코드를 반환했다.
- 로그인된 운영 화면에서 매물 `10,151개`, 카드 18개, 클러스터 62개가 정상 렌더링됐다. `갈마동 366-10` 검색으로 같은 표시 키를 공유하는 `M-af744599...`의 `3,000/150`과 `M-b4795878...`의 `1,000/75` 두 1층 매물을 재현해, 두 번째 클릭 시 두 번째 카드·클러스터만 각각 1개 선택되고 첫 번째 클릭 시에도 선택 개수가 각각 1개인 채 정확히 이동하는 것을 확인했다. 앱 오류는 없었고 브라우저 위치 권한이 없는 환경의 `GeolocationPositionError` 경고만 있었다.

## 2026-08-19 적용 필터 칩·매물/지도 선택 강조

- 디자인 개선 목록의 4번·5번만 적용했다. 출처·구분·중개보수, 검색어, 보증금·월세·권리금·평수·층 범위, 업종, 임장/오늘 신규/계약완료/반경 조건이 적용되면 매물목록 필터 바로 아래에 파란 칩으로 표시된다. 칩의 `×`를 누르면 해당 조건만 해제하고 다른 필터는 유지한 채 목록과 지도를 즉시 다시 계산한다. 기본값인 `계약완료 숨김`과 정렬 방식은 불필요한 칩으로 표시하지 않는다.
- 매물카드 선택 시 기존 `selectedItemKey → selectedGroupKey → redrawSelectedMarkers()` 연결 흐름을 공통 `selectListingOnMapV844()`로 묶었다. 운영 검증에서 더블클릭 스마트카드는 연결됐지만 평소 한 번 클릭 상세카드는 선택 상태를 만들지 않던 기존 차이를 발견해, 이제 버튼이 아닌 카드 본문을 한 번 클릭해 상세카드를 열 때도 같은 연결 상태를 먼저 적용한다. 매물 합치기 대상 선택과 카드 안 전화·로드뷰·수정 등 개별 버튼은 이 연결을 건드리지 않는다. 선택 카드에는 굵은 파란 왼쪽선·테두리·옅은 파란 배경을 표시하고, 같은 매물이 속한 일반·고정 공간·행정구역 클러스터도 파란색 링과 그림자로 강조한다. 다른 카드를 누르면 단일 선택 상태를 따라 강조가 새 카드와 클러스터로 이동한다.
- 이전 요청대로 일반 클러스터의 흰 테두리는 되살리지 않았다. 선택 클러스터도 흰 외곽선 없이 파란 반투명 링만 사용하며, 모션 감소 설정에서는 강조 애니메이션을 끈다. 모바일은 칩을 한 줄 가로 스크롤로 고정하고 칩이 보일 때 목록 높이를 그만큼 줄여 카드가 가려지지 않게 했다.
- 새 집중 테스트 4개를 포함한 전체 테스트 `303/303`, Cloudflare 전용 테스트 `153/153`, 변경 JavaScript 문법 검사, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 자산 버전은 `script.js?v=6.10.4-filter-chips-selection`, `unified-listings-v8.js?v=8.1.32-linked-selection`, `map.js?v=8.2.17-linked-selection`, `unified-listings-v8.css?v=8.0.44-filter-chips-selection`, `mobile-app-v1.css?v=1.0.7-filter-chips-selection`이다. D1 스키마·운영 매물·고객·통합 원본 연결·수집기는 변경하지 않았다.
- 기능 커밋 `97591ac`과 한 번 클릭 연결 보완 커밋 `e80e12a`를 원격 `codex/cloudflare-js-map`에 푸시하고 최종 운영 Worker `aebb5d51-c851-4d8c-9267-fdb9d36e6f01`로 배포했다. 운영 홈과 새 JS·CSS 자산은 모두 HTTP 200을 반환했다. 로그인된 운영 화면에서 카드 18개·클러스터 62개가 정상 렌더링된 상태로 `출처 네이버`와 `중개보수 200 이상` 칩을 동시에 만들고 중개보수 칩만 제거해 출처 조건이 유지되는 것을 확인했다. 카드 본문을 한 번 클릭하면 상세카드와 함께 선택 카드 1개·파란 클러스터 1개가 표시되고, 다른 카드 클릭 시 두 선택 상태가 각각 1개인 채 새 매물로 이동하는 것도 확인했다.

## 2026-08-19 매물카드 승강기 유무 아이콘 복구

- 8월 18일 카드 경량화 때 승강기 정원 숫자뿐 아니라 승강기 아이콘 HTML과 CSS까지 함께 제거돼, 대장에 승강기가 확인된 매물도 카드에서 아무 표시가 보이지 않던 회귀 결함을 수정했다.
- 매물카드는 건축물대장 저장값이 `확인완료/connected`이고 승강기가 1대 이상인 경우에만 기존 승강기 모양 아이콘을 표시한다. 카드에는 승강기 대수와 `10인·15인` 같은 최대정원 숫자, 정원 미확인 표식을 만들지 않는다.
- 카드가 열린 뒤 건축물대장 배지가 갱신되는 경우에도 같은 유무 규칙으로 아이콘만 표시하며 툴팁과 접근성 문구도 `승강기 있음`으로 제한했다. 대장 버튼으로 여는 상세 화면의 `승용/비상용` 대수와 `최대정원 n인` 표시는 그대로 유지했다.
- 카드 진입 시 외부 승강기 API를 부르지 않고 D1에 이미 저장된 건축정보만 사용한다. D1 스키마·운영 매물·고객·원본 연결·승강기 데이터는 변경하지 않았다.
- JavaScript 문법 검사, 승강기·카드 집중 테스트 `13/13`, 전체 테스트 `299/299`, Cloudflare 전용 테스트 `153/153`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 자산 버전은 `script.js?v=6.10.3-card-elevator-icon`, `building-register-v6.js?v=8.2.9-card-elevator-icon`, `unified-listings-v8.css?v=8.0.43-card-elevator-icon`이다.
- 작업 커밋 `54296a6`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `d56882a9-2ce4-40f0-903b-5d083a67bef8`로 배포했다. 운영 홈과 새 JS·CSS는 모두 HTTP 200과 새 버전을 반환했다. 로그인된 운영 화면에서 카드 18개 중 저장된 승강기 정보가 있는 6개에 아이콘이 표시되고, 카드 정원 숫자 요소는 0개이며 아이콘 접근성 문구는 모두 `승강기 있음`인 것을 확인했다.

## 2026-08-19 공실박스 수동 전체수집 목록조회 중단 자동복구

- 사용자가 공실박스에서 선택한 `2,615개` 전체클러스터가 화면의 최초 20건만 표시한 채 `처리 완료 0`, `조회·저장 실패 1`로 멈추는 현상을 진단했다. 운영 D1에는 같은 날 새 공실박스 수집 세션이 생성되지 않았으므로 D1 저장 오류가 아니라, 수집기가 선택 ID를 300건씩 다시 읽는 공실박스 `/api/maps/lists` 목록 확장 단계에서 한 번의 요청 실패로 전체를 중단한 것이 원인이다.
- 공실박스 수집기 `2.1.5`는 각 목록 요청을 최대 3회 자동 재연결한다. 반복 실패한 300건 묶음은 절반씩 재분할해 정상 매물은 계속 읽고, 끝까지 응답하지 않거나 목록에서 사라진 단일 원본만 실패 목록으로 격리해 나머지 수집·저장을 계속한다. 처음 화면에서 이미 받은 20건은 불필요하게 다시 요청하지 않는다.
- 목록조회 제외 또는 단일 D1 저장 실패가 있어도 해당 원본을 다음 수집에서 다시 확인하도록 남기고 나머지를 끝까지 처리한다. 실패가 하나라도 있으면 완전수집·미노출 판정을 실행하지 않으므로 기존 JS 매물은 숨겨지지 않는다. 저장 진행 위치와 실패 원인은 브라우저 체크포인트에 함께 보존한다.
- 공실박스 집중 테스트, 변경 JavaScript 문법 검사, 전체 테스트 `299/299`, Cloudflare 전용 테스트 `153/153`, 정적 자산 130개 빌드, Wrangler 4.124.0 dry-run과 `git diff --check`를 통과했다. 운영 DB는 진단용 읽기만 실행했으며 스키마·매물·고객·대표 연결은 직접 변경하지 않았다.
- 작업 커밋 `d066546`을 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `5a4973b5-24b3-4a36-a277-909192bd2f19`로 배포했다. `https://js-map.com/js/gongsil-collector.js`가 HTTP 200, 버전 `2.1.5`, 목록 3회 재시도·재분할·단일 실패 계속처리 코드를 반환하고 메인 사이트도 HTTP 200과 정상 제목을 반환하는 것을 확인했다. 이미 열려 있는 `2.1.4` 패널은 사용자가 `JS 매물 수집`을 다시 누르면 기존 선택 클러스터를 이어받아 새 런타임으로 교체된다.

## 2026-08-19 자동수집 실제 시작 확인·진행 감시 자동복구

- 오전 11시 예약 실행은 Windows 작업 스케줄러에서 정상 시작·종료 코드 0이었지만, 네이버 3개 구 완료 뒤 제공처 탭만 열린 채 다음 서버 수집 세션이 만들어지지 않는 상태를 확인했다. 기존 설치본 1.0.35의 임시 감시는 콘텐츠 스크립트가 메시지를 받은 순간을 실제 수집 시작으로 오인했고, 진행 숫자의 실제 속성인 `data-metric`이 아니라 존재하지 않는 `data-role^=metric-`만 읽어 화면이 살아 있는 상태와 실제 진행을 정확히 구분하지 못했다.
- 확장 프로그램 1.0.36은 메인 페이지 수집 런타임이 로드되고 `runAutomatic()`을 실제 호출한 뒤에만 `JS_COLLECTOR_AUTOMATION_STARTED` 확인 신호를 보낸다. 런타임은 최대 30초 기다리고, 콘텐츠는 35초 안에 실제 시작 확인이 없으면 실패로 반환하며, 백그라운드는 2분 안에 시작 단계가 끝나지 않으면 같은 대상을 저장 지점부터 자동 재시도한다. 같은 실행 ID의 중복 시작 메시지는 한 번만 실행한다.
- 페이지 새로고침은 로딩·시작확인·수집 단계 모두 즉시 재연결한다. 1분 상태 신호가 8분 없거나 상태·퍼센트·상세문구·진행 막대·전체 `data-metric` 숫자가 20분 동안 전혀 변하지 않으면 마지막 저장 지점에서 복구하고 알림을 남긴다. 진행 화면에도 현재 단계, 진행 문구와 마지막 진행 시각을 표시한다.
- 새 설치의 기본 예약 시간과 설치 스크립트 기본값도 사용자가 지정한 매일 `11:00`으로 통일했다. 실제 Windows 작업 `JSMap Auto Collector`의 다음 실행은 2026-08-20 11:00이며, 기존 저장 설정과 대상 목록은 보존했다.
- 이 PC의 전용 Edge 확장을 1.0.36으로 교체하고 중단 지점부터 재개했다. 재개 후 네이버 목록 `8,500/8,500`을 찾고 빠른 비교 `5,000/8,500`에서 실제 처리 `7,621`, 남은 `879`까지 계속 증가하는 것을 확인했다. 운영 D1에도 새 세션 `S-NAVER-1787121771208-q3r2gcm2`가 `running`으로 생성돼 브라우저 표시와 서버 저장이 함께 진행 중임을 확인했다.
- 변경 JavaScript 4개 문법 검사, 자동수집 집중 테스트 `8/8`, 전체 테스트 `299/299`, `git diff --check`를 통과했다. D1 스키마·운영 매물·대표매물 연결·고객 데이터는 직접 수정하지 않았으며 현재 실행 중인 정상 수집 결과만 기존 수집 API로 저장된다.
- 작업 커밋 `2d623a5`를 원격 `codex/cloudflare-js-map`에 푸시하고 운영 Worker `89c1a1e0-4f23-4deb-a29b-f6aac6dd6f94`로 배포했다. `https://js-map.com`은 HTTP 200과 정상 제목을 반환했다. 배포 후에도 같은 세션의 브라우저 처리가 `7,871/8,500`, 남은 `629`로 증가했고 D1 `updated_at`도 `2026-08-19T06:48:48.335Z`까지 갱신돼 수집이 중단되지 않았음을 재확인했다.

## 2026-08-19 중개보수 필터 누적 하한 방식 전환

- 실사용 선택 기준에 맞춰 중개보수 필터를 서로 겹치지 않던 구간형에서 `100 이하`, `100 이상`, `200 이상`, `300 이상`, `500 이상` 누적 기준으로 변경했다. 예를 들어 계산 중개보수가 360만원이면 `100 이상`, `200 이상`, `300 이상`에 모두 포함된다.
- 경계값은 문구 그대로 포함한다. 정확히 100만원인 매물은 `100 이하`와 `100 이상` 양쪽에 포함되고, 정확히 500만원인 매물은 `100·200·300·500 이상`에 모두 포함된다. 과거 화면 DOM이 잠시 남은 경우에도 기존 선택값을 같은 순서의 새 의미로 호환한다.
- 상가 임대차 법정 상한요율 0.9%, 거래금액의 월세 100배와 5,000만원 미만 70배 계산식, 목록·지도·정렬·다른 필터 조합 방식은 변경하지 않았다. D1 스키마와 운영 매물 데이터도 수정하지 않았다.
- 자산 버전은 `commercial-brokerage-v1.js?v=1.2.0-minimum-thresholds`다. 집중 테스트 `9/9`, 전체 테스트 `299/299`, Cloudflare 전용 테스트 `153/153`, 정확히 100만원인 경계값 회귀검사, JavaScript 문법 검사, Cloudflare 정적 자산 130개 빌드와 Wrangler 4.124.0 dry-run, `git diff --check`를 통과했다.
- 운영 Worker `f59aca51-b9c0-42e6-85cb-a24e55abf2a0`으로 `js-map.com`에 배포했다. 로그인된 운영 화면에서 매물 `10,099개`가 정상 로드되고 새 선택지 5개가 표시되는 것을 확인했다. `500 이상` 선택 시 395개, `100 이하` 선택 시 3,152개로 목록과 지도 클러스터가 함께 갱신돼 실제 누적 하한·상한 필터 동작도 확인했다.

## 2026-08-19 전일 변경 전수검사 및 새로고침·빠른등록 결함 수정

- 8월 18일 작업 직전 기준 `381ec08`부터 집에서 복구·푸시한 `6ca4995`, `26e66d4`, `729447a`와 이번 미커밋 변경까지 기능·로딩 순서·분리 모듈·Cloudflare 저장 경계를 전수 비교했다. 사무실의 오래된 미커밋본은 `office-2026-08-18-pre-home-sync-backup` stash에 안전하게 보존했으며 강의 전사 관련 개인 미추적 파일은 건드리지 않았다.
- 운영 웹에서 로그인 상태로 새로고침하면 간헐적으로 `setupQuickAddShortcuts is not defined`가 발생하고 매물목록이 0건에 머무는 결함을 재현했다. 인증 필수 스크립트를 병렬 다운로드한 뒤 DOM 순서대로 실행하는 과정에서 `map.js`가 `parser.js`보다 먼저 실행될 수 있었던 것이 원인이며, `parser.js?v=6.4.19-distinct-listing-save`를 초기 필수 자산으로 올려 항상 초기 캐시 다음·지도 초기화 전에 실행되도록 고정했다.
- 빠른등록 서버가 주소만 같은 유사 매물까지 중복으로 차단해 같은 건물의 다른 층·호실 매물이 저장되지 않을 수 있던 결함을 수정했다. 이제 주소·호실/층·보증금·월세·면적이 모두 같은 정확한 중복만 차단하고, 같은 주소라도 층·조건·면적이 다르면 별도 매물로 저장한다. 정확 중복 SQL에도 면적 비교를 추가했으며 두 경우를 D1 모의 실행 테스트로 고정했다.
- 메인 지도의 공용 지오코딩 좌표 저장은 `JSDataAccessV6.mutate("saveGeocodeCache")` 공통 경계를 사용한다. 공통 저장 성공·구형 화면 직접 fallback·공통 저장 실패 시 대기열 유지와 이중 저장 방지를 동적 테스트로 검증했다.
- 개발 도구 의존성의 알려진 보안 경고를 없애기 위해 Wrangler를 `4.124.0`, 간접 의존성 undici를 `7.29.0`으로 올리고 실제 요구사항에 맞춰 Node.js 최소 버전을 22로 명시했다. 저장소의 14일 안정화 정책에서 이 보안 수정 버전만 예외로 허용했으며 `pnpm audit --audit-level=low` 결과 알려진 취약점은 0건이다.
- 전체 테스트 `299/299`, Cloudflare 전용 테스트 `153/153`, 변경 JavaScript 14개 문법 검사, Cloudflare 정적 자산 130개 빌드와 Wrangler 4.124.0 dry-run, `git diff --check`를 통과했다. D1 스키마·운영 매물·고객·통합 원본 연결·R2 객체는 변경하지 않았고 운영 배포도 실행하지 않았다. 따라서 현재 운영 웹의 새로고침 결함은 이 커밋을 별도로 배포한 뒤 해소된다.

## 2026-08-19 지오코딩 좌표 저장 데이터 접근 계층 전환

- 작업 시작 시 원격 브랜치가 사무실 PC보다 3개 커밋 앞선 것을 확인했다. 사무실의 8월 18일 미커밋본은 `office-2026-08-18-pre-home-sync-backup` Git stash로 보존하고, 집에서 복구해 푸시한 `6ca4995`, `26e66d4`, `729447a`를 fast-forward로 반영했다. 강의 전사 관련 개인 미추적 파일은 건드리지 않았다.
- 최신 HANDOFF의 다음 후보였던 빠른등록은 집에서 복구한 최신본에 이미 `JSDataAccessV6.mutate("quickAdd")`로 완료돼 있었다. 따라서 남은 다음 순서인 메인 지도의 공용 지오코딩 좌표 저장을 `JSDataAccessV6.mutate("saveGeocodeCache")` 공통 경계로 전환했다.
- 좌표 저장의 최대 500건 묶음, D1 `geocode_cache` UPSERT, 공용 좌표 R2 캐시만 무효화하는 서버 계약, 실패 항목 대기열 유지와 최대 3회 지연 재시도는 변경하지 않았다. 공통 모듈이 없는 구형 캐시 화면에서만 기존 `/api/data` 직접 POST를 사용하며, 공통 mutation이 실패한 경우 직접 요청으로 중복 저장하지 않는다.
- 메인 스크립트 캐시 버전은 `script.js?v=6.10.2-geocode-data-access`다. 공통 저장 성공, 구형 화면 fallback, 공통 저장 실패 시 대기열 유지·직접 중복 요청 차단을 VM 동적 테스트 3개로 고정했고 Cloudflare 전환 문서에도 현재 경계를 반영했다.
- 지오코딩 변경 단독 기준 전체 테스트 `297/297`, Cloudflare 전용 테스트 `151/151`을 통과했다. 이어진 전수검사 결함 수정과 도구 업데이트까지 포함한 최종 결과는 위 전수검사 항목의 `299/299`, `153/153`, Wrangler 4.124.0 기준이다. D1 스키마·운영 매물·좌표 데이터·R2 객체는 변경하지 않았으며 이번 작업은 배포하지 않았다.

## 2026-08-19 초기화면 빠른 표시 및 중개보수 독립 구간

- 초기화면 지연은 클러스터 방식 하나가 아니라 로그인 뒤 D1 매물 CSV 약 10,078건, 통합 원본매물 메타데이터, 좌표·목록 렌더링이 모두 끝날 때까지 화면이 기다리던 경로가 원인이었다. 변경 전 운영 재측정에서 매물목록 완료까지 약 2.8~3.3초가 걸렸다.
- 인증 직후 `JSDataAccessV6`를 지도 SDK보다 먼저 불러 D1 매물·통합 원본매물 읽기를 즉시 예열한다. 인증에 꼭 필요한 통합매물·중개보수·메인·초기 캐시·지도·모바일 스크립트만 먼저 실행하고 나머지 관리 기능은 화면 잠금을 푼 뒤 순서대로 이어서 불러오도록 로더 우선순위를 분리했다.
- IndexedDB에 최근 매물 최대 80건과 그 매물에 필요한 통합 원본매물 그룹만 3일간 보관하는 `initial-listings-cache-v1.js`를 추가했다. 재접속 때 이 작은 스냅샷으로 카드부터 표시하고 최신 D1 전체목록은 항상 백그라운드에서 다시 읽어 완전히 교체한다. 위치·지도 객체 같은 런타임 값과 1만 건 전체는 저장하지 않으며 로그아웃 때 캐시를 삭제한다.
- 운영 웹의 준비된 캐시 재접속 측정에서 최근 매물 안내와 카드 표시가 약 0.53초에 시작됐고, 뒤이어 최신 필터 결과 7,346건으로 정상 교체됐다. 첫 방문 또는 캐시 만료 뒤 한 번은 최신 전체목록을 받아 작은 캐시를 새로 만든다.
- 중개보수 필터를 누적 상한이 아닌 서로 겹치지 않는 독립 구간으로 변경했다. 경계는 `0~100 이하`, `100 초과~200 이하`, `200 초과~300 이하`, `300 초과~500 이하`, `500 초과`이며 각 매물은 정확히 한 구간에만 포함된다. 중개보수 계산식은 기존 상가 임대차 법정 상한요율 0.9%, 저가 거래 70배 기준을 그대로 유지한다.
- D1 스키마·운영 매물·통합 원본 연결·R2 객체는 수정하지 않았다. 이번 변경은 정적 코드와 로그인한 사용자의 브라우저 로컬 초기화면 캐시에만 적용된다.

### 테스트와 운영 배포

- 전체 테스트 `294/294`, Cloudflare 전용 테스트 `148/148`, JavaScript 문법 검사, `git diff --check`를 통과했다.
- Cloudflare 정적 자산 130개 빌드와 Wrangler 4.118.0 dry-run을 통과하고 운영 Worker `c3c0ea12-9fef-44ea-879e-f5bf32226bb9`로 `js-map.com`에 배포했다.
- 로그인된 운영 웹에서 지도 자산 `8.2.16-fast-cache-snapshot`, 5개 중개보수 구간, 캐시 우선 표시 뒤 최신 전체목록 교체를 확인했다. 콘솔 앱 오류는 없었고 브라우저 위치 권한이 없는 환경의 `GeolocationPositionError` 경고만 확인됐다.

### 다음 작업과 주의사항

- 배포 직후 첫 접속은 스키마 2 캐시를 새로 만드는 정상적인 콜드 로드다. 같은 브라우저의 다음 새로고침부터 빠른 카드 표시가 적용되며, 최신 데이터 정합성을 위해 D1 전체 갱신은 생략하지 않는다.
- 캐시 스냅샷은 의도적으로 80건만 저장한다. 다시 전체 1만 건을 캐시하거나 캐시 단계에서 전체 지도 클러스터를 즉시 그리면 초기 표시 이득이 사라지므로 성능 측정 없이 확대하지 않는다.

## 2026-08-19 8월 18일 사무실 미커밋 작업 전체 복구

- 사무실에서 2026-08-18 작업한 내용은 Git 커밋에는 없었지만 Cloudflare Worker 버전 기록에는 남아 있었다. 마지막 사무실 배포는 버전 200 `ec54745a-8741-47e7-97be-ef45ac33b00b`(2026-08-18 19:35 KST), 그날 작업 직전 기준은 버전 191 `8b976d6c-474a-410d-99d2-a096cdaa727b`(2026-08-17 19:16 KST)로 확인했다. 191→200과 각 중간 배포를 대조해 8월 18일 변경 범위를 다시 구성했다.
- 복구한 기능은 매물카드 승강기 장식 제거, 최종 CSS 259줄을 `app-final-overrides-v690.css`로 분리, 매물 수정·삭제 705줄을 `property-edit-v648.js`로 분리, 찜목록·통합매물·운영센터·매물검증·관리자 화면의 공통 `JSDataAccessV6` 경로 단일화, 빠른등록의 실제 D1 저장결과 확인, 상가 임대차 중개보수 필터, 모바일의 중복 층 빠른필터 제거다.
- 중개보수는 상가 임대차 법정 상한요율 0.9% 기준이며 거래금액은 `보증금 + 월세×100`, 5,000만원 미만이면 `보증금 + 월세×70`을 사용한다. 필터 구간은 100/200/300/500만원 이하와 500만원 초과다. 예시 `3,000/150`은 162만원, `1,000/30`은 27.9만원으로 검증했다.
- 빠른등록은 더 이상 요청 직후 성공으로 표시하거나 입력을 먼저 지우지 않는다. `JSDataAccessV6.mutate("quickAdd")`의 응답이 `persisted:true`일 때만 완료 안내·입력 초기화·목록 갱신을 실행하고, 중복 또는 저장 실패 때는 입력을 보존한다. 현재 Worker의 `cloudflare/src/d1-api.js`가 정확히 이 응답 계약을 제공하는 것도 회귀검사로 고정했다.
- 8월 18일 사무실 `script.js`를 통째로 덮어쓰지 않고, 이후 GitHub에서 추가된 네이버 로드뷰·지도 복구 보강은 유지한 채 사무실 변경만 병합했다. 최종 사무실 배포의 나머지 14개 복구 자산은 내용 또는 기능 단위로 일치하며, 새 모듈 3개는 해당 배포 자산과 동일하다.
- 공통 데이터 전용 화면은 공통 모듈이 준비되지 않았을 때 서로 다른 직접 `fetch` 경로로 우회하지 않고 명확한 초기화 오류를 반환한다. 공지·AI임장·입점진단·비동기 큐·현재 비활성 중복정리처럼 별도 호환 계약이 있는 모듈의 기존 fallback은 변경하지 않았다.
- D1 스키마, 운영 매물, 고객, 원본 연결, R2 객체는 수정하지 않았다. 이번 복구는 코드와 정적 자산만 반영했다.

### 테스트와 운영 배포

- JavaScript 문법 검사, 복구 집중 테스트 37개, 전체 테스트 `290/290`, Cloudflare 전용 테스트 `148/148`, `git diff --check` 통과.
- Cloudflare 정적 자산 129개 빌드와 Wrangler 4.118.0 dry-run 통과. 운영 Worker 버전 `5a92b347-c5a9-413a-9971-26029ec335e7`로 `js-map.com`에 배포했다.
- 운영 홈과 복구 자산을 캐시 우회 조회해 모두 HTTP `200`을 확인했다. 운영 HTML에서 중개보수 필터 노출·층 빠른필터 제거·분리 모듈 로드·공통 데이터 전용 버전을 확인했고, 배포된 중개보수 0.9%/저가 70배 계산, 매물 수정 모듈, 최종 CSS, 빠른등록 `persisted` 확인 코드도 직접 검증했다.
- 앱 내 브라우저에서는 승인 계정 선택 화면까지 확인했으나 Google iframe 자동 선택이 세션 로그인을 완료하지 못해 보호된 매물목록의 실데이터 클릭 회귀는 실행하지 못했다. 인증 전 화면의 콘솔 오류는 없었고, 실제 데이터 변경을 수반하는 빠른등록 제출은 테스트하지 않았다.

### 다음 작업과 주의사항

- 8월 18일 작업은 이번 커밋과 푸시로 처음 Git 이력에 보존한다. 이후 다른 PC에서 작업할 때는 시작 전 `git pull --ff-only`, 종료 전 테스트·HANDOFF·커밋·푸시를 완료해야 같은 덮어쓰기 사고를 막을 수 있다.
- 중개보수 필터는 계산 상한 기준의 목록 필터이며 실제 계약 보수액 입력·저장 기능은 아니다. 부가세는 계산에 포함하지 않는다.

## 2026-08-18 비활성 중복정리 저장 데이터 접근 계층 전환

- 작업 시작 시 `CODEX_HANDOFF.md`를 UTF-8로 전체 확인하고 `git fetch --prune origin`, `git pull --ff-only origin codex/cloudflare-js-map`을 실행했다. 원격의 최신 4개 커밋을 fast-forward로 반영했으며 작업 시작 기준 HEAD는 `381ec08`이었다.
- HANDOFF의 다음 후보였던 `js/listing-duplicate-cleanup-v1.js`의 `consolidateExistingMasters` 저장을 `JSDataAccessV6.mutate()` 공통 경계에 연결했다. 대표매물 ID, 정리 대상 ID 목록, 수동 승인 표시·사유와 정리 성공 후 목록 제거·고객매칭 새로고침·전체 목록 최신화 계약은 그대로 유지했다.
- 이 모듈과 CSS는 2026-08-02부터 `index.html`에 연결되지 않은 비활성 기능이다. 이번 작업에서도 중복정리 버튼이나 UI를 재활성화하지 않았고, 운영 매물·D1 데이터에는 정리 요청을 실행하지 않았다.
- 공통 데이터 모듈이 없거나 mutation 메서드를 제공하지 않는 구형 로딩 환경에서는 기존 `saveApiURL` 또는 `/api/data` 직접 POST 경로를 유지한다. 다만 중복정리는 파괴적 변경이므로 공통 mutation 요청 자체가 실패하면 직접 경로로 자동 재시도하지 않아 이중 실행 가능성을 막는다. 직접 경로도 HTTP 오류, 비정상 JSON, `ok:false`를 모두 실패로 처리한다.
- 공통 경계 사용, 직접 fallback, 공통 요청 실패 시 무재시도, 직접 API 오류 전달을 VM에서 실제 실행하는 동적 테스트 4개를 추가했다. 모듈이 계속 HTML에서 비활성 상태인지 확인하는 회귀 조건도 데이터 접근·중복정리 테스트 양쪽에 고정했다.

### 테스트와 운영 배포

- 집중 테스트 `19/19`, 전체 테스트 `280/280`, Cloudflare 테스트 `147/147` 통과.
- `js/listing-duplicate-cleanup-v1.js`와 새 동적 테스트 문법 검사, Cloudflare 정적 자산 126개 빌드, Wrangler 4.118.0 dry-run, `git diff --check` 통과.
- 운영 Worker `7ee0cdc2-1262-4a81-a9b5-1a3d4997f831`로 배포했다. `https://js-map.com`의 배포 자산과 홈을 캐시 우회 조회해 HTTP `200`, 공통 mutation 코드·직접 fallback 포함, 중복정리 UI 비활성 유지 상태를 확인했다.

### 남은 작업과 주의사항

- 다음 데이터 접근 후보는 빠른등록과 지오코딩 저장 경로다. 저장 계약·캐시 무효화·오프라인 또는 구형 로딩 fallback이 서로 다르므로 기능별 동적 실행 테스트를 먼저 만든 뒤 순차 이관한다.
- 비활성 중복정리 UI를 다시 노출하려면 현재 대표매물 분리·합치기 흐름과 중복되는지 별도 제품 판단과 실제 브라우저 회귀가 필요하다. 데이터 접근 이관만을 이유로 자산을 `index.html`에 다시 연결하지 않는다.

## 2026-08-17 로그인 새로고침 초기 로딩 단축

- 운영 웹의 로그인 새로고침을 직접 계측한 결과, 배포 전에는 브라우저 `load` 이벤트 이후에도 매물 CSV와 약 7.25MB의 통합 원본 응답을 다시 내려받고 순차 처리하면서 카드 표시와 `매물 10048개 불러옴` 완료까지 첫 측정 8.40초, 일반 반복 새로고침 5.46초가 걸렸다. 인증과 기본 화면은 1초 안에 끝났으므로 병목은 데이터 전송·파싱과 불필요한 공용 좌표 캐시 요청이었다.
- 인증 직후 매물 CSV와 통합 원본 조회를 나머지 화면 스크립트 로딩과 병렬로 시작하도록 `JSDataAccessV6.warmInitialData()`를 추가했다. 인증용 head 자산도 CSS·preconnect를 먼저 붙인 뒤 지도 스크립트를 기다리도록 순서를 조정해 화면 스타일 요청이 늦게 시작되지 않게 했다.
- `unifiedListings`와 `geocodeCache` 응답에 사용자별 `private` ETag 재검증을 적용했다. 브라우저는 새로고침 때 최신 여부를 서버에 확인하지만 데이터가 바뀌지 않았으면 `304`로 본문 재전송을 생략한다. 시간값 쿼리로 브라우저 캐시를 매번 무력화하던 동작도 이 두 대용량 조회에서만 제거했다. 브라우저 저장소에는 데이터를 복사하지 않으므로 기존 저장용량 초과 문제와도 무관하다.
- CSV의 좌표 또는 현재 브라우저 메모리의 좌표가 모든 매물에 준비돼 있으면 공용 좌표 캐시를 별도로 내려받지 않고 바로 목록·지도를 그린다. 강제 새로고침, 좌표가 실제로 부족한 예외 경로, 매물/클러스터/검색/필터/상세 기능은 기존 흐름을 유지한다. D1 스키마와 운영 매물 데이터는 변경하지 않았다.
- 운영 Worker `3e16c195-7aa9-44fe-81a1-b2f35b1fd5d7`로 배포 후 같은 로그인 브라우저에서 측정했다. 새 자산을 처음 받는 새로고침은 7.65초로 8.40초 대비 약 9% 단축됐고, 브라우저 재검증이 적용된 연속 새로고침은 4.05초와 3.34초로 기존 5.46초 대비 약 26~39% 단축됐다. 각 측정에서 `매물 10048개 불러옴`, 화면 카드 18개가 정상 완료됐고 최종 화면에서 공간 클러스터 65개, 검색창과 주요 메뉴가 정상 확인됐다. 콘솔에는 앱 오류가 없었고 브라우저 위치 권한 미허용에 따른 기존 경고만 있었다.
- 전체 테스트 `275/275`, Cloudflare 테스트 `142/142`, Cloudflare 자산 126개 빌드, Wrangler 4.118.0 dry-run과 `git diff --check`를 통과했다. 새 회귀 테스트는 인증 후 병렬 예열, 중복 요청 방지, ETag/304 응답, 로컬 좌표 우선 처리를 고정한다.

## 2026-08-17 네이버 자동수집 상세조회 무한대기 방지

- 운영 D1에서 `대전 서구 전체` 네이버 세션 `S-NAVER-1786932848135-8344k9ez`가 수집기 `5.9.1`로 목록 7,999건을 비교하고 첫 상세 묶음 100건까지 정상 저장한 뒤 `2026-08-17 11:15:55 KST`부터 갱신되지 않은 것을 확인했다. 서버 저장 오류는 0건이었고 다음 상세 묶음의 네이버 API 응답을 기다리던 단계였으므로, D1이나 이번 매물 분류 변경이 아니라 상세 `fetch`에 시간제한이 없던 것이 원인이었다.
- 네이버 수집기 `5.9.2`는 각 상세 요청을 15초 후 취소하고 기존 횟수만큼 재시도한다. 끝까지 응답하지 않는 매물은 해당 매물만 실패로 기록하며 같은 묶음의 다른 매물과 이후 묶음은 계속 처리한다. 상세조회 실패는 주소 누락과 구분해 실패 수와 첫 실패 원인에 표시한다.
- 자동수집 Edge 확장 프로그램은 `1.0.33`으로 올리고, 단일 대상 제한시간과 감시 복구 기준을 기존 5시간/6시간에서 90분으로 줄였다. 제한을 넘기면 마지막 저장 지점부터 대상 탭을 재시작한다.
- 멈춘 5.9.1 세션은 기존 저장 100건을 보존한 채 `partial`로 종료하고 오류 원인을 기록했다. 운영 Worker `b83153ee-b6a5-4483-99f5-4da1115a4b97`로 배포하고 이 PC의 전용 Edge 확장을 교체한 뒤 숨김 상태로 자동수집을 재개했다.
- 재개된 `5.9.2` 서구 세션 `S-NAVER-1786941353099-qe4nghva`는 목록 `7,998/7,998`을 처리해 `completed`로 정상 종료했다. 결과는 신규 9건, 기존 대표 합치기 108건, 조건 갱신 4건, 검증대기 29건, 상세 중복 32건, 서버 실패 0건이며 주소·층 정보가 부족한 81건만 제외했다. 전체 테스트 `270/270`, Cloudflare 테스트 `140/140`, 집중 테스트 `37/37`, 자산 126개 빌드와 Wrangler dry-run을 통과했다.

## 2026-08-17 공실박스 명확한 별도 호실의 검증대기 누락 방지

- `서구 도안동 1772 럭키프라자2 114호`의 공실박스 `보증금 300 / 월세 60 / 6평` 원본이 같은 주소의 기존 `1층` 매물들과 가격·면적이 모두 다른데도 `층 표기와 호실 표기 비교 필요`로 검증대기에 머물던 문제를 진단했다. 해당 원본은 기존 대표에 합치지 않고 별도 활성 매물로 먼저 복구했으며 사진 3장과 연락처 2개를 함께 보존했다.
- 공간 판정은 같은 층의 `호실 표기 매물`과 `층만 표기된 매물`을 비교할 때 임대조건이 다르고 면적 차이도 1평 이상이면 이제 `다른 매물`로 확정한다. 같은 호실이 명확하거나 가격·면적이 일치하는 경우의 기존 합치기 규칙은 유지하고, 일부 조건만 달라 불확실한 경우도 계속 검증대기에 남긴다.
- 이 판정은 새 수집의 단일 후보 자동합치기보다 먼저 적용되므로 앞으로 같은 유형이 기존 대표에 잘못 합쳐지거나 검증대기에 숨지 않고 즉시 별도 매물로 생성된다.
- 기존 검증대기도 자동 판정 버전 5로 분당 20건씩 재검사하도록 예약 복구 경로를 연결했다. 공실박스를 먼저 처리한 뒤 다른 출처를 이어서 검사하며, 확실한 다른 매물만 자동 생성하고 동일 매물은 기존 규칙대로 합치고 애매한 항목은 사람 검증 대상으로 유지한다. 자동 합치기로 연결된 대표매물도 상세 캐시 무효화 대상에 포함하고 중복 검증행만 정리된 경우에도 운영현황 캐시가 갱신된다.

### 테스트와 운영 배포

- 럭키프라자2 실제 조건을 재현한 양방향 판정과 기존 동일·애매·다른 층/호실 회귀 테스트를 포함한 집중 테스트 `11/11` 통과.
- 전체 테스트 `270/270`, Cloudflare 테스트 `140/140`, Cloudflare 자산 126개 빌드, Wrangler 4.118.0 dry-run 및 `git diff --check` 통과.
- 공실박스 검증대기 74건을 운영 D1에서 판정 버전 5로 재검사한 결과, 명확한 별도 매물 24건을 새 활성 매물로 생성하고 동일 매물 4건을 기존 대표에 합쳤으며 중복 검증행 2건을 정리했다. 나머지 44건은 조건이 애매해 검증대기에 유지했다. 새로 생성한 24건은 대표매물과 원본 연결이 모두 활성 상태(`24/24`)임을 확인했고, 구버전 미처리 공실박스 검증행은 `0건`이다.
- 운영 Worker 최종 버전 `a7e19056-675a-42de-b8cc-b61e9b2a2cec`로 배포했다. 매물 목록·상세·운영현황·리비전 캐시와 자동 합치기된 대표 4건의 상세 캐시를 무효화했으므로 다음 인증 요청부터 재생성된다. 다른 출처의 과거 검증행도 예약 작업이 분당 20건씩 같은 보수적 규칙으로 계속 재검사한다.

## 2026-08-17 v6-cloud 공지사항 조회 데이터 접근 계층 전환

- 작업 시작 시 `CODEX_HANDOFF.md`를 UTF-8로 확인하고 원격 브랜치를 fast-forward로 갱신했다. `e593242` 이후 최신 3개 커밋을 받아 작업 시작 기준 HEAD는 `58391a1`이었다.
- HANDOFF의 다음 미완료 작업이었던 `js/announcement-v1.js`의 활성 공지 조회를 `JSDataAccessV6.read("announcement")` 공통 경계에 연결했다. Cloudflare D1 응답의 `announcement` 중첩 객체와 기존 직접 API의 평면 응답을 모두 같은 공지 형식으로 정규화한다.
- D1의 `body`, `updated_at` 필드를 기존 화면의 `content`, `updatedAt` 계약으로 변환한다. 공지 닫기, 같은 공지 재표시 방지용 `localStorage` 키, 단계별 본문 서식과 모달 동작은 변경하지 않았다.
- 공통 모듈이 없거나 읽기 메서드를 제공하지 않는 구형·비정상 로딩 환경에서는 기존 `saveApiURL?action=announcement` 직접 조회를 그대로 사용한다. 직접 조회에도 동일 출처 세션 자격 증명을 명시했으며, 공지가 없거나 조회가 실패하면 기존처럼 조용히 무시한다.
- 공지 스크립트 자산 버전은 `announcement-v1.js?v=1.0.2-data-access`이다. `docs/CLOUDFLARE_MIGRATION.md`에도 공지 조회 적용 범위와 호환 fallback을 반영했다.

### 테스트와 빌드

- 공통 경계 D1 응답, 기존 직접 API fallback, 공지 없음·조회 실패를 실제 스크립트 실행 환경으로 검증하는 동적 테스트 3개를 추가했다.
- 공지·데이터 접근 집중 테스트 `15/15`, 전체 테스트 `270/270`, Cloudflare 테스트 `140/140` 통과.
- Cloudflare 자산 126개 빌드 및 Wrangler 4.118.0 dry-run, `js/announcement-v1.js` 문법 검사와 `git diff --check` 통과.

### 남은 작업과 주의사항

- 다음 데이터 접근 후보는 `listing-duplicate-cleanup-v1.js`이며, 그다음 빠른등록·지오코딩 저장 경로를 기능별 실행 테스트와 함께 검토한다. 기존 직접 `fetch`는 공통 모듈 미로딩 시 호환 fallback이므로 실제 브라우저 회귀 확인 전까지 제거하지 않는다.
- 이번 작업은 공지 조회 경로만 변경했다. 지도·클러스터·매물 정렬·대표매물·고객매칭 로직, D1 스키마·운영 데이터·R2 객체는 변경하지 않았고 운영 Worker 배포도 수행하지 않았다.

## 2026-08-15 건축물대장 0건 지번의 안전한 동일매물 대체 조회

- `중구 선화동 12-9` 매물의 대장 버튼과 인증·라우팅은 정상이며, 국토교통부 건축HUB가 해당 지번의 표제부·총괄표제부를 정상 응답하면서도 모두 0건으로 반환하는 것이 직접 원인이었다. 같은 통합매물의 활성 네이버 원본에는 좌표가 거의 같은 `중구 선화동 12-3`이 함께 기록되어 있고, 이 지번의 공식 캐시에는 `재경빌딩` 표제부 1건이 존재한다.
- 대장 0건인 활성 매물을 전수 확인한 결과 167건이었으며, 동일매물 원본에 현재 지번과 다른 정확한 지번이 있고 그 지번의 공식 대장이 존재해 안전하게 복구할 수 있는 경우는 4건이었다. 확인된 복구 후보는 `선화동 12-9→12-3`, `중리동→104-5`, `대흥동→249-1`, `목동→8-15`이다. 나머지는 정확한 대체 지번이 없으므로 임의의 인접 번지를 추측하지 않는다.
- Worker의 건축물대장 조회는 기본 지번의 공식 결과가 0건일 때만 같은 통합매물의 활성 원본 주소를 검사한다. 표시 주소가 정확한 지번이면 같은 구·법정동이면서 좌표 120m 이내인 원본 지번만 허용하고, 표시 주소가 동까지만 있는 매물은 같은 구·법정동의 활성 원본에 기록된 정확한 지번만 허용한다. 후보는 최대 4개만 조회하며 대장이 실제 존재하는 첫 후보만 사용한다. 잘못 묶인 원본이 멀리 떨어져 있으면 절대 대체 조회하지 않는 음성 회귀 테스트도 추가했다.
- 대장 0건 응답을 `building_info_status='확인완료'`로 저장하던 오류를 막았다. 공식 표제부·총괄표제부·호실·레코드 수가 모두 0이면 캐시만 남기고 매물의 건물 확인 상태는 갱신하지 않는다.
- 브라우저의 과거 0건 로컬 캐시를 즉시 무효화하도록 건축물대장 로컬 캐시를 v12, 배지 캐시를 v4, 클라이언트를 `8.2.8`로 올렸다. 대체 지번을 사용한 경우 상세 대장 상단에 원래 지번과 실제 조회 지번을 함께 적은 파란 안내문을 표시하고, 이어지는 전체 대장 조회도 대체 지번으로 수행한다. 매물의 저장 주소 `12-9` 자체는 덮어쓰지 않았다.

### 테스트와 운영 배포

- 대체 지번 성공, 동만 있는 주소의 정확한 원본 지번 복구, 120m 밖 잘못 묶인 원본 거부를 포함한 신규 동적 테스트 4개 통과.
- 전체 테스트 `266/266`, Cloudflare 자산 126개 빌드 및 Wrangler dry-run, JavaScript 문법 검사, `git diff --check` 통과.
- 운영 D1에서 `12-9` 캐시는 건물 0건, `12-3` 캐시는 `재경빌딩` 1건임을 재확인했다. 운영 HTML·JS·CSS에서 `8.2.8-alternate-parcel-fallback`과 대체 지번 안내 코드가 제공되는 것도 HTTP로 확인했다.
- 최종 운영 Worker 버전은 `3f37e7a2-a090-4241-9b63-1d5c3a23f9be`이다. D1 스키마와 기존 매물 주소·원본 연결 데이터는 변경하지 않았다.

## 2026-08-15 초기 로딩·상세 사진 전환 과부하 수정

- 운영 탭의 `This page crashed`와 느린 최초 화면을 추적한 결과, 초기 진입 250ms 뒤 상단 8개 매물의 상세를 자동 조회하고 현재 데이터 기준 사진 약 45장을 한꺼번에 선로드하던 흐름이 있었습니다. 선로드한 `Image` 객체와 디코딩 비트맵을 전역에 최대 80개까지 보관해 브라우저 메모리가 급증할 수 있었습니다.
- 최초 목록 진입의 상세 8건 자동 워밍업을 제거했습니다. 상세는 카드 hover/focus/pointerdown 또는 실제 열기 시에만 조회하고, 사진은 현재 장 다음 1장만 준비합니다. 선로드 `Image` 객체는 전역에 보관하거나 강제 `decode()`하지 않아 로드 종료 후 회수될 수 있게 했습니다.
- 상세자료가 도착하기 전에도 사진 개수가 2장 이상이면 이전·다음 버튼을 즉시 사용할 수 있습니다. 사용자가 먼저 다음 사진을 누르면 이동 요청을 보존하고 상세 응답 직후 요청한 장으로 이동하므로, 버튼이 비활성화되거나 첫 장에 고정되던 현상을 막았습니다.
- D1 CSV의 모든 행에 좌표가 있는 현재 운영 데이터에서는 0.7MB 공용 지오코드 캐시를 요청하지 않습니다. CSV 좌표를 읽는 즉시 카카오 `LatLng`으로 만들고 목록·지도·클러스터를 한 번만 렌더해, 좌표 준비 전 임시 렌더와 완료 후 재렌더가 연속 실행되던 초기 과부하를 제거했습니다. 좌표가 빠진 과거·예외 데이터와 수정 후 화면 복원 경로는 기존 지오코딩 및 선택 복원 절차를 유지합니다.
- 자산 버전은 `unified-listings-v8.js?v=8.1.30-fast-photo-carousel`, `map.js?v=8.2.12-single-pass-initial-load`입니다. 대표매물 우선순위, 같은 주소·층 카드의 독립 체크박스, 임장 상태, 클러스터 집계, 원본 분리·합치기 로직은 변경하지 않았습니다.

### 테스트와 배포

- `js/unified-listings-v8.js`, `js/map.js` 문법 검사와 초기 사진·단일 렌더 집중 테스트: 통과
- 전체 테스트 `262/262`, Cloudflare 테스트 `132/132`, Cloudflare 자산 `126개` 빌드 및 Wrangler dry-run, `git diff --check`: 통과
- 운영 HTML에서 새 자산 버전 연결과 배포된 스크립트의 사진 이동 대기·좌표 단일 렌더 코드를 HTTP `200`으로 확인했습니다. 자동 브라우저는 승인 계정 선택 화면까지 확인했지만 보호된 매물 화면 세션은 이어받지 못해 로그인 후 실데이터 사진 이동은 사용자 세션에서 최종 체감 확인이 필요합니다.
- 운영 Worker 버전은 `0a92d666-678f-43f0-9fb0-ab98396c521e`입니다. D1 스키마와 운영 매물 데이터는 변경하지 않았습니다.

## 2026-08-14 v6-cloud 입점진단 저장 데이터 접근 계층 전환

- 작업 시작 시 `CODEX_HANDOFF.md`를 UTF-8로 확인하고 `git fetch --prune origin` 및 `git pull --ff-only`을 실행했다. 원격의 최신 8개 커밋을 fast-forward로 반영했으며 작업 시작 기준 HEAD는 `e593242`였다.
- HANDOFF의 다음 작업이었던 `js/diagnosis-storage.js`의 `loadCloudState` 조회와 `saveCloudState` 저장을 `JSDataAccessV6.read/mutate` 공통 경계에 연결했다. 진단 레코드 키·업종별 분리·변경 이력·로컬 저장 형식과 STEP 6 화면 동작은 바꾸지 않았다.
- 기존 5초 요청 제한을 공통 경계에서도 유지하기 위해 `JSDataAccessV6.read/mutate`가 호출자의 `AbortSignal`을 실제 요청에 전달하도록 확장했다. 클라우드 조회 실패·미발견 시 로컬 진단을 반환하고, 브라우저 저장공간 초과 시에도 클라우드 저장을 계속하며, 클라우드 저장 실패 시 로컬 기록을 보존한 채 기존 경고를 반환한다.
- 공통 모듈이 없거나 필요한 메서드를 제공하지 않는 구형·비정상 로딩 환경에서는 기존 `saveApiURL` 또는 `/api/data` 직접 GET·POST 요청을 그대로 사용한다. 공통 경계와 직접 fallback 모두 동일한 `permitDiagnosis` 범위와 요청 본문을 사용한다.
- 데이터 접근 자산은 `data-access-v6.js?v=6.0.1-request-signals`, 진단 저장 자산은 `diagnosis-storage.js?v=1.2.1-data-access`로 갱신했다. `docs/CLOUDFLARE_MIGRATION.md`에도 입점진단 적용 범위와 보존 계약을 반영했다.

### 테스트와 빌드

- 진단 저장·공통 경계 집중 실행 테스트와 기존 입점진단 STEP 6 테스트: 통과
- `pnpm test`: 261/261 통과
- `pnpm run test:cloudflare`: 132/132 통과
- `pnpm run cf:check`: Cloudflare 자산 126개 빌드 및 Wrangler 4.118.0 dry-run 통과
- `js/diagnosis-storage.js`, `js/data-access-v6.js` 문법 검사와 `git diff --check`: 통과

### 남은 작업과 주의사항

- 공통 경계로 전환된 모듈의 직접 `fetch`는 모듈 미로딩 시 호환 fallback이므로 실제 브라우저 회귀 확인 전까지 제거하지 않는다.
- 다음 데이터 접근 정리는 아직 공통 경계가 없는 `announcement-v1.js`, `listing-duplicate-cleanup-v1.js`, 빠른등록·지오코딩 저장 경로를 기능별 실행 테스트와 함께 우선순위화한 뒤 진행한다. `mode: no-cors`와 비동기 작업 큐 계약이 섞인 경로는 일괄 치환하지 않는다.
- D1 스키마·운영 데이터·R2 객체는 변경하지 않았고 이번 작업에서 운영 Worker 배포도 수행하지 않았다.

## 2026-08-14 초기 1km 동 단위 클러스터 고정

- 운영 브라우저에서 카카오 축척을 직접 측정해 `level 8=2km`, `level 7=1km`임을 확인했습니다. 기본 지도 확대 단계를 `level 8`에서 `level 7`로 변경해 첫 화면과 초기화 버튼 사용 시 실제 1km 동 단위로 열리도록 고정했습니다.
- 행정 전환 기준은 `level 8 이상=구 단위`, `level 7=동 단위`, `level 6 이하=공간 클러스터`입니다. 따라서 한 단계 축소한 2km부터는 구 단위로 전환됩니다. 지도 데이터·필터·목록·클러스터 수량 계산은 변경하지 않았습니다.
- 지도 자산은 `map.js?v=8.2.11-initial-1km-neighborhood`입니다. `?clusterEngine=legacy` 복구 경로는 그대로 유지됩니다.
- 전체 테스트 `253/253`, Cloudflare `124/124`, `cf:check`, `git diff --check`를 통과했습니다. 운영 새 접속과 확대 후 초기화 버튼을 각각 검증해 두 경우 모두 하단 축척 `1km`, 동 클러스터 `63개`, 구·공간 클러스터 `0개`로 복귀하는 것을 확인했습니다.
- 운영 Worker 버전은 `0fca522d-03dc-4831-9c42-a28851626b5f`입니다.

## 2026-08-14 공간 클러스터 흰색 테두리 제거

- `world-grid` 공간 클러스터 원형의 2px 흰색 테두리를 제거했습니다. 클러스터 크기·색상·그림자·선택 표시와 구·동 행정 클러스터 카드 디자인은 변경하지 않았습니다.
- 스타일 자산은 `style.css?v=6.5.41-borderless-world-grid`입니다.
- 전체 테스트 `253/253`, Cloudflare `124/124`, `cf:check`, `git diff --check`를 통과했습니다. 운영 HTML의 새 CSS 연결, 공간 클러스터 `border: 0`, 기존 흰색 테두리 규칙 제거를 HTTP로 확인했습니다.
- 운영 Worker 버전은 `7b7bbb2b-b69e-47c4-8680-afb9d6361628`입니다.

## 2026-08-14 공간 클러스터 화면 매물 수 보정

- 동일한 현재 데이터·지도 중심·확대 단계에서 수정 전 `legacy`와 새 `world-grid` 엔진을 직접 비교했습니다. 수정 전은 화면 목록 `2,777개`와 클러스터 합 `2,777개`가 일치했지만, 새 엔진은 화면 목록 `2,775개`에 클러스터 합 `2,696개`로 화면 안 매물 `79개`가 빠지는 것을 재현했습니다.
- 원인은 공간 클러스터를 전역 데이터로 먼저 만든 뒤 큰 격자의 평균 중심점만 화면 경계로 검사한 것이었습니다. 격자 일부가 화면 안에 있어도 평균 중심이 화면 밖이면 해당 격자 전체가 숨겨졌습니다.
- `level 8 구`, `level 7 동` 행정 클러스터는 전체 지역 합계를 사용합니다. `level 6 이하` 공간 클러스터만 현재 지도 목록과 정확히 같은 매물 집합을 세계 고정 격자에 배치하도록 분리했습니다. 세계 격자 키와 확대 단계별 크기는 유지되어 드래그 시 재버킷 현상은 다시 생기지 않습니다.
- 현재 지도 자산은 `map.js?v=8.2.11-initial-1km-neighborhood`입니다. `?clusterEngine=legacy` 안전 복구 경로는 그대로 유지됩니다.
- 수정 후 운영에서 같은 중심·`100m` 축척을 다시 측정해 화면 목록 `2,775개`와 34개 공간 클러스터의 합 `2,775개`가 정확히 일치하고 누락이 `0개`인 것을 확인했습니다. 전체 테스트 `253/253`, Cloudflare `124/124`, `cf:check`, `git diff --check`를 통과했습니다.
- 운영 Worker 버전은 `3f5b797a-2986-4a82-8d9d-079c813e86bb`입니다. D1 스키마와 운영 데이터는 변경하지 않았습니다.

## 2026-08-14 당근형 고정 공간 클러스터·안전 복귀 장치

- 메인 지도와 찜목록 지도에 화면 픽셀 기준으로 매번 다시 묶던 방식 대신, 위·경도를 Web Mercator 세계 좌표의 고정 셀에 넣는 `world-grid` 엔진을 기본 적용했습니다. 같은 확대 단계에서는 지도를 드래그해도 셀 키와 셀 구성원이 바뀌지 않고, 화면 안으로 들어온 셀만 다시 표시됩니다.
- 공간 클러스터 셀은 카카오 지도 단계별로 `level 6 이상 1,280m`, `5=640m`, `4=320m`, `3=160m`, `2=80m`이며 최대 확대 `level 1`은 같은 정확한 주소만 묶습니다. 최초 640m 설정은 동 다음 확대 화면에 작은 원이 너무 많이 보여 1,280m로 보정했습니다. 확대할수록 한 셀이 절반 크기의 셀로 분리되므로 큰 클러스터가 자연스럽게 여러 개로 풀립니다.
- 현재 행정구역 단계는 기본 대전 전체 화면 `level 8 이상=구 단위`, `level 7=동 단위`, `level 6 이하=고정 공간 클러스터`입니다. 행정 카드의 수량은 현재 검색·출처·구분·층·찜목록 필터 전체 결과를 기준으로 계산하고, 화면 밖 카드나 충돌하는 낮은 우선순위 카드는 다른 지역으로 멀리 밀지 않고 숨깁니다.
- 지도/사이드바 폭이 인증·CSS 로딩 뒤 바뀔 때 카카오 투영 폭이 예전 값으로 남아 구 카드들이 오른쪽에 몰리던 문제를 `ResizeObserver`와 중심 보존 `relayout()`으로 보정했습니다. 운영 화면에서 서구·유성구·중구·동구·대덕구가 모두 실제 위치에 표시되는 것을 확인했습니다.
- 안전 복귀는 URL `?clusterEngine=legacy`로 즉시 가능합니다. 새 방식으로 돌아올 때는 `?clusterEngine=world-grid`를 사용합니다. 콘솔 API `JSMapClusterEngineV690.useLegacy()` / `useStable()`도 제공하며, 바꾸는 대상은 클러스터 표시 계층뿐이고 DB·원본·대표매물·고객매칭 데이터는 변경하지 않습니다.
- 회귀검사에서 상세와 함께 열린 AI 브리핑 패널이 운영현황·고객매칭 등 다른 전역 메뉴 위에 남는 기존 결함을 발견해, 전역 메뉴 전환 시 상세 드로어와 AI 패널만 닫고 매물·클러스터 선택은 유지하도록 보완했습니다. 통합매물 자산은 `8.1.29-overlay-ai-exclusivity`입니다.
- 자동 테스트는 전체 `253/253`, Cloudflare `124/124`, `cf:check`, `git diff --check`를 통과했습니다. 로그인된 운영 화면에서 새 엔진 클래스, 대전 5개 구 배치, 괴정동 검색, 당근 출처 필터, 찜폴더 5개 지도 반영, 상세→운영현황 전환, 기존 엔진 복귀를 직접 확인했습니다. 밀도 보정 뒤 운영 `500m` 축척의 첫 공간 단계는 화면 안 `25개` 클러스터, 최소 3건·최대 419건·평균 108건으로 확인했습니다. 운영 자산 `map 8.2.8-world-grid-density`, `style 6.5.40-world-grid-clusters` 연결도 HTTP로 확인했습니다.
- 최종 운영 Worker 버전은 `72c9a4db-f8c5-4e13-ae3f-0b399f90da9b`입니다. D1 스키마와 운영 데이터 변경은 없습니다.

## 2026-08-14 당근 대표매물 최우선 규칙
- 통합매물의 대표 우선순위를 `당근 > 네이버 > 공실박스 > 직접등록`으로 고정했습니다. 기존 `preserveRepresentative` 표시는 일반적인 대표정보 보존에는 계속 사용하지만, 더 높은 우선순위인 당근 원본이 새로 연결될 때는 당근 대표 승격을 막지 않습니다.
- 앞으로 자동수집·자동통합에서 당근 원본이 기존 네이버·공실박스 대표에 연결되면 대표 출처와 제목·건물명·호실·금액·면적·메모·원본 링크가 당근 기준으로 갱신됩니다. 당근 원본에 좌표가 없으면 기존 대표 좌표는 보존합니다.
- 기존 운영 DB도 보정했습니다. 활성 당근 원본이 있는데 다른 출처가 대표였던 969건을 당근 대표로 승격했고, 보정 후 잘못된 우선순위 잔여 건수는 `0건`입니다. 여러 당근 원본이 있으면 실제 사진이 있는 원본을 먼저 선택하고, 그다음 먼저 연결된 원본을 사용합니다.
- 확인 사례 `M-b98b960d-5e33-4826-9679-bae91ab08f76`(서구 갈마동 333-6)은 대표가 당근으로 변경됐으며 `보 5,000 / 월 240 / 면적 35.9㎡`, 당근 사진 3장으로 확인했습니다. 연결된 네이버 원본 `2643642194`는 하위 원본으로 그대로 보존됩니다.
- 상세창의 상세 API 응답과 이미 받아 둔 상세 캐시 모두 당근 우선 정렬을 적용했습니다. 따라서 목록이 당근인데 상세창만 네이버로 열리던 캐시 순서 문제도 함께 수정했습니다. 통합매물 자산 버전은 `8.1.28-cached-source-priority`입니다.
- 전체 테스트 `252/252`, Cloudflare 테스트 `124/124`, `cf:check`, `git diff --check`를 통과했습니다. 운영 Worker 버전은 `e74bdcb3-a77a-4c6a-828d-c7da6b0df72f`이며 운영 HTML·통합매물 자산 HTTP 200과 새 버전 연결을 확인했습니다. 로그인된 운영 화면을 새로고침해 갈마동 333-6 메인 카드가 당근 금액·면적·사진으로 표시되고 상세창도 당근 원본과 사진 3장으로 먼저 열리는 것을 직접 확인했습니다.

## 2026-08-14 당근 상세수집 PersistedQuery 변경 대응

- 유성구 당근 자동수집이 목록 `1,939건`과 기존 동일 `1,885건`까지 확인한 뒤 상세대상 `54건`에서 10분 이상 `0%`로 보이던 현상을 운영 D1 작업에서 추적했습니다. 작업 자체는 갱신되고 있었지만 모든 상세호출이 `PersistedQueryNotFound`로 실패해 재시도만 누적되는 상태였습니다.
- 당근 공식 운영 자산 `ArticleDetailQuery-DXI2ALAA.js`의 현재 `ArticleDetailQuery` 식별값을 확인하고, 실제 오류 대상 원본번호 `3059947`로 상세 응답이 정상 반환되는 것을 검증한 뒤 Worker의 `DAANGN_DETAIL_HASH`를 갱신했습니다.
- 배포 후 진행 중이던 같은 D1 작업을 새로 시작하지 않고 그대로 복구했습니다. 화면이 `0% → 33%`로 움직였고 최종적으로 `54/54`, 남은 `0건`, 전체 목록 `1,939건` 완전수집으로 종료했습니다.
- 최종 결과는 신규 `1건`, 자동통합 `8건`, 조건변경 `15건`, 검증대기 `3건`, 상세중복 `5건`입니다. API 재시도로 최종 누락된 원본은 `0건`이며 실패 `22건`은 당근이 주소·층을 공개하지 않은 기존 정상 경고 대상입니다.
- 집중 테스트 `28/28`, Cloudflare 테스트 `123/123`, 전체 테스트 `251/251`, `cf:check`, `git diff --check`를 통과했습니다. 운영 Worker 버전은 `e7a81c2b-0da7-43b0-8575-99080c65f1d9`입니다.

## 2026-08-14 비동기 저장 큐 공통 데이터 접근 계층 전환

- 작업 시작 시 원격 브랜치를 `git pull --ff-only`로 최신화해 `98ed607 Space detail source link`까지 6개 커밋을 fast-forward로 반영한 뒤, 이전 HANDOFF의 다음 작업이었던 `js/async-mutation-queue-v1.js` 전환을 진행했습니다.
- 비동기 저장 큐의 `enqueueMutation` 쓰기와 `workQueueStatus` 조회를 `JSDataAccessV6.mutate/read` 공통 경계로 연결했습니다. 공통 모듈을 불러오지 못한 구형·비정상 로딩 상황에서는 기존 `/api/data` 직접 요청을 그대로 사용합니다.
- 저장 큐의 기존 계약인 동일 `requestId` 중복 방지, 실패 작업 로컬 보존, 3초 후 재시도, 서버 완료 감지, 작업 상태 이벤트와 페이지 종료 시 `keepalive` 요청을 유지했습니다. 이를 위해 `JSDataAccessV6.mutate`가 선택적으로 `keepalive`를 전달하도록 확장했습니다.
- 실제 큐를 실행하는 테스트를 추가해 중복 요청이 한 번만 전송되는지, 실패 후 재시도되는지, 성공 시 outbox·활성 작업이 정리되는지, 상태 조회와 완료 이벤트가 유지되는지 검증했습니다. 자산 버전은 `async-mutation-queue-v1.js?v=1.0.9-data-access`입니다.
- 전체 테스트 `251/251`, Cloudflare 테스트 `123/123`, `cf:check`, `git diff --check`를 통과했습니다. 운영 정적 자산은 HTTP `200`이고 배포본에서 공통 `mutate/read`, `keepalive` 전달 코드를 확인했습니다.
- 운영 Worker 버전 `50e43fa3-4889-4dcb-969c-d6c986584f76`로 배포했습니다. D1 스키마·운영 데이터는 변경하지 않았습니다.
- 다음 마이그레이션 대상은 `js/diagnosis-storage.js`입니다. 저장·조회 의미와 기존 직접 요청 fallback을 유지하면서 `JSDataAccessV6` 경계로 옮기고 같은 방식의 실행 테스트를 추가합니다.

## 2026-08-13 기존매물 1건 검증원본 자동 합치기

- 매물검증 원본 중 저장된 후보가 1개이고, 실행 시점에도 같은 정확한 주소의 활성 대표매물이 실제로 1개뿐인 경우만 그 대표매물의 동일매물 하위 원본으로 자동 연결합니다. 같은 주소에 활성 대표매물이 여러 개인 건은 자동 처리하지 않고 검증대기에 유지합니다.
- 자동 연결 시 대표매물의 제목·주소·호실·가격·면적·메모·좌표·대표 출처·사진 선택을 갱신하지 않습니다. 연결된 원본에는 `preserveRepresentative` 표시를 저장하고 화면 대표 원본 우선순위에서도 뒤로 보내 기존 대표정보가 유지됩니다.
- 향후 자동수집에서도 같은 조건의 검증 원본은 검증대기에 쌓지 않고 즉시 하위 원본으로 연결합니다. 기존 검증대기는 매분 20건씩 백그라운드 처리하며, 매물검증 화면의 `기존매물 1건 자동합치기` 버튼으로 연속 처리가 가능합니다.
- 운영 적용 전 안전 조건 대상은 3,305건이었고, 저장 후보는 1개지만 같은 주소의 활성 대표매물이 여러 개인 약 680건은 제외했습니다. 운영 데이터 처리를 완료해 안전 조건 잔여는 `0건`, 전체 검증대기는 `6,557 → 3,247건`이 되었습니다. 다중 대표 후보 및 별도 판단 대상은 그대로 남아 있습니다.
- 화면 연속 처리는 검증원본 ID를 8개 독립 구간으로 나눠 20건씩 병렬 처리합니다. 도안동 1131 사례의 원본번호 `2641855977`, `2641946297`, `2642778809`가 모두 대표매물 아래 보호 원본으로 연결됐고, 대표정보 `그린호수 / 102호 / 보 2,000 / 월 100 / 면적 15㎡`는 처리 전과 동일함을 D1에서 확인했습니다. 자동합치기 오류는 `0건`입니다.
- 전체 테스트 `246/246`, Cloudflare 테스트 `118/118`, 집중 테스트 `8/8`, `cf:check`, `git diff --check`를 통과했습니다. 운영 Worker 최종 버전은 `390232eb-6512-4796-9e3d-93db0acb001a`입니다.

## 2026-08-13 매물 상세창과 다른 메뉴 중첩 차단

- 매물 상세 드로어가 열린 상태에서 운영현황·고객매칭 등 다른 메뉴를 열면 새 창 아래나 위에 상세창이 남던 문제를 수정했습니다. 메뉴 클릭의 캡처 단계에서 상세 드로어의 열림·전환 클래스를 즉시 제거하고 진행 중인 상세 요청도 무효화한 뒤 새 메뉴를 엽니다.
- 운영현황·고객매칭은 `openOperationsCenter` 진입점에서도 상세창을 닫아 화면 버튼 외의 프로그램 호출까지 보호합니다. Tell, AI임장, 빠른등록, 찜목록, 필터, 보기·작업·정렬 메뉴에도 동일한 화면 전환 규칙을 적용했습니다.
- 통합 매물 스크립트 캐시는 `8.1.23-overlay-exclusivity`, 운영센터 스크립트 캐시는 `7.22.4-overlay-exclusivity`로 갱신했습니다.
- 실제 클릭 순서 모의 테스트를 포함한 전체 테스트 `241/241`, Cloudflare 테스트 `114/114`, `cf:check`를 통과했습니다. 운영 HTML과 두 스크립트에서 새 버전 연결·즉시 닫기·메뉴 감지·운영센터 직접 닫기 로직을 확인했습니다. 검증 브라우저에는 승인된 Google 로그인 세션이 없어 보호된 실제 매물 화면은 자동화 테스트와 운영 자산 확인으로 검증했습니다.
- 운영 Worker 버전 `167ac3c0-0287-456d-817a-ce5d60e61b43`로 배포했습니다.

## 2026-08-13 닫기 버튼 미니멀 원형 디자인·로드뷰 이중 아이콘 수정

- 기존 44px 테두리 사각형 닫기 버튼을 40px 무테 연회색 원형 디자인으로 변경했습니다. 상세창, 고객관리창, 매물 수정창 등 기존 공통 대상에 동일하게 적용되며 호버·눌림·키보드 포커스 상태는 유지합니다.
- 로드뷰 버튼의 기존 `×` 문자 위에 CSS 가상 아이콘이 추가되고 로드뷰 전용 고우선순위 글자 스타일이 살아 있어 `×`가 두 개 보이던 원인을 수정했습니다. 가상 아이콘을 완전히 제거하고 원래 `×` 한 개만 표시하며 로드뷰 전용 크기·색상도 마지막 공통 스타일에서 고정합니다.
- 전체 테스트 `240/240`, Cloudflare 테스트 `114/114`, 로드뷰·닫기 버튼 집중 테스트 `5/5`, `cf:check`를 통과했습니다. 운영 HTML에서 `1.1.0-minimal-circle` CSS 연결과 단일 아이콘·원형·로드뷰 전용 재정의를 확인했습니다.
- 운영 Worker 버전 `c6c0ab4d-07e2-4402-b052-1fc7f9756e8e`로 배포했습니다.

## 2026-08-13 공실박스 무사진 매물 오사진 차단·닫기 버튼 통일

- 공실박스에서 실제 매물사진은 원본의 `Photos/photos` 배열만 인정하도록 수집기와 통합 조회 API를 수정했습니다. 중개사 프로필 `avatars/1.png`, 목록 대표값 `Xbfimg`, 건물 대표값 `bfxphoto`는 사진이 없는 매물을 채우는 용도로 더 이상 사용하지 않습니다.
- 실제 `Photos` 배열이 있는 공실박스 매물은 사진을 그대로 유지합니다. 과거에 잘못 저장된 이미지가 남아 있어도 목록·상세 응답에서 즉시 제외하며, 데이터베이스를 일괄 삭제하지 않고 이후 수집 때 정상 사진 상태로 정리됩니다.
- 목록 API는 전체 원본 JSON을 내려받지 않고 D1의 `$.list.Photos`만 추출해 처리합니다. R2 캐시는 `unified-listings-v4-actual-gongsil-photos.json`, 상세 캐시는 `unified-detail-v2-actual-photos/`로 갱신해 기존 오사진 캐시를 즉시 우회했습니다.
- 매물 상세창, 매물관리·고객매칭창, 매물 임대조건 수정창을 포함한 주요 모달·드로어의 닫기 버튼을 44px 정사각형, 명확한 테두리·그림자·호버·키보드 포커스가 있는 공통 디자인으로 통일했습니다. 공통 스타일은 `css/modal-close-v1.css`입니다.
- 전체 테스트 `240/240`, Cloudflare 테스트 `114/114`, `cf:check`를 통과했습니다. 운영 HTML과 새 CSS가 각각 200 응답이고 CSS 연결·44px 크기·포커스 스타일이 반영된 것을 확인했습니다. 별도 검증 브라우저에는 승인된 Google 로그인 세션이 없어 보호된 매물 화면의 실브라우저 점검은 자동화 테스트와 운영 자산 확인으로 대체했습니다.
- 운영 Worker 버전 `3bef7eaa-0a57-440e-a2b3-8579af18dc4e`로 배포했습니다.

## 2026-08-13 네이버·당근 원본 매물번호 검색

- 메인 검색창에서 네이버·당근 원본 매물번호를 정확히 일치 검색할 수 있게 했습니다. `2643232701`, `네이버 2643232701`, `3768587`, `당근3768587` 형식을 지원합니다.
- 검색 결과는 해당 원본이 연결된 JS 대표 매물 한 건으로 표시하며, 카드와 상세창에는 원본 매물번호를 노출하지 않습니다. 일반 가격 검색과 충돌하지 않도록 6자리 이상의 숫자 전체가 원본번호와 일치할 때만 전용 검색으로 처리합니다.
- `unifiedListings` 응답에 네이버·당근 원본번호의 숨김 검색 인덱스를 추가했습니다. 현재 비활성화된 과거 원본번호도 삭제되지 않은 대표 매물에 연결돼 있으면 검색되며, 화면의 활성 동일매물 개수에는 영향을 주지 않습니다.
- 검색 스크립트 캐시는 `6.5.79-source-listing-search`, 통합 매물 스크립트 캐시는 `8.1.22-source-listing-search`, R2 API 캐시는 `unified-listings-v3-source-search.json`으로 갱신했습니다.
- 전체 테스트 `236/236`, Cloudflare 테스트 `110/110`, `cf:check`를 통과했습니다. `js-map.com`에서 새 HTML·검색 파서·정확 일치 함수·숨김 인덱스 스크립트를 확인했습니다.
- 운영 Worker 버전 `5b7c96de-b7bf-4824-a7db-0a1d724135cd`로 배포했습니다.

## 2026-08-10 자동수집 실측 검수와 당근 상세 보조값 수정

- 오전 실행은 네이버 중구만 완전수집으로 끝났습니다. 유성구·대덕구는 원본의 주소·층 누락으로 부분수집, 서구·동구는 실행 중 `js-map.com` 통신 오류로 부분수집됐습니다.
- 당근의 목록 비교는 정상 작동했습니다. 예를 들어 대덕구 578건 중 기존 동일 560건을 상세조회 없이 생략하고 신규·변경 후보 18건만 조회했습니다.
- 당근 상세 API에 주소·층·임대조건 일부가 빠지면 목록 단계에서 이미 확보한 값을 이어받지 않아 후보가 실패 처리되던 문제를 수정했습니다. `mergeDaangnDetailWithList`가 목록의 주소·층·보증금·월세·면적을 상세 응답의 안전한 보조값으로 사용합니다.
- 상세 저장 실패 원인은 D1 작업의 `detailErrors`에 출처 ID와 함께 남깁니다. 원본 자체의 `주소·층 오류`는 복구 화면을 무한 반복하지 않고 원인을 기록한 뒤 다음 대상으로 넘어갑니다.
- Cloudflare 테스트 54개와 자동수집 테스트를 통과했고 Worker 버전 `d9318517-7fb8-4c95-a466-a8ea1cc1d389`를 배포했습니다. Edge 확장 버전은 1.0.21, 당근 수집기는 1.4.1입니다.

## 2026-08-10 네이버·당근 기존매물 상세 재수집 차단

- 네이버 자동 구 단위 수집이 수동 수집과 달리 목록 페이지를 곧바로 상세저장하던 구형 경로를 사용하고 있었습니다. 네이버 v5.9.0부터 자동·수동 모두 `전체 목록 ID 수집 → D1 기존매물 비교 → 신규·실제 조건변경만 상세조회` 한 경로를 사용합니다.
- 당근과 네이버의 목록 지문이 달라져도 보증금·월세·면적(1평 미만 허용)·층·주소가 같으면 제목·설명·태그·대표사진 같은 표시 변화로 판단해 상세수집하지 않습니다. 실제 핵심 조건이 달라지거나 출처 ID가 처음 발견된 경우만 상세조회합니다.
- 잘못 생성된 유성구 v5.8.0 세션은 3,300건에서 `partial`로 종료했습니다. 수정 후 새 v5.9.0 실운영 검증에서 유성구 5,242건 중 기존 동일 5,022건을 상세조회 없이 생략하고, 실제 변경 3건과 과거 출처 ID가 없던 217건만 상세대상 220건으로 분류했습니다.
- 첫 수정 실행의 상세대상 220건 중 155건만 저장 접수됐고 결과는 신규 8건·조건변경 2건·검증대기 145건이었습니다. 주소·층 정보가 없는 65건 때문에 세션은 부분수집으로 보존됐으며 자동수집기는 다음 구로 계속 진행했습니다.
- Edge 자동수집 확장 프로그램은 v1.0.20, 네이버 수집기는 v5.9.0으로 전용 프로필에 설치했고 예약 시간은 매일 오전 11시를 유지했습니다.
- Cloudflare 전체 테스트 53개와 `cf:check`를 통과했고 운영 Worker 버전 `813aae3b-56d0-478a-af9c-0bf6e5844805`로 배포했습니다.

## 2026-08-10 자동수집 기존매물 재수집 방지

- 수집 전 변경분 판별이 `listing_sources`만 조회해 매물검증 대기 상태인 `collector_raw` 원본을 누락하던 문제를 수정했습니다. 이제 아직 통합되지 않은 검증 대기 원본도 출처 ID와 목록 스냅샷을 비교해 변경이 없으면 상세조회·재저장을 건너뜁니다.
- 과거 복구 데이터의 `snapshot_hash='legacy-recovery'`를 실제 FNV 해시로 잘못 해석하던 문제를 수정했습니다. 실제 `fnv1a-xxxxxxxx` 형식만 해시 비교하고, 복구 표식은 보증금·월세·면적·층·주소 등 핵심 조건으로 비교합니다.
- D1 `collector_raw(source, source_listing_id, created_at DESC)` 인덱스 마이그레이션 `0011_collector_raw_source_lookup.sql`을 운영 DB에 적용했습니다. 기존매물 판별 조회가 전체 원본을 반복 탐색하지 않습니다.
- 자동수집 전용 Edge 실행 스크립트에 `-Force` 재실행과 선택적 `-Debug` 진단 모드를 추가했습니다. 일반 예약 실행은 기존 동작과 동일합니다.
- Cloudflare 전체 테스트 51개와 `cf:check`를 통과했고 운영 Worker 버전 `8e540468-9e71-4402-987f-1c5e2e192965`로 배포했습니다.
- 2026-08-10 오전 실행 중 유성구가 1,620건에서 중단돼 해당 세션을 보존한 채 수정본으로 재개했습니다. 재개 후 0건부터 다시 시작하지 않고 같은 세션의 1,620건 다음 지점부터 진행하는 것을 D1에서 확인했습니다.

## 2026-08-09 자동수집 완주·재시도 안정화

- 네이버 자동수집 v5.8.0은 새 탭에서 화면 클러스터 사전분석이 먼저 시작돼도 자동수집 요청이 오면 해당 수동 준비 작업만 무효화합니다. 유성구·서구처럼 매물이 많은 지역이 `준비 90초`에서 멈추던 충돌을 제거했습니다.
- 당근 자동수집 v1.4.0은 상세조회 실패 ID를 버리지 않고 D1 작업의 `pendingDetailIds`에 보존합니다. 최초 조회는 동시 2개, 재시도는 동시 1개로 낮추며 ID별 최대 8회까지 이어서 조회하고 실제 API 오류를 `detailErrors`에 남깁니다.
- Edge 자동수집 확장 프로그램 v1.0.19는 대상별 즉시 재시도 4회 후에도 실패하면 다른 지역을 진행하고, 저장 지점부터 지연 재시도를 최대 8주기 수행합니다. 일시 오류는 최종 실패로 집계하지 않습니다.
- 설치 스크립트는 전용 Edge 프로필 프로세스만 종료한 뒤 확장 파일을 교체하므로 Manifest V3 서비스워커가 예전 `background.js`를 계속 사용하는 문제를 막습니다. 사용자 일반 Edge 창과 자동수집 설정·로그인 프로필은 삭제하지 않습니다.
- Cloudflare 전체 테스트 47개와 `cf:check`를 통과했고 운영 Worker 버전 `fa36a138-4919-49ad-8cd4-bfe8e5256f4e`로 배포했습니다.
- 현재 PC에는 확장 프로그램 v1.0.19와 네이버 v5.8.0·당근 v1.4.0이 설치됐으며 원본 파일과 설치 파일 해시가 일치합니다. 예약 시간은 기존대로 매일 오전 11시입니다.

## 2026-08-09 매물검증 자동판단 완료

- 자동판단 전 검증대기 `5,373`건을 Cloudflare 예약 작업으로 끝까지 처리했고 최종 `unscanned=0`을 확인했다.
- 마지막 6건은 복구된 과거 검증 데이터에 위도·경도가 없어 D1 바인딩에 `undefined`가 전달되며 반복 실패했다.
- 과거 검증 데이터를 처리하기 전에 주소·층·임대조건·사진·연락처·좌표를 현재 형식으로 정규화하고, 선택값이 없으면 `undefined`가 아닌 `null` 또는 빈 배열을 사용하도록 수정했다.
- 마지막 6건 중 2건은 자동 통합·등록되어 대기에서 제거됐고, 4건은 애매한 매물로 수동검증 대상에 안전하게 남았다.
- 최종 상태는 전체 수동검증 대상 `8,132`건, 자동판단 전 `0`건, 자동판단 완료 후 수동확인 `8,132`건이다.
- 관련 공간판단 테스트 7개와 Cloudflare 전체 테스트 46개, `cf:check`를 통과했다. 운영 Worker 버전은 `4841a3e7-8689-4746-bd8a-31096b1f53b3`이다.

## 노트북에서 작업 이어하기

- GitHub 저장소는 `https://github.com/cho711920-ops/my-map`이고 현재 작업 브랜치는 `codex/cloudflare-js-map`이다.
- 새 노트북에서는 저장소를 받은 뒤 `git switch codex/cloudflare-js-map`과 `git pull origin codex/cloudflare-js-map`을 실행하고, 이 `CODEX_HANDOFF.md`를 처음부터 끝까지 읽은 뒤 작업을 이어간다.
- 기존 저장소가 없다면 `git clone https://github.com/cho711920-ops/my-map.git` 후 저장소 폴더에서 위 브랜치로 전환한다.
- 배포 전 `pnpm run test:cloudflare`, `pnpm run cf:check`, `pnpm run build:cloudflare`를 실행하고, 배포는 `pnpm exec wrangler deploy`를 사용한다.
- Cloudflare D1·R2·Worker와 등록된 Secret은 원격 계정에 그대로 유지되므로 복사하지 않는다. 새 노트북에서 배포하거나 사용량을 조회할 때만 `pnpm exec wrangler login`으로 같은 Cloudflare 계정에 로그인한다.
- Edge 로그인 세션은 Windows 보안에 묶인 로컬 정보라 복사하지 않는다. 자동수집 대상·예약시간은 현재 PC의 자동수집 설정에서 `설정 내보내기`로 JSON 파일을 저장하고, 노트북 설치 후 `설정 가져오기`로 한 번에 복원한다. 노트북에서는 사이트별 로그인과 수집기 보안키 입력만 최초 1회 다시 한다.
- 운영 사이트는 `https://js-map.com`, Worker 이름은 `js-map`, D1은 `js-map-primary`, R2는 `js-map-media`다.
- 작업 시작 시 반드시 `git status`로 사용자 미완료 변경이 없는지 확인하고, 운영 D1 데이터를 삭제하거나 초기화하지 않는다.

## 2026-08-08 노트북 자동수집 설정 이전

- Edge 자동수집 설정 화면에 `설정 내보내기`와 `설정 가져오기`를 추가했다.
- 이동 파일에는 자동수집 사용 여부, 실행시간, 완료 탭 닫기, 네이버·당근·공실박스 대상 URL과 지역만 포함한다.
- 비밀번호, 로그인 쿠키, 수집기 보안키는 이동 파일에 포함하지 않는다.
- 노트북에서는 저장소 설치 스크립트를 실행하고 JSON을 가져온 다음, 각 사이트에 한 번 로그인하고 수집기 보안키를 입력하면 같은 자동수집 구성이 복원된다.
- ChatGPT 프로젝트로 채팅을 옮기는 것은 대화·파일 문맥 정리에 유용하지만 로컬 저장소나 Edge 프로필을 복사하지 않는다. 코드는 GitHub 브랜치, 운영 상태는 Cloudflare, 자동수집 대상은 위 JSON을 기준으로 이어간다.

## 2026-08-08 업무 중심 매물 변경이력

- 복구 버튼 문구를 의미가 명확한 `이전으로 복구`로 변경했다. 선택한 이력의 변경 직전 업무값으로 되돌리는 동작은 그대로다.
- 변경이력 비교 시 `id`, `property_id`, `status`, `main_source`, 주소와 시스템 필드를 제외하고 매물명·호실·보증금·월세·관리비·권리금·면적·임대인/임차인 연락처·메모·연락처 목록만 비교한다.
- 수정 화면의 `rent`, `memo`, `contacts` 같은 필드명을 D1의 `monthly_rent`, `operating_memo`, `contacts_json`으로 표준화해, 기존 값이 `없음`으로 바뀐 것처럼 보이던 잘못된 이력을 수정했다.
- 과거 이력도 API 응답 시 같은 규칙으로 다시 해석하므로 데이터 삭제나 재저장 없이 바로 정상 표시된다.
- 연락처는 JSON 대신 `역할 전화번호` 형식으로 표시하고 전화번호 하이픈·숫자 쉼표만 다른 경우는 변경으로 잡지 않는다.
- 관리비·권리금 같은 숫자값의 기존 빈칸과 저장 후 `0`은 모두 `없음`으로 취급해 가짜 변경이력으로 표시하지 않는다.
- 앞으로 수동 매물 수정 이력에는 업무 필드만 저장해 D1 저장공간 증가도 줄인다.
- 변경이력 목록은 수동 빠른등록·임대조건/메모 수정·복구 이력만 조회하며 수집기 내부 처리 기록은 표시하지 않는다.
- 빠른등록은 사람이 직접 수행한 업무이므로 신규 매물의 매물명·호실·임대조건·메모·연락처를 `없음 → 등록값`으로 표시한다.
- 관련 신규 테스트 3개와 Cloudflare 전체 테스트 46개, `cf:check`를 통과했다.

## 2026-08-08 운영센터 탭 겹침 수정

- `변경이력` 또는 `사용자 관리`를 연 뒤 기존 `운영현황·수집현황·매물검증·고객매칭` 탭으로 이동할 때 새 패널과 활성 버튼이 남아 화면 아래에 겹치던 문제를 수정했다.
- 기존 탭 전환 전에 변경이력·사용자 관리 패널을 항상 숨기고 두 버튼의 활성 상태도 해제한다.
- 브라우저 캐시 버전을 `operations-admin-v1.js?v=1.0.1-tab-isolation`으로 올렸다.
- 전문 운영 테스트 5개, 기존 Cloudflare 회귀 테스트 43개와 `cf:check`를 통과했다.
- 실서비스 HTML과 수정 자산의 200 응답 및 새 버전 연결을 확인했다.
- 운영 Worker 버전은 `84a50d06-f128-4cb9-bbe0-a1a703d9300a`다.

## 2026-08-08 전문 운영 최적화·권한·변경이력

- 운영현황의 대형 집계 결과를 D1 `operations_snapshots` 한 행에 저장하고, 일반 매물 수정은 숫자 증감만 반영하도록 바꿨다. 수집 완료처럼 전체 수치가 바뀌는 작업만 백그라운드에서 한 번 재계산한다.
- 5분 변경 확인 시 작은 매물 수정은 해당 매물 ID만 다시 받아 화면에 합치며, 수집 완료·통합처럼 범위가 큰 작업만 전체 목록을 갱신한다.
- 외부 매물 사진의 브라우저 캐시를 7일로 늘렸고 기존 상세창의 다음 사진 선로딩을 유지했다. 유료 이미지 변환 서비스는 추가하지 않았다.
- 매물 변경이력 탭에서 수정 전후 차이와 작업자·시간을 확인하고, owner/admin은 허용된 매물 필드의 이전 상태를 복원할 수 있다.
- 사용자 관리 탭에서 owner/admin이 Google 이메일의 member/viewer 권한과 사용 여부를 관리한다. owner와 환경변수 고정 계정은 화면에서 변경할 수 없다.
- viewer는 조회와 개인 찜 저장만 가능하며, 매물·수집·운영 데이터 쓰기는 서버에서 차단한다.
- 알림 기능과 고객매칭 로직은 이번 작업에서 변경하지 않았다.
- D1 마이그레이션 `0010_operations_snapshot_and_access.sql`을 운영 DB에 적용했다.
- 전문 운영 테스트 5개와 기존 Cloudflare 회귀 테스트 43개, `cf:check`를 모두 통과했다.
- 운영 Worker 버전은 `4fcb33f7-4a4b-4de7-855e-368003d3468f`다.

## 2026-08-07 PC·태블릿 브랜드 로고 확대

- PC·태블릿 상단의 파란 `JS` 마크를 38px에서 42px로, 내부 글자를 15px에서 17px로 확대했다.
- 검정 `JS부동산` 워드마크는 18px에서 22px로 확대했다.
- 상단 헤더 높이와 메뉴 시작 위치는 유지해 검색·관리 버튼 영역을 침범하지 않는다.
- 로고·모바일 테스트 4개, Cloudflare 테스트 36개 및 `cf:check`를 통과했다.
- 운영 Worker 버전은 `ee161bbe-c133-41b7-a209-50ebd9555c39`다.

## 2026-08-07 PC·태블릿 브랜드 로고 통일

- 모바일 헤더의 파란 그라데이션 `JS` 박스와 검정 `JS부동산` 워드마크를 PC·태블릿 왼쪽 상단에도 동일하게 적용했다.
- 기존 헤더 너비와 메뉴 배치는 유지했고 로고 새로고침 클릭/터치 동작도 보존했다.
- 운영 화면을 1024x768 태블릿과 1440x900 PC 크기로 확인했으며 헤더는 243x60, 로고 마크는 38x38로 정상 표시되고 가로 넘침이 없다.
- 로고·모바일 테스트 4개, Cloudflare 테스트 36개 및 `cf:check`를 통과했다.
- 운영 Worker 버전은 `c974132f-82f7-49a7-bdd1-88d7bcd99ed0`다.

## 2026-08-07 상단 JS부동산 로고 새로고침

- 모바일 앱 헤더의 `JS부동산` 로고와 PC 왼쪽 상단 `J S 부 동 산` 제목을 누르면 현재 사이트를 새로고침한다.
- PC 제목은 Enter/Space 키로도 실행할 수 있도록 버튼 역할과 접근성 레이블을 추가했다.
- 전용 로고 테스트 1개, 모바일 UI 테스트 3개, Cloudflare 테스트 36개 및 `cf:check`를 통과했다.
- 태블릿의 PC형 헤더에서도 외부 함수 로딩에 의존하지 않고 로고가 직접 `window.location.reload()`를 실행하도록 보강했으며 터치 조작을 명시했다.
- 운영 Worker 버전은 `0e75f1b8-8cfb-4ed8-bd25-7e7f8d8a6135`다.

## 2026-08-07 계정별·기기 간 찜 동기화

- 찜/방문 목록의 브라우저 캐시를 로그인 이메일별로 분리해, 동일 기기에서 두 허용 계정의 목록이 섞이거나 서로 덮어쓰지 않게 했다.
- 업데이트 전 PC에 저장된 기존 찜은 현재 로그인 계정의 전용 저장소로 한 번 이전하고, D1의 기존 찜과 병합한 뒤 서버에 저장한다.
- 계정을 바꾸면 화면의 이전 계정 찜 상태와 메모리 상태를 초기화하되, 각 계정의 전용 캐시는 그대로 보존한다.
- D1 클라우드 상태는 기존대로 정규화한 `owner_email`별로 저장되어 같은 Google 계정은 PC/태블릿에서 동기화되고 다른 계정은 분리된다.
- GG 로컬 기존 찜 + GG 서버 기존 찜 병합 및 같은 브라우저에서 CHO 계정 전환을 검증하는 `tests/account-favorite-sync.test.cjs`를 추가했다.
- 찜 회귀 테스트, Cloudflare 테스트 36/36, `cf:check`를 통과했다.
- 운영 Worker 버전은 `649307d1-2ff4-465d-9391-8963528ac56b`다.
- 태블릿/모바일에서 열어 둔 사이트로 다시 돌아올 때도 15초 제한으로 서버 목록을 재확인해, 새로고침 없이 다른 기기의 최신 찜을 받는다.

## 2026-08-06 네이버 거리뷰 연동 지도 인증 복구

- 네이버 Cloud Maps `JS-Realestate` 애플리케이션의 Web 서비스 URL에는 기존 `https://my-map-ten-roan.vercel.app`만 등록되어 있어 `js-map.com`에서 SDK 인증이 실패했다.
- Web 서비스 URL에 `https://js-map.com`을 추가하고 저장했다. Dynamic Map 이외의 API 선택은 변경하지 않았다.
- 새 페이지에서 `네이버 거리뷰 연결 완료`, 인증 실패 클래스 제거, 오른쪽 연동 지도 컨테이너 하위 지도 요소 3개 생성을 확인했다.

## 2026-08-06 검증매물 목록 복구

- 검증 화면 상태 문구를 `전체 검증대기 5,654건 중 현재 작업 600건을 248개 묶음으로 표시` 형식으로 변경해 전체 대기 수와 속도 보호용 작업 배치를 명확히 구분했다. 운영 Worker 버전은 `1d48a0e9-5553-4adb-988c-3027bca57e08`이다.
- D1 `collector_raw`에는 검증 대기 원본 5,654건이 정상적으로 복구되어 있었고 JSON, 주소, 층 데이터도 유효했다.
- 검증 작업공간 API가 최대 600개 주소마다 통합매물 후보를 개별 조회하여 한 화면 요청에 최대 약 601개의 D1 쿼리를 순차 실행하던 N+1 문제가 원인이었다.
- 통합매물 후보 조회를 주소 75개 단위의 `IN (...)` 배치 쿼리로 변경하여 최대 약 9개 쿼리로 줄였다.
- 새로고침 요청과 최초 요청이 겹치면 빈 상태를 먼저 그리던 프론트엔드 경쟁 조건도 하나의 진행 중 Promise를 공유하도록 수정했다.
- Cloudflare 자동 테스트 31개와 `cf:check`가 통과했다.
- 운영 Worker 버전 `8eec1b5d-87e6-4969-b826-bacc1e754f59`에 배포했다.
- 실제 `js-map.com`에서 검증 대기 5,654건, 검증대상 600건, 248개 주소 묶음 및 첫 항목 `동구 가양동 32-4`의 기존매물 1건/신규수집 8건 표시를 확인했다.

## 2026-08-06 전체 데이터 복구 및 D1 사용량 최적화

### 운영 상태

- 운영 주소: `https://js-map.com`
- Cloudflare Worker: `js-map`
- 배포 버전: `320ea09e-bb9b-4281-bad5-29e89909245d`
- 작업 브랜치: `codex/cloudflare-js-map`
- 테스트: Cloudflare 자동 테스트 30개 통과, `wrangler deploy --dry-run` 통과

### 기존 Google Sheet 전체 대조 및 복구 완료

- 기존 Sheet 20개 탭을 모두 CSV로 별도 보관하고 D1과 항목별로 대조했다.
- 통합매물은 `8,756`개다. 기존 D1의 `8,754`개에서 빠진 직접등록 2개를 복구했다.
- 연결 원본은 `23,077`개이며 연결된 원본 누락은 0개다.
- 검증 대기 원본 `5,654`개를 `collector_raw`에 복구했다.
- 기존 고객 1명과 신규 고객 1명을 모두 보존해 총 2명이다.
- 고객매칭은 기존 57개와 신규 14개를 모두 보존해 총 71개다.
- 상담이력 17개, 수집회차 24개, 공지 1개, 사용자별 cloud state 5개를 복구했다.
- 변경이력은 기존 D1 기록까지 포함해 `46,542`개다.
- 연결되지 않는 과거 이력/검증 스냅샷 `5,720`개는 `legacy_archive`에 보존했다.
- 사진 URL `112,456`개, 연락처 `3,151`개, 좌표 캐시 `7,202`개는 누락 없이 유지됐다.
- 현재 D1 실측 크기는 약 `322.6MB`이며 무료 저장 한도 5GB의 약 6.5%다.
- 운영화면에서 활성 통합매물 8,756, 검증 대기 5,654, 원본 23,077, 고객매칭 71,
  변경이력 46,542가 표시되는 것을 확인했다.
- 지도 표시 8,749개와 통합매물 8,756개의 차이 7개는 데이터 누락이 아니라 좌표 없는 주소 오류 7개다.

### 백업 및 복구 자료

- 전체 D1 복구 전 SQL 백업: `../work/d1-current-before-recovery-2026-08-06.sql`
- 기존 Sheet 20개 탭: `../work/sheet-tabs-full-2026-08-06/`
- 전체 대조 보고서: `../work/data-audit-2026-08-06.json`
- 복구 보고서와 재실행 가능한 SQL: `../work/recovery-2026-08-06/`
- Google Sheet와 기존 Vercel 프로젝트는 안전 확인 기간이 끝날 때까지 삭제하지 않는다.

### 사용량 및 속도 최적화

- 운영현황 전체집계를 R2에 1시간 캐시한다. 같은 화면을 반복해서 열어도 D1 전체집계를 다시 하지 않는다.
- Sheet CSV와 통합원본 목록 R2 캐시를 5분에서 1시간으로 늘렸다.
- 매물 수정/삭제/수집완료처럼 실제 관련 데이터가 바뀔 때만 해당 캐시를 즉시 지운다.
- 즐겨찾기 저장은 공용 매물 캐시를 지우지 않고, 좌표 저장은 좌표 캐시만 지운다.
- 통합매물/Sheet/좌표의 페이지 조회를 OFFSET에서 rowid keyset 방식으로 변경했다.
- 건축물대장 배지 저장 시 전체 매물을 훑던 OR 조건을 기본키/매물번호 인덱스 조회로 분리했다.
- 지도 자체의 5분 자동 새로고침은 유지했다. 따라서 위 최적화로 화면 최신성이 늦어지지 않는다.
- 배포 전 3시간 쿼리 분석에서 운영현황 100회가 7.80M행, 통합원본 생성 82회가 2.36M행을
  읽은 것이 과다 사용의 핵심 원인이었다. 배포 후 실제 재접속에도 두 실행횟수가 증가하지 않았다.
- 2026-08-06 확인값: Workers 오늘 5,124/100,000(5.12%), R2 925.23MB/10GB(9.25%),
  Class A 10.85k/1M(1.09%), Class B 18.78k/10M(0.19%), 청구 사용량 `$0.00`.
- 2026-08-06 14:35:09 KST 재조회: Workers 오늘 12,034/100,000(12.03%), D1 읽기 128.28M/5M,
  쓰기 1.1M/100k, 저장공간 323.89MB/5GB, R2 버킷 1.18GB/10GB(11.80%), Class A
  12.84k/1M(1.28%), Class B 24.97k/10M(0.25%), 전체 청구 사용량 `$0.00`.
- D1 대시보드의 127.6M/1.1M은 이전 실행과 표시 지연이 섞인 누적값이다. 정상 운영 기준 일일
  사용량은 다음 오전 9시 초기화 뒤 별도로 다시 확인한다.

## 현재 운영 주소

- 운영: https://js-map.com
- 보조 도메인: https://www.js-map.com
- GitHub: https://github.com/cho711920-ops/my-map
- 작업 브랜치: `codex/cloudflare-js-map`
- Cloudflare Worker: `js-map`
- 현재 배포 버전: `320ea09e-bb9b-4281-bad5-29e89909245d`

## 현재 구조

- Cloudflare Worker가 정적 사이트와 모든 API를 제공한다.
- D1 `js-map-primary`가 매물·원본매물·사진 URL·연락처·메모·고객·수집기 작업·변경이력을 저장한다.
- R2 `js-map-media`가 실제 열어 본 외부 사진과 다음 사진을 7일간 캐시한다.
- Google은 OAuth 로그인에만 사용한다. 허용된 Gmail 계정만 접속할 수 있다.
- Google Sheet와 Apps Script는 운영 런타임에서 호출하지 않는다. 기존 자료는 백업으로 보존한다.
- Vercel은 운영 런타임에서 사용하지 않는다. 2026-08-05 Pro 구독을 해지하고 Hobby 무료 플랜으로 전환했다.

## 현재 완료 데이터

- 통합매물: 8,756개
- 활성 원본매물: 23,077개
- 사진 URL: 112,456개
- 연락처: 3,151개 / 2,321개 매물
- 고객: 2명
- 검증 대기: 5,654개
- 고객매칭: 71개
- D1 크기: 약 322.6MB

## 이전 완료 기능

- 매물 목록·검색·필터·지도·상세·전체 사진
- 메모, 임대조건, 연락처, 즐겨찾기와 고객매칭 저장
- 운영현황, 매물검토함, 변경이력
- 네이버·공실박스·당근 수집기와 중복 판정·수집 재개
- 국토교통부 건축물대장 직접 조회와 D1 캐시
- 소상공인시장진흥공단 상권 조회와 D1 캐시
- SGIS·R-ONE 지역통계 직접 조회와 D1 캐시
- 허용 Gmail 로그인과 세션 보호
- 외부 사진 프록시, 다음 사진 미리받기, R2 7일 캐시

## 안전장치

- R2 외부 사진 캐시는 7일 후 자동 삭제한다.
- 7일 사진 저장 예산은 6GB로 제한한다.
- 과거 `apps-script-sync` 예약 작업은 종료 처리했고 1분 Cron Trigger를 제거했다.
- `/api/apps-script` 경로는 제거했다. 프런트는 `/api/data`와 `/api/collector`만 사용한다.
- 수집기 키와 공공데이터 키는 Cloudflare Secret으로만 저장되어 있다. 문서·코드·로그에 값을 쓰지 않는다.

## 마지막 검증

- Cloudflare 자동 테스트 30개 통과
- `js-map.com`과 `www.js-map.com` HTTP 200
- 비로그인 `/api/data` HTTP 401
- 제거된 `/api/apps-script` HTTP 404
- 잘못된 수집기 키 HTTP 403
- 로그인 상태에서 매물 8,749개 화면 로딩(주소 오류 7개는 기존 데이터)
- 운영현황 D1 통합매물 8,756개, 원본매물 23,077개 확인
- 사진 17장 상세창과 2·3번째 사진 전환 확인
- 건축물대장 공식 API 실조회 확인

## 2026-08-05 확인된 이전 누락과 복구 원칙

- D1의 `collector_raw`가 0건이라 기존 웹의 검증대기 5,000건 이상이 이전되지 않았다.
- 기존 고객 `하하99`는 D1에 있지만 기존 고객매칭이 0건이다. 현재 `customer_matches` 14건은 2026-08-05 새로 만든 고객 `야호`의 신규 데이터다.
- `customer_activities`는 0건이고, `listing_history`는 새 사이트에서 생성된 `updateProperty` 2건만 있다. 기존 고객활동과 과거 변경이력도 누락 여부를 대조해야 한다.
- 일반 매물 8,754건, 원본매물 23,077건, 사진 URL 112,456건, 연락처 3,151건은 D1에서 확인됐지만 건수만으로 전체 이전 완료라고 판단하지 않는다.
- 기존 Vercel 프로젝트와 Google Sheet·Apps Script 자료는 전체 데이터 대조와 복구 검증이 끝날 때까지 삭제하지 않는다.
- 다음 복구 작업은 검증대기와 고객매칭만이 아니라 고객활동, 즐겨찾기·임장목록, 메모·임대조건, 연락처, 사진, 원본 연결, 수집상태, 변경이력 등 모든 사용자 데이터를 기존 웹과 D1 사이에서 전수 대조한다.
- 재생성 가능한 캐시와 사용자 원본 데이터를 구분하고, 사용자 원본 데이터는 건수·키·표본 내용·화면 표시까지 확인한 뒤 누락분만 D1에 복구한다.
- D1 무료 일일 한도가 초기화되는 한국시간 오전 9시 이후 사용량을 먼저 확인하고, 복구는 일일 쓰기 한도를 넘지 않도록 소량 배치로 진행한다.

## 다음 작업 원칙

1. 작업 전 `git status`를 확인하고 사용자 변경을 보존한다.
2. 모든 운영 저장은 D1을 기준으로 한다. Google Sheet 동기화를 다시 연결하지 않는다.
3. 키는 propertyId, customerId, sourceListingId를 사용한다. 화면 행번호로 수정하지 않는다.
4. 배포 전 `pnpm run test:cloudflare`와 `pnpm run cf:check`를 실행한다.
5. 배포는 `pnpm run build:cloudflare` 후 `pnpm exec wrangler deploy`로 한다.
6. Vercel `my-map` 프로젝트 영구 삭제나 기존 Google 자료 삭제는 사용자 승인 없이 하지 않는다. Pro 구독 해지는 완료됐다.
7. 비용은 `outputs/JS_MAP_MONTHLY_COST.md`의 무료·유료 시나리오를 기준으로 판단한다.
8. 사용자가 “기존데이터 전체확인 후 빠진 것 체크해서 복구해줘”라고 요청하면 위 누락 항목에 한정하지 말고 전체 사용자 데이터 정합성 감사를 먼저 수행한다.

## 2026-08-05 네이버 지도 인증 오류 수정

- 배포 버전은 `34e96049-fc87-4282-b1f0-d3f486fd7e86`이다.
- 원인은 새 운영 도메인이 네이버 Maps 애플리케이션의 Web 서비스 URL 허용 목록에 아직 등록되지 않아 Dynamic Map 인증이 거절되는 것이다.
- 네이버 SDK의 공식 `window.navermap_authFailure` 콜백을 처리했다. 인증 오류가 발생하면 반복되는 오류 타일을 제거하고, 정상 작동하는 네이버 거리뷰를 전체 너비로 확장하며, 별도 지도 탭은 계속 사용할 수 있다.
- `https://js-map.com` 운영 화면에서 거리뷰 유지와 오류 타일 제거를 확인했다.
- 영구 설정은 네이버 클라우드 로그인 후 Web Dynamic Map을 활성화하고 Web 서비스 URL에 `https://js-map.com`, `https://www.js-map.com` 두 주소를 등록해야 완료된다. 포트와 하위 경로는 넣지 않는다.

## 2026-08-05 클러스터 상세창·일시 전환 상태 수정

- 클러스터 목록에서 매물 상세창을 연 상태로 지도에서 다른 클러스터나 매물을 선택하면 이전 상세창과 AI 보조 패널을 먼저 닫도록 수정했다.
- 로드뷰를 열고 닫거나 브라우저 탭을 잠시 전환할 때 발생하는 지도 재배치 이벤트는 실제 사용자 지도 이동과 구분한다. 따라서 선택한 클러스터와 해당 매물 목록이 유지된다.
- 사용자가 지도를 직접 드래그하거나 휠·터치로 확대/축소하면 기존 의도대로 클러스터 선택을 해제한다.
- 운영 사이트에서 `선택 매물 3개` 클러스터로 로드뷰를 열고 닫은 뒤에도 선택 마커 1개와 목록 3개가 유지되는 것을 확인했다.
- 관련 자동 테스트와 Cloudflare 테스트 40개가 통과했으며 D1/R2 데이터 쓰기는 발생하지 않았다.

## 2026-08-05 추출 원본 미노출·재노출 및 계약완료 보기 수정

- 네이버·당근·공실박스의 정상 전체수집 결과에서만 미노출 횟수를 올리고, 3회 연속 미노출된 추출 원본만 `active=0`으로 숨긴다.
- 부분수집·안전중단·오류 수집은 누락 횟수를 올리지 않는다.
- 원본이 다시 관찰되면 내용 변경 여부와 관계없이 `missing_count=0`, `active=1`로 복구한다. 따라서 누락은 반드시 연속 3회여야 한다.
- 추출 원본의 일반 숨김·관찰 과정에서는 `listings.status`를 변경하지 않고 사용자의 `계약완료` 판단도 계속 수동으로만 한다. 단 2026-08-26부터 계약완료 후 3회 미노출됐던 원본이 다시 나타나거나 새 원본 광고가 연결된 경우에만 대표를 자동 활성화한다.
- 거래확인 후보는 아직 계약완료되지 않은 통합매물 중 연결된 모든 원본이 3회 연속 미노출된 경우만 매물 단위로 집계한다.
- 보기 메뉴와 지도 세로 보기 버튼에 `계약완료만 보기` 체크박스를 추가했다. `계약완료 숨김`과 상호 배타적으로 작동하고 선택 상태는 새로고침 뒤에도 유지된다.
- 운영 사이트에서 계약완료만 보기 4개가 모두 계약완료 카드임을 확인했고, 숨김 전환 후 계약완료 카드가 0개가 되는 것을 확인했다.
- 배포 버전은 `34e96049-fc87-4282-b1f0-d3f486fd7e86`이다.
# 2026-08-06 스마트폰 전용 모바일 앱 UI

- PC 화면은 변경하지 않고 `max-width: 768px`에서만 작동하는 `mobile-app-v1.css/js`를 추가했다.
- 모바일은 지도·매물·임장·고객·더보기 하단 탭, 상단 검색, 빠른등록, 더보기 바텀시트를 사용하는 앱형 구조다.
- 지도 클러스터가 기존 사이드바를 열면 모바일 매물 탭으로 자동 전환된다.
- 모바일 매물카드는 사진·핵심 임대조건·전화·내비만 우선 표시하고 카드 선택 시 전체 화면 상세로 열린다.
- 통합매물 사진과 상세 열기는 모바일에서도 지원하며 상세 안에서 내비·로드뷰·대장·수정을 사용할 수 있다.
- 390px 모바일 지도/목록/더보기/상세와 1280px PC 비활성 분기를 검수했다.
- Cloudflare 자동 테스트 36개와 `cf:check`가 통과했다.
- 운영 Worker 버전은 `dc641d83-4854-443d-bea7-8147d7859bd0`이다.

## 2026-08-07 모바일 뒤로가기 창 닫기

- 스마트폰 크기(`max-width: 768px`)에서만 브라우저/안드로이드 뒤로가기와 모바일 창 상태를 연결했다.
- 매물 상세, 사진, 연락처, 수정, 건축물 등록, 로드뷰, 빠른등록, 더보기, 운영센터·고객 창, 목록관리 등 열린 창이 있으면 뒤로가기 1회로 가장 위 창만 닫힌다.
- 열린 창이 없고 매물 목록 화면이면 뒤로가기 1회로 지도 화면으로 돌아간다. 그다음 뒤로가기는 브라우저의 원래 이동을 따른다.
- 화면의 X/닫기 버튼으로 창을 닫았을 때도 불필요한 방문 기록을 함께 정리해 뒤로가기를 여러 번 누르는 현상을 방지했다.
- PC에서는 모바일 모드가 활성화되지 않으므로 기존 동작을 변경하지 않는다.
- Cloudflare 자동 테스트 36개와 `cf:check`가 통과했다.
- 운영 Worker 버전은 `e70c5f40-dc41-4144-8934-dd234307bbbe`이다.

## 2026-08-07 Mobile customer matching app UI

- This change is mobile-only (`max-width: 768px`); desktop customer matching layout and behavior are unchanged.
- The customer workspace now uses a master/detail flow: full-width customer list, then a separate full-width matching-list screen after customer selection.
- Added a visible `customer list` back button and integrated the matching screen with mobile browser/device back history.
- Customer cards, selected-customer actions, filters, and matched listing cards were resized and wrapped to prevent horizontal clipping at 390px.
- Verified at 390x844: no horizontal overflow, list/detail transition works, and browser back returns from matches to the customer list.
- Verified at 1280x800: mobile mode is inactive, both original desktop panes remain visible, and the mobile back button is hidden.
- Cloudflare tests: 36/36 passed. `cf:check` passed.
- Production Worker version: `79b2d8fc-88cb-4aa9-955e-3f1a63f41849`.

## 2026-08-07 Mobile customer card overlap fix

- Fixed a mobile-only layout regression where the matched-listing grid compressed every outer card to 20px while its inner content remained about 110px tall, causing all cards to overlap.
- The matched-listing container now uses normal block flow; every card has automatic content height with a 126px minimum and an 8px vertical gap.
- Customer cards now reserve 124px and keep the 24px statistics row visible instead of clipping the bottom content.
- Production verification at 390x844 used the real signed-in dataset: 41 matched listings rendered at 128-131px each without overlap, and both customer cards showed condition and statistics rows.
- Desktop verification at 1280px confirmed mobile mode is inactive and the mobile back control remains hidden.
- Cloudflare tests: 36/36 passed. `cf:check` passed.
- Production Worker version: `c89c7e62-fd46-4069-beb2-ecd74e82366a`.

## 2026-08-07 Mobile keyword search fix

- Fixed the mobile-only main search so its keyword is synchronized to the existing listing filter while typing and before switching to the listing tab.
- Mobile keyword searches now scan the full loaded listing dataset instead of intersecting with the current map viewport. Desktop continues to use the existing map-bounds behavior.
- Added a mobile empty-result message instead of leaving a blank listing panel.
- Production verification at 390x844: `괴정동` returned 202 listings, retained the query after opening the listing tab, and rendered 14 cards in the virtual window.
- Verified a nonsense query renders the explicit zero-result message.
- Verified at 1280x800: mobile mode and mobile search flag are inactive, so desktop behavior is unchanged.
- The search remains client-side over already loaded listings and adds no D1 requests.
- Cloudflare tests: 36/36 passed; mobile search tests: 2/2 passed. `cf:check` passed.
- Production Worker version: `4ab663bb-38c9-44c4-9c11-08195a0bf89c`.

## 2026-08-07 Mobile listing sort alignment

- Fixed the mobile-only listing toolbar so `source`, `type`, `floor`, and `sort` stay in one four-column row.
- Reset a legacy `grid-row: 2` rule on `#sortDropdown` that pushed the sort button over the first listing card.
- Normalized all four controls to the same 36px height and top coordinate at 390px.
- Production visual verification confirmed no overlap and a single aligned row; desktop remained in its original flex layout with mobile mode inactive.
- Mobile tests: 3/3 passed; Cloudflare tests: 36/36 passed. `cf:check` passed.
- Production Worker version: `4016b362-40d6-4e5a-a6b1-927062d409ed`.

## 2026-08-07 Edge 자동수집 안정화

- Windows 예약 작업 `JSMap Auto Collector`를 30분 반복이 아닌 매일 오전 11시 1회 실행으로 변경했다. 놓친 실행만 `StartWhenAvailable`로 보완한다.
- Manifest V3 서비스 워커가 장시간 수집 중 종료되어 두 번째 대상에서 멈추던 문제를 실행 상태 영속화와 대상 완료 메시지 방식으로 수정했다.
- 대상 완료 메시지가 서비스 워커 재시작 시 유실되지 않도록 최대 12회 재전송한다.
- 자동수집 대상 탭은 백그라운드로 열고 전용 Edge 창을 대상 전환 전후 최소화하여 창이 앞으로 반복해서 나타나지 않게 했다.
- 네이버 자동수집이 등록한 구를 현재 지도 화면의 선택 구로 덮어쓰던 문제를 수정했다. 자동실행은 등록된 `cortarNo`를 그대로 사용한다.
- 공실박스 자동 대상은 사용자 선택에 따라 비활성화했다. 현재 자동 대상은 네이버 5개 구와 당근 5개 구이며 공실박스는 수동 수집한다.
- 전용 Edge 확장 프로그램 `1.0.11`을 로컬 프로필에 적용했고 기존 로그인·대상 설정은 보존했다.
- 5분 연속 관찰에서 전용 Edge 프로세스 1개와 최소화 상태가 유지됐고 추가 창/재실행은 없었다. 다음 예약은 `2026-08-08 11:00 KST`다.
- `tests/edge-auto-collector.test.mjs` 5/5와 확장 프로그램 JavaScript 구문 검사를 통과했다.

## 2026-08-10 Naver/Daangn automatic collection repair

- Naver v5.9.0 completed a manifest-first revalidation for all five Daejeon districts. Jung-gu completed with no issue; Yuseong-gu, Daedeok-gu, Seo-gu and Dong-gu were retained as partial only for provider rows that expose no usable address/floor (65, 10, 81 and 2 rows respectively). There was no remaining network or storage failure on the final pass.
- Daangn detail parsing now inherits address, room and rental terms from its list manifest, accepts additional provider address fields, normalizes absent coordinates to null, and stores a compact checkpoint. This removed the prior D1 type error and oversized checkpoint failure.
- Daangn browser jobs are scoped by district/cluster. A new automatic tab now waits for server job status and resumes a running or paused checkpoint instead of replacing it with a new session.
- Final live Daangn verification proved manifest skipping: Seo-gu scanned 3,629 IDs but skipped 3,528 unchanged rows and fetched only 101 detail candidates; Dong-gu scanned 592, skipped 563 and fetched only 29.
- Remaining Daangn failures are provider-data omissions, not collector crashes: Yuseong 22, Seo 25, Daedeok 11, Jung 6 and Dong 20 rows had no usable address/floor. Partial sessions preserve all successful saves and do not mark missing listings as inactive.
- D1 reached its 500 MB per-database ceiling during Seo-gu. Removed 5,540 superseded collector-review snapshots plus two obsolete job checkpoints; production size fell to about 445.4 MB. Listings, photos, memos, contacts, latest reviews and business history were preserved.
- Migration `0012_collector_review_dedup.sql` adds one active review row per `(source, source_listing_id)`. Repeated collection now refreshes that row instead of growing D1 indefinitely. Duplicate active review count is 0 after cleanup.
- Stale `running` collector sessions were marked `paused`; there are no falsely running sessions left.
- Production Worker version: `d17ba9af-7946-40e8-a5d1-6c3ae69b560d`.
- Installed Edge automation: extension `1.0.25`, Daangn collector `1.4.5`, daily schedule 11:00 KST.
- Full Cloudflare/Edge suite: 62/62 passed; Wrangler dry-run also passed.

## 2026-08-10 Pending-review deduplication and collector completion semantics

- Production review audit showed 9,243 active review rows with 9,243 distinct provider IDs: there were no repeated rows for the same provider ID. The growth came from provider re-posts that received new Naver/Daangn IDs.
- Ingestion now compares a new provider ID with active pending reviews at the same address. If floor/unit, deposit, rent and area satisfy the existing physical-listing rule, it stores a compact alias instead of adding another active review row.
- Scheduled review repair decision version 4 collapses already queued exact pending duplicates into the oldest canonical review. The first production cycles reduced active reviews from 9,243 to 9,224 and marked 29 aliases while leaving ambiguous listings for manual review.
- Migration `0013_collector_review_address_index.sql` indexes active review address lookup. Production query planning confirms `idx_collector_raw_review_address` is used, avoiding a full collector table scan on every repair cycle.
- Daangn collector `1.4.6` reconnects a manual interrupted run from the saved checkpoint up to five times. Automatic runs use the same checkpoint path.
- A completed scan with provider address/floor omissions now returns a completed partial result with warnings. It is not treated as a crash, so the automation extension does not restart that district from zero.
- Installed Edge automation is `1.0.26`; Windows task remains daily at 11:00 KST. Gongsilbox remains manual.
- Production Worker version: `c11ec97b-d825-4d4e-a461-c0af26e5ce99`.
- Full test suite: 90/90 passed; Wrangler dry-run passed; live asset verification confirmed Daangn `1.4.6`, partial completion mode and manual recovery.

## 2026-08-10 Edge automatic runner v1.0.29

- The Windows launcher now finds the dedicated unpacked-extension ID from its profile and triggers an extension-owned autorun page through the local Edge debugging endpoint. It no longer depends on the signed-in `js-map.com/collector-install` page to deliver the daily start signal.
- The internal autorun page reloads a stale Manifest V3 background worker once after an extension upgrade, then the launcher sends a second idempotent trigger. Normal daily launches observe an already active run without creating a duplicate.
- Automatic collection settings show the installed extension version in the header. Installed production profile is `v1.0.29`; the registered targets remain Naver 5 districts plus Daangn 5 districts, with Gongsilbox disabled/manual.
- Fully enumerated districts with address/floor omissions are now accepted as partial completion warnings. They are not put into the deferred full-district retry queue. Two legacy queued Naver retries were removed from the active run.
- Target transitions no longer minimize the Edge window. The scheduled launch starts minimized, but once the user restores the window it remains visible while Naver/Daangn advance between districts.
- Live verification: Naver completed its five-district first pass and the active run advanced into Daangn; extension state reported `v1.0.29`, 10 total targets and an empty retry queue after cleanup.
- Edge automation tests: 7/7 passed.

## 2026-08-10 Per-district collection report and partial-run retry v1.0.30

- The Edge automatic-collection settings screen now keeps a single visible report for all 10 registered targets: Naver five districts and Daangn five districts. Each row shows waiting, running, retrying, completed, partial or failed state, timestamps and available counts.
- The report persists in the dedicated Edge profile and refreshes every five seconds. It adds no D1 reads or writes.
- A Daangn payload with `partial: true` is no longer accepted as a completed target even when the bridge message itself is technically successful. It is retried from the saved checkpoint and the runner advances only after an actual completion.
- Naver's fully enumerated provider omissions remain warning-completions so known address/floor omissions do not restart an entire district.
- Production-data inspection confirmed the user's report: the latest automatic run did not complete all 10 targets. Daangn Yuseong ended partially and Daangn Seo had no completion record. The JS map currently contains only first-floor originals for Yongmun-dong 257-5; the reported second-floor Daangn listing has not yet been ingested.
- Installed extension files were updated in place to `v1.0.30`. The registered targets and schedule were preserved, and no Edge window was restarted or minimized.
- A user-forced/manual launcher run now opens and stays visible; only the unattended Windows scheduled run starts minimized. Target transitions do not change the user's chosen window state.
- Edge automation tests: 8/8 passed; Cloudflare tests: 56/56 passed.

## 2026-08-10 Gongsilbox incremental detail refresh v2.1.4

- Audited a manual Gongsilbox run that selected 2,531 listings, fetched 2,364 details, created 11 listings and took about one hour. The daily slowdown came from refreshing unchanged contact details after only 20 hours and from the provider-safe single-detail request queue.
- Unchanged Gongsilbox details now refresh every seven days instead of every daily collection. New listings and material rental changes still request details immediately; daily manifest comparison remains intact.
- Import results now separate actual rental-condition changes from source-information refreshes such as photos, contacts and provider snapshots. The collector UI shows `임대조건 변경` and `정보 최신화` independently instead of labeling all source updates as condition changes.
- Gongsilbox remains manual-only. Installed collector is `v2.1.4`; Edge automatic runner remains `v1.0.30` for Naver five districts plus Daangn five districts.
- Full Cloudflare tests: 58/58 passed; Edge automation tests: 8/8 passed; Wrangler dry-run passed.
- Production Worker version: `35151ebf-834c-4ac8-b96d-edd4da4cea4c`.

## 2026-08-11 Professional desktop/tablet navigation refresh

- Rebuilt the desktop and landscape-tablet header as one 60px navigation row while preserving every existing click handler and data path.
- Added compact line icons, clearer primary/secondary button hierarchy, a branded subtitle and a blue quick-registration action.
- The map's vertical quick tools now use independent translucent glass buttons without the previous solid background rail.
- Mobile app layouts remain isolated at 768px and below; no mobile data or interaction logic was changed.
- Render QA covered 900, 1024, 1199, 1200, 1280, 1440, 1540 and 1920px with zero overlaps or viewport overflow.
- Targeted UI tests: 5/5 passed; Wrangler build and dry-run passed.
- Production Worker version: `0be2698b-ab49-4cbd-8ac9-e8d401889f60`.
- Follow-up refinement connected and capped the search group, removed desktop/tablet green status dots, reduced button padding and unused status-column width, and rebuilt the translucent vertical toolbar with per-action line icons and compact labels.
- Refined production Worker version: `5deb54c1-c177-4c91-b042-2a8d528bc7fc`.
- Balanced-toolbar follow-up widened the desktop search input by about 1.5x, widened Tell, reduced the JS부동산 brand weight, forced the heart icon to remain visible and preserved the multi-select SVG when its active state changes.
- Live desktop QA at 1794px measured a 348px keyword input, 78px Tell button, zero search seam gap and visible heart/multi-select icons. Landscape-tablet QA at 1024px reported zero toolbar overflow.
- Final production Worker version: `fdc79bd4-b402-481a-a0c6-3f9c95123738`.
- Roomier desktop actions increased the 1540px+ header labels to 13px with 8px inter-button gaps and wider action cells; 1200px uses 12px/6px while 900-1024px keeps the compact tablet fit. Mobile remains unchanged.
- Responsive QA at 900, 1024, 1200 and 1794px reported zero toolbar overflow. Production Worker version: `2d2ed80c-7f3a-4199-8217-39b7a003db3e`.
- Search-button balance follow-up widened the 1540px+ blue search action from 60px to 76px without reducing the 360px keyword input; both remain 42px high with a zero-gap seam. Production Worker version: `e0b2488c-1b66-4292-8645-0255033a2ac1`.
- Quick-registration emphasis follow-up raises the desktop action to 14px/900 weight (13px at 1200px) while retaining the existing button footprint and zero header overflow. Production Worker version: `465c8f99-318f-49d0-a691-04b18d299e07`.

## 2026-08-11 BuildingHUB credential recovery and elevator-capacity lookup

- The building-register failure was traced to an empty `DATA_GO_KR_SERVICE_KEY` Worker secret plus a missing approval for the separate Ministry of Land BuildingHUB API. The user completed the BuildingHUB application, and the dedicated secret was populated without reusing it in code as an elevator credential.
- BuildingHUB encoded display keys are decoded exactly once before `URLSearchParams` serializes them. This keeps encoded and decoded portal keys equivalent.
- JSONP failures from the building-register and elevator endpoints now remain valid executable callbacks and include a readable public error; Worker logs record the internal action/status for diagnosis.
- Production verification passed on `Seo-gu Dunsan-dong 1236`: opening the register used cached official data, and pressing `Latest lookup` returned a fresh live BuildingHUB response with the current timestamp.
- Elevator-capacity discovery now tries normalized address variants, caches a district-wide operation index in R2 when exact lookup is empty, and enriches matched elevator numbers with the separately approved building-elevator detail API. BuildingHUB and elevator environment variables remain logically separate.
- Cloudflare tests: 88/88 passed. Targeted elevator tests: 5/5 passed. Wrangler build/dry-run passed.
- Production Worker version before the secret recovery verification: `1a2dc74d-42af-4bcf-b629-78d93124be8b`.

## 2026-08-11 Elevator maximum-capacity production repair

- Production D1 proved the display path had never succeeded: 935 active listings had a verified elevator count, but 0 had `building_elevator_capacity` before this repair. Every v1-v3 capacity cache entry was either unmatched or timed out.
- The data.go.kr operation proxy returned zero rows for parcel-address searches. The Elevator Safety Agency source gateway returned the district data normally, but its response identifies buildings by road address rather than parcel address.
- Capacity lookup now passes the selected BuildingHUB title row's official road address and building name, matches the operation response by normalized road address, and falls back to the official source gateway when the proxy is empty.
- Operation rows that already contain `ratedCap` are used immediately instead of waiting for a redundant building-detail call. The old detail endpoint remains a fallback only when capacity is missing.
- Capacity caches are versioned per parcel and road address. A successful D1 persistence now invalidates `api-cache/d1-sheet.csv` and touches the listings revision so the saved value survives reloads instead of waiting for the old one-hour sheet cache.
- Live official-source verification for `대덕구 송촌동 477-1` / `동춘당로 79` returned elevator `5000593`, maximum 13 persons. That verified value was safely persisted to listing `M-e0d98b9b-e3c7-43da-92c4-04742ea290a7`; no other listing fields were changed.
- The regenerative R2 object `api-cache/d1-sheet.csv` was removed once after deployment so the next authenticated sheet request rebuilds it from the corrected D1 row.
- Cloudflare tests: 89/89 passed. Targeted elevator tests: 6/6 passed. Wrangler build/dry-run passed.
- Production Worker version: `72a7be29-4653-4d12-b781-c5f148f06418`.

## 2026-08-11 Precomputed elevator badges for current and future listings

- Listing cards no longer start an elevator-capacity request when they enter the viewport. They render only the BuildingHUB/elevator values already stored in D1, eliminating the late capacity flash.
- Naver, Daangn and Gongsilbox normalization now preserves a provider road address when available. BuildingHUB persistence also writes the official title-row road address without overwriting an existing value.
- Collection completion starts background enrichment immediately, and the existing every-minute Worker cron repeats it. Newest listings are prioritized; cached parcel data is reused, unfamiliar Daejeon parcels derive BuildingHUB inputs from the legal-dong cache, and only official maximum-capacity values are saved.
- A read-first production backfill loaded the five official Elevator Safety Agency district indexes and matched BuildingHUB road addresses exactly. Of 746 eligible missing rows, 738 received a verified maximum capacity; all 746 received the official road address. The remaining eight exact-road nonmatches were left at zero for later official recheck rather than inferred.
- Production active elevator listings after backfill: 936 total, 748 with a saved capacity, 746 with an official road address. Older uncached parcels continue through the automatic minute-by-minute enrichment path.
- Live post-deploy cron verification then advanced those counts to 954 total, 752 with capacity and 763 with an official road address, proving the automatic enrichment path was processing new/cached BuildingHUB rows in production.
- Cloudflare tests: 92/92 passed. Targeted scheduler/elevator tests: 6/6 passed. Wrangler build/dry-run passed.
- Production Worker version: `b65c443b-e056-431e-ade9-f8f6aa0e6f1b`.

## 2026-08-11 Unknown elevator-capacity marker

- A BuildingHUB-verified elevator with no saved official maximum capacity now renders as `🛗 X` instead of leaving the capacity label blank.
- Verified capacities continue to render as `🛗 13인`; listings without a verified elevator still hide the elevator badge entirely.
- The unknown marker is muted gray and its accessible label/title states that maximum capacity is unconfirmed.
- Cloudflare tests: 92/92 passed. Targeted elevator tests: 5/5 passed. Wrangler build/dry-run passed.
- Production Worker version: `bc9272c4-ebda-4cfa-a686-de7fc480701a`.

## 2026-08-11 Listing-card elevator rendering repair

- The list-card template previously created every elevator element hidden and relied entirely on a later badge-binding pass. This allowed the register modal to show official values while the listing itself remained blank when card binding had not run yet.
- Cards now render persisted D1 elevator data directly during HTML creation: verified capacity displays `🛗 13인`, verified elevator with missing capacity displays `🛗 X`, and no verified elevator remains hidden.
- The badge module now performs a startup rebinding pass for already-rendered/virtualized cards and can recover the card item by `property_id` when a card was created before the module loaded.
- A fresh BuildingHUB modal lookup now refreshes the matching listing card by `property_id` as well as parcel key, so closing the register immediately leaves the icon visible on the list.
- Direct card-markup execution passed for known, unknown and absent elevator cases. Cloudflare tests: 92/92 passed; Wrangler build/dry-run passed.
- Production Worker version: `c00ad386-5d3a-4523-ae19-20862fb8ca1e`.

## 2026-08-11 Independent elevator-registry audit and repair

- `서구 용문동 227-1` exposed an official-source discrepancy: BuildingHUB reported passenger/emergency elevators as zero, while the Elevator Safety Agency operation registry reported two active elevators, including a 13-person accessible elevator.
- Elevator-registry discovery no longer requires a positive BuildingHUB count. Separate D1 fields now preserve the BuildingHUB count, safety-registry count, lookup state and checked time; the effective card count is the maximum of the two official sources so a later BuildingHUB refresh cannot erase a verified safety-registry match.
- Older Daangn source snapshots were also inspected. Generic `address`/`publicAddress` fields recovered road addresses that had not previously reached the listing row, and future collection now preserves those verified road-address values automatically.
- The complete Daejeon five-district operation index contained 28,117 elevator rows across 12,012 road addresses. All 3,725 active listings that could be linked by an official/provider road address were audited: 1,943 listings matched the safety registry at 1,023 road addresses.
- The first audit repaired 168 listings where BuildingHUB had already reported zero and 714 previously unchecked listings where the safety registry proved an elevator. The expanded source-address audit recovered another 252 missing badges, for 1,134 newly visible elevator badges in total.
- The immediate post-audit snapshot was 2,138 active elevator listings, 1,938 with a verified person capacity and 200 correctly showing `X`; the live minute scheduler continues advancing those counts as remaining addresses acquire an official road-address link.
- `서구 용문동 227-1` has 12 active listings; all 12 now persist two elevators and maximum capacity 13. Production invariants confirmed zero registry-matched listings with an effective count of zero and zero source-merge count mismatches.
- Future automatic and manual listings run the safety-registry check independently after collection/BuildingHUB enrichment. Matches, no-matches and temporary upstream failures use separate refresh windows, preventing the same locations from being queried every minute.
- Regenerative listing caches were invalidated and the listings revision was advanced for an immediate client refresh. Cloudflare tests: 95/95 passed; targeted elevator tests and Wrangler dry-run passed.
- Production Worker version: `af56dfe4-3067-4cf1-9ddb-c08fa5004c8e`.

## 2026-08-12 v6-cloud 브라우저 데이터 접근 계층 기반

- 작업 시작 시 `codex/cloudflare-js-map` 브랜치의 깨끗한 작업 트리를 확인하고, 원격보다 뒤처진 6개 커밋을 `git fetch --prune`과 `git pull --ff-only`로 반영했다. 시작 기준 원격 HEAD는 `527eb40`이었다.
- 현재 저장소는 이미 Google Sheet·Apps Script·Vercel을 운영 런타임에서 제거하고 Worker + D1 + R2로 운영 중이므로, 새 Cloudflare 스켈레톤을 중복 생성하지 않고 브라우저 데이터 접근 경계를 추가했다.
- 새 `js/data-access-v6.js`는 `/api/data` D1 조회·저장과 `/api/sheet` CSV 호환 조회를 한곳에서 관리한다. 동일 출처 세션 자격, 캐시 정책, 강제 새로고침 헤더, JSON 오류와 재시도 가능 상태를 공통 처리한다.
- 매물 CSV 초기 로딩, 리비전 확인, 변경 매물 델타, 통합매물 조회, 원본매물 이동·대표매물 통합 경로를 공통 계층에 연결했다. 기존 직접 `fetch` 코드는 모듈 미로딩 시 호환 폴백으로 남겨 기존 빠른등록·임장ON·메모수정·로드뷰/지도·선택인쇄 동작을 변경하지 않았다.
- 캐시 버전을 `data-access` 버전으로 올렸고, 데이터 접근 모듈이 모든 소비 모듈보다 먼저 로드되는지 자동 검증한다. 자세한 경계와 점진 전환 원칙은 `docs/CLOUDFLARE_MIGRATION.md`에 추가했다.

### 변경 파일

- 신규: `js/data-access-v6.js`, `tests/cloudflare-data-access.test.mjs`
- 런타임 연결: `index.html`, `js/script.js`, `js/map.js`, `js/unified-listings-v8.js`
- 문서: `docs/CLOUDFLARE_MIGRATION.md`, `CODEX_HANDOFF.md`
- 회귀 계약/캐시 버전 갱신: `tests/cloudflare-cluster-selection-persistence.test.mjs`, `tests/cloudflare-field-visit-cluster.test.mjs`, `tests/cloudflare-list-virtualization.test.mjs`, `tests/cloudflare-separate-original.test.mjs`, `tests/cloudflare-whole-master-merge.test.mjs`

### 테스트와 빌드

- `pnpm run test:cloudflare`: 99/99 통과
- `pnpm run cf:check`: Cloudflare 자산 124개 빌드 및 Wrangler 4.118.0 dry-run 통과
- `pnpm run build:cloudflare`: 통과, `.cloudflare-assets/js/data-access-v6.js` 포함 확인
- 변경 파일과 빠른등록·임장·메모·찜·인쇄·건축물대장·운영센터 관련 핵심 브라우저 스크립트 12개의 `node --check`: 통과
- `git diff --check`: 통과

### 남은 작업과 주의사항

- 이번 변경은 안전한 첫 경계다. `operations-*`, `list-manager`, `ai-visit`, 비동기 저장 큐 등 나머지 브라우저 모듈의 직접 `/api/data` 호출은 기능별 테스트를 붙인 뒤 공통 계층으로 점진 전환한다.
- 호환용 `saveApiURL`, `sheetURL`, 직접 `fetch` 폴백은 모든 소비 모듈 전환과 실제 브라우저 회귀 확인 전까지 제거하지 않는다.
- 오래된 독립 `.cjs` 테스트 일부는 현재보다 과거인 캐시 버전을 고정 검사하거나 다른 PC의 절대 경로를 참조해 일괄 실행할 수 없다. 공식 릴리스 게이트인 `test:cloudflare`는 전부 통과했으며, 레거시 테스트는 별도 정리 후 공식 스크립트에 편입하는 것이 안전하다.
- D1 스키마·운영 데이터·R2 객체는 변경하지 않았고 이번 작업에서 운영 Worker 배포도 수행하지 않았다.

## 2026-08-12 v6-cloud 운영센터 데이터 접근 계층 전환

- 작업 시작 시 `CODEX_HANDOFF.md`를 UTF-8로 확인하고 `git fetch --prune origin`을 실행했다. 로컬 `codex/cloudflare-js-map`과 원격은 모두 `c957ecf`로 이미 일치해 추가 병합은 없었다.
- `operations-center-v7.js`, `operations-collection-v8.js`, `operations-admin-v1.js`의 현황·고객·매물검증·변경이력·사용자 관리 조회와 저장을 `JSDataAccessV6.read/mutate`에 연결했다.
- 공통 계층이 없는 구형 독립 실행 환경에서는 기존 `saveApiURL`·직접 `fetch`가 계속 동작하도록 가드된 폴백을 유지했다. D1 액션 이름, 요청 본문, 세션 자격, 화면별 오류 처리 흐름은 바꾸지 않았다.
- 운영센터 자산 캐시 버전을 각각 `7.22.2-data-access`, `7.25.3-data-access`, `1.0.4-data-access`로 갱신했고, 공통 계층 선행 로드와 세 모듈의 조회·저장 연결을 공식 Cloudflare 테스트에서 자동 검증한다.
- `docs/CLOUDFLARE_MIGRATION.md`의 브라우저 데이터 접근 경계에 운영센터 적용 범위를 반영했다.

### 테스트와 빌드

- 변경한 운영센터 JavaScript 3개의 `node --check`: 통과
- 데이터 접근·전문 운영센터 대상 테스트: 10/10 통과
- `pnpm run test:cloudflare`: 100/100 통과
- `pnpm run cf:check`: Cloudflare 자산 124개 빌드 및 Wrangler 4.118.0 dry-run 통과
- `pnpm run build:cloudflare`: 통과
- `git diff --check`: 통과
- 오래된 독립 운영센터 `.cjs` 테스트 4개는 코드 실행 전에 다른 PC의 절대 경로 또는 현재 저장소에 없는 과거 `outputs` 폴더를 읽다가 종료됐다. 이번 변경의 공식 테스트와 대상 테스트는 모두 통과했으며, 이 레거시 경로 문제는 별도 테스트 정리 작업으로 남겼다.

### 남은 작업과 주의사항

- 다음 전환 후보는 `list-manager-v6.js`, `ai-visit-session-v6.js`, `async-mutation-queue-v1.js`, `diagnosis-storage.js`다. 비동기 큐는 재시도·작업상태 계약이 있으므로 독립 테스트의 절대 경로 문제를 먼저 제거한 뒤 전환하는 편이 안전하다.
- 운영센터의 직접 `fetch` 코드는 공통 계층 미로딩 시의 호환 폴백이므로 실제 브라우저 회귀 확인 전까지 제거하지 않는다.
- D1 스키마·운영 데이터·R2 객체는 변경하지 않았고 운영 Worker 배포도 수행하지 않았다.

## 2026-08-12 찜목록 지도 이동 동기화 및 행정구역 클러스터 축척 조정

- 찜폴더의 `지도 보기`에서 동 단위 행정구역 클러스터를 선택한 뒤 지도를 직접 드래그하거나 확대·축소하면, 일반 클러스터 고정 상태와 행정구역 목록 선택 상태를 함께 해제하도록 수정했다. 이동 완료 후 목록은 `favoriteOnly` 조건을 유지한 채 새 지도 화면 안의 찜 매물로 다시 계산된다.
- 카카오 지도 축척 기준을 한 단계씩 늦췄다. 레벨 7(`1km`)은 동 단위, 레벨 8 이상(`2km` 이상)은 구 단위로 묶고, 레벨 6(`500m`) 이하에서는 일반 공간 클러스터를 사용한다.
- 지도 캐시 버전을 `8.2.5-favorite-map-navigation`으로 갱신하고, 선택 상태 초기화와 축척 전환 경계를 독립 상태 테스트 및 Cloudflare 회귀 테스트에 반영했다.

### 테스트와 운영 확인

- 지도 JavaScript 문법 검사와 대상 회귀 테스트 2개 통과
- `pnpm run test:cloudflare`: 100/100 통과
- `pnpm run cf:check`: Cloudflare 자산 124개 빌드 및 Wrangler dry-run 통과
- 운영 배포 후 `https://js-map.com/`에서 찜폴더 5개 매물과 `1km` 동 클러스터 표시를 확인했다.
- 운영 HTML은 `js/map.js?v=8.2.5-favorite-map-navigation`을 제공하며, 운영 지도 스크립트의 `1km=동`, `2km 이상=구`, 사용자 지도 이동 선택 해제 코드 포함을 직접 확인했다.
- Production Worker version: `82d031f1-a2d9-4b98-9b33-4b35f400df95`.

## 2026-08-13 v6-cloud 찜목록 데이터 접근 계층 전환

- 작업 시작 시 `CODEX_HANDOFF.md`와 깨끗한 작업 트리를 확인하고 `git fetch --prune origin` 및 `git pull --ff-only`을 실행했다. 원격의 최신 7개 커밋을 fast-forward로 반영했으며 작업 시작 기준 HEAD는 `51abfb6`이었다.
- `list-manager-v6.js`의 찜목록·임장목록 D1 조회와 저장을 `JSDataAccessV6.read/mutate` 공용 경계에 연결했다. 로그인 계정 확인용 `/api/session`은 데이터 API가 아니므로 기존 세션 요청을 유지했다.
- 공용 모듈이 없는 구형 독립 실행 환경에서는 기존 `saveApiURL` 또는 `/api/data` 직접 요청이 계속 작동하도록 가드된 폴백을 유지했다. `loadCloudState`·`saveCloudState` 액션, 계정별 병합, 삭제 tombstone, 재시도 흐름은 바꾸지 않았다.
- 찜목록 자산 캐시 버전을 `6.4.34-data-access`로 갱신했다. 공용 경계 선행 로드, 읽기·쓰기 연결, 직접 요청 폴백을 Cloudflare 테스트에서 자동 검증하며 계정 전환 테스트는 공용 경계와 폴백 양쪽을 실행한다.

### 테스트와 빌드

- 찜목록 대상 회귀 테스트 5종과 `js/list-manager-v6.js` 문법 검사: 통과
- `pnpm test`: 229/229 통과
- `pnpm run test:cloudflare`: 104/104 통과
- `pnpm run cf:check`: Cloudflare 자산 125개 빌드 및 Wrangler 4.118.0 dry-run 통과
- `git diff --check`: 통과

### 남은 작업과 주의사항

- 다음 데이터 접근 전환 후보는 `ai-visit-session-v6.js`, `async-mutation-queue-v1.js`, `diagnosis-storage.js`다. 비동기 큐는 재시도·작업상태 계약을 보존하는 대상 테스트를 먼저 추가한 뒤 전환한다.
- `list-manager-v6.js`의 직접 `fetch`는 공용 계층 미로딩 시의 호환 폴백이므로 실제 브라우저 회귀 확인 전까지 제거하지 않는다.
- D1 스키마·운영 데이터·R2 객체는 변경하지 않았고 이번 작업에서 운영 Worker 배포도 수행하지 않았다.

## 2026-08-13 Tell 주소 붙여쓰기 검색 지원

- Tell 주소 연락처 조회가 입력 문자열을 그대로 D1 `LIKE` 비교해 `탄방동 793`은 검색되지만 `탄방동793`은 검색되지 않던 문제를 수정했다.
- 검색어의 연속 공백을 정규화하고, 기존 원문 검색과 주소 공백 제거 검색을 함께 수행한다. 이제 `탄방동793`, `탄방동 793`, `서구탄방동793`처럼 동명과 번지를 붙이거나 띄어도 같은 연락처를 찾는다.
- 신규 `tests/cloudflare-tell-address-spacing.test.mjs`에서 붙여 쓴 입력과 띄어 쓴 입력의 검색 패턴 및 D1 바인딩을 검증한다.
- `pnpm test`: 230/230 통과, `pnpm run test:cloudflare`: 105/105 통과, `pnpm run cf:check`: 통과했다.
- 운영 D1 읽기 검증에서 `탄방동 793` 원문 조건과 `탄방동793` 공백 제거 조건이 모두 동일한 연락처 4건을 찾았고 데이터 쓰기는 발생하지 않았다.
- 운영 Worker 버전 `9f21d94c-7aba-448a-91f9-64f2dd478ef8`로 배포했다.

## 2026-08-13 Tell 직접 메모 연락처 통합

- Tell 주소 검색 결과에 기존 공실박스 연락처뿐 아니라 사용자가 매물 메모에 역할과 함께 직접 입력한 번호도 합쳐 표시하도록 수정했다.
- `주인`, `임대인/임`, `세입자/임차인`, `관리인`, `남/여`, `가족` 등 역할이 명시된 번호만 직접 메모 연락처로 인정한다. 역할 없는 광고성 번호와 네이버·당근 원본 전화번호는 계속 제외한다.
- 동일 매물에서 공실박스 번호와 직접 메모 번호가 같으면 한 번만 표시하고, 직접 메모 번호에는 응답상 `contactSource: 직접 메모`를 기록한다.
- 주소·건물명·호실뿐 아니라 역할이 명시된 메모 내용과 전화번호로도 Tell 검색이 가능하다.
- Tell/개인정보 대상 테스트 5/5, 전체 테스트 231/231, Cloudflare 테스트 106/106, Wrangler dry-run을 통과했다.
- 운영 Worker 버전 `84767dcd-5b5c-4169-a5d7-53345893dfc4`로 배포했다.

## 2026-08-13 고객매칭 고객 영구 삭제

- 고객매칭 화면의 선택 고객 작업 영역에 빨간색 `고객 삭제` 버튼을 추가했다. 기존 `종료` 상태는 보관용으로 그대로 유지하며, 삭제는 필요 없어진 고객을 완전히 제거할 때만 사용한다.
- 삭제 전 확인창에 고객명과 현재 연결된 매칭·상담 이력 건수를 표시하고, 복구할 수 없다는 경고에 동의한 경우에만 삭제 요청을 보낸다.
- Worker의 `deleteCustomer` D1 작업은 고객 존재 여부를 먼저 확인한 뒤 `customer_matches`, `customer_activities`, `customers` 순서로 하나의 D1 batch에서 정리한다. 삭제 결과에는 실제 정리된 매칭 및 상담 이력 건수를 반환한다.
- 삭제 성공 시 브라우저의 고객·매칭·상담 캐시에서 해당 고객을 즉시 제거하고 다음 고객을 선택한다. 해당 고객의 지도 필터가 열려 있으면 해제하며, 운영자료 캐시를 무효화한 후 서버 자료를 다시 조회한다.
- 정적자산 버전은 CSS `7.20.7-customer-delete`, JS `7.22.3-customer-delete`로 갱신했다.
- `pnpm test`: 233/233 통과, `pnpm run test:cloudflare`: 108/108 통과, `pnpm run cf:check`: 통과했다.
- 운영 `js-map.com`, `www.js-map.com`, Workers 도메인에서 새 HTML·CSS·JS와 삭제 함수 제공을 확인했다.
- 운영 Worker 버전 `0ddfe9d0-0bad-4b83-b50b-b1e410cb13ef`로 배포했다.

## 2026-08-13 AI임장 브라우저 저장공간 초과 복구

- AI임장 실행 시 계정의 진행상태를 `js_ai_visit_sessions_v6` 기기 저장소에 복사하다 브라우저 할당량을 넘으면 영문 `Storage quota` 오류창에서 실행이 중단되던 문제를 수정했다.
- 기기 저장이 실패하면 AI임장 전용 현재·레거시 캐시만 제거하고 한 번 재시도한다. 그래도 공간이 부족하면 계정에서 불러온 진행상태를 현재 화면의 메모리에 유지하고 서버 저장을 계속해 AI임장이 정상적으로 열린다.
- `localStorage.clear()` 또는 다른 키 삭제는 사용하지 않아 찜목록·매물·고객정보·사이트 설정은 건드리지 않는다. D1 스키마와 운영 데이터 변경도 없다.
- AI임장 자산 버전을 `6.4.38-storage-fallback`으로 갱신하고 용량 초과 복구 범위, 메모리 대체, 서버 저장 지속을 Cloudflare 회귀 테스트에 추가했다.
- AI임장·로드뷰 대상 테스트 6/6, `pnpm run test:cloudflare` 117/117, `pnpm run cf:check`, `git diff --check`를 통과했다.
- 전체 `pnpm test` 244/244를 통과했고 운영 HTML의 새 자산 버전과 운영 JavaScript의 용량 초과 대체 코드를 직접 확인했다. 운영 Worker 버전은 `9d1ed057-0fd3-485d-b6d8-3a0d4923db1a`이다.

## 2026-08-13 대표매물 전체 합치기 화면 기능

- 매물 상세창에 기존 `원본 1개 합치기`와 별도로 `대표매물 전체 합치기` 버튼을 추가했다. 활성 원본이 없어 임시 대표정보로 열리는 단독 매물에도 전체 합치기 버튼은 표시된다.
- 버튼을 누른 뒤 오른쪽 목록에서 유지할 대상 매물 카드를 선택하고 마지막 확인을 거치면 기존 서버 `consolidateExistingMasters`를 호출한다. 대상 대표매물의 대표정보·임대조건은 유지하고 출발 매물의 원본·사진·연락처·고객매칭을 대상에 이전한 뒤 출발 대표카드만 제거한다.
- 원본 하나만 이동하는 기존 흐름과 전체 대표매물 이동 상태를 명확히 분리했고, 배너와 확인창에 이동 범위 및 되돌릴 수 없음을 표시한다. 출발 매물 자체를 대상으로 선택하는 것도 차단한다.
- 합치기 성공 응답 후 출발 카드를 현재 목록에서 즉시 제거하고 전체 목록을 다시 불러온다. 목록·운영현황 캐시뿐 아니라 출발·대상 상세 캐시도 응답 전에 함께 무효화한다.
- 자산 버전은 JS `8.1.24-whole-master-merge`, CSS `8.0.39-whole-master-merge`로 갱신했다. 전체 테스트 245/245, Cloudflare 테스트 118/118, Wrangler dry-run을 통과했다.
- 운영 HTML의 새 JS·CSS 버전과 배포 JavaScript의 전체 합치기 버튼·선택 함수·서버 호출을 확인했다. 실제 매물 데이터를 바꾸는 합치기 실행은 임의 매물로 수행하지 않았다. 운영 Worker 버전은 `ae688e9f-8b50-4f29-9132-643f1df88268`이다.

## 2026-08-13 동일매물 대표사진 우선순위 조정

- 동일 공간의 대표 원본 선택에서 수집 순서 때문에 사진 없는 네이버 원본이 먼저 노출되던 문제를 수정했다.
- 당근 원본이 있으면 당근 우선 정책을 유지하되 당근 원본들 중 실제 사진이 있는 매물을 먼저 표시한다. 당근 원본이 전혀 없으면 사진이 있는 네이버 원본을 최우선으로 표시한다.
- 카드 썸네일, 상세창 첫 화면, 동일매물 원본 목록 순서에 같은 규칙을 적용한다. 원본 연결·임대조건·DB 데이터는 변경하지 않는다.
- 통합매물 JS 자산 버전을 `8.1.25-photo-priority`로 갱신하고 당근 사진 우선, 당근 부재 시 네이버 사진 우선 시나리오를 자동검사에 추가했다.
- 전체 테스트 245/245, Cloudflare 테스트 118/118, Wrangler dry-run을 통과했고 운영 HTML·JavaScript에서 새 우선순위 제공을 확인했다. 운영 Worker 버전은 `90623294-6f30-4cd6-8e6f-ebf14e00d9f4`이다.

## 2026-08-13 v6-cloud AI임장 데이터 접근 계층 전환

- 작업 시작 시 `CODEX_HANDOFF.md`와 깨끗한 작업 트리를 확인한 뒤 `git fetch --prune origin` 및 `git pull --ff-only`을 실행했다. 원격의 최신 16개 커밋을 fast-forward로 반영했으며 작업 시작 기준 HEAD는 `921bfdd`였다.
- `ai-visit-session-v6.js`의 계정별 AI임장 진행상태 조회·저장을 `JSDataAccessV6.read/mutate` 공용 경계에 연결했다. `loadCloudState`·`saveCloudState`의 범위, 레코드 키, 기기 캐시·메모리 대체와 지연 저장 방식은 바꾸지 않았다.
- 임장완료 시 매물 메모를 저장하는 `toggleDone`도 공용 D1 저장 경계로 연결해 JSON 응답으로 성공 여부를 즉시 확인한다. 저장 실패 시 화면 메모를 이전 값으로 되돌리고, 성공 시 기존처럼 매물 목록을 새로 읽는다.
- 공용 모듈이 없는 구형 독립 실행 환경에서는 기존 `saveApiURL` 직접 요청과 `mutationStatus` JSONP 폴링이 계속 동작하도록 가드된 폴백을 유지했다. D1 액션 이름과 요청 본문, 임장 확인·미확인 의미, 완료 메모 변환 로직은 변경하지 않았다.
- AI임장 자산 캐시 버전을 `6.4.39-data-access`로 갱신하고, 공용 경계 선행 로드·조회·저장 연결·레거시 폴백을 Cloudflare 회귀 테스트에 추가했다. `docs/CLOUDFLARE_MIGRATION.md`에도 적용 범위를 반영했다.

### 테스트와 빌드

- `js/ai-visit-session-v6.js` 문법 검사와 AI임장·데이터 접근·확인 상태·로드뷰 대상 테스트: 12/12 통과
- `pnpm test`: 247/247 통과
- `pnpm run test:cloudflare`: 119/119 통과
- `pnpm run cf:check`: Cloudflare 자산 126개 빌드 및 Wrangler 4.118.0 dry-run 통과
- `git diff --check`: 통과

### 남은 작업과 주의사항

- 다음 데이터 접근 전환 후보는 `async-mutation-queue-v1.js`, 그다음 `diagnosis-storage.js`다. 비동기 큐는 재시도·작업상태·중복 요청 계약을 보존하는 동작 테스트를 먼저 보강한 뒤 전환한다.
- `ai-visit-session-v6.js`의 직접 `fetch`와 `mutationStatus` 폴링은 공용 계층 미로딩 시의 호환 폴백이므로 실제 브라우저 회귀 확인 전까지 제거하지 않는다.
- D1 스키마·운영 데이터·R2 객체는 변경하지 않았고 이번 작업에서 운영 Worker 배포도 수행하지 않았다.

## 2026-08-13 동일매물 분리 대표값·체크 선택·당근 대표 우선순위 복구

- `서구 갈마동 366-10`에서 `3,000/150`과 `1,000/75` 원본을 별도 매물로 분리한 뒤 기존 대표행이 이전 `3,000/150`을 유지하던 문제를 수정했다. 앞으로 원본을 분리하거나 다른 대표로 이동하면 출발 대표행의 임대조건을 남은 활성 원본들의 보증금·월세 다수 조건으로 다시 계산하고, 같은 조건에서는 당근·네이버·공실박스 순으로 대표 원본을 선택한다.
- 이미 잘못 저장된 운영 대표 `M-b4795878-6cec-400f-a03e-77461df0e3e5` 한 건은 대상 ID·활성 상태·기존 `3,000/150`을 모두 조건으로 걸어 `1,000/75`, 관리비 1, 권리금 0, 9.9평으로 정정했다. `repairSeparatedMasterTerms` 이력을 남겼고 다른 매물은 변경하지 않았다.
- 주소·층·종류가 같은 별도 대표카드들이 동일 표시키를 공유해 체크박스 하나를 누르면 둘 다 선택되던 문제를 수정했다. 작업 선택키는 이제 `property:<고유 propertyId>`를 사용하며 선택인쇄·계약완료·임장완료·찜목록 추가도 각각 정확한 대표매물만 처리한다.
- 자동 검증 합치기의 `preserveRepresentative`는 D1 대표행을 덮어쓰지 않기 위한 저장 안전장치로만 유지한다. 화면 정렬에서 이 플래그가 당근을 맨 뒤로 보내던 처리를 제거해, 사진 있는 당근이 공실박스보다 우선하는 기존 대표 원본 정책을 다시 적용했다.
- 브라우저 캐시 버전은 `script.js?v=6.5.80-property-selection`, `unified-listings-v8.js?v=8.1.26-representative-priority`로 갱신했다.

### 테스트·운영 반영

- 분리·선택·대표 우선순위 집중 테스트: 12/12 통과
- `pnpm test`: 248/248 통과
- `pnpm run test:cloudflare`: 120/120 통과
- `pnpm run cf:check`: Cloudflare 자산 126개 및 Wrangler 4.118.0 dry-run 통과
- 정정 후 운영 D1 재조회에서 두 대표가 각각 `3,000/150`과 `1,000/75`로 분리된 것을 확인했고, `api-cache/d1-sheet.csv`와 목록 리비전 캐시를 제거해 즉시 재생성되도록 했다.
- 운영 HTML과 JavaScript에서 새 자산 버전, propertyId 선택키, 당근 우선순위 및 `preserveRepresentative` 강제 후순위 제거를 확인했다.
- 운영 Worker 버전: `096b4170-f344-4a96-a734-07e66195c92e`.
- 이번 배포에는 직전 `v6-cloud AI임장 데이터 접근 계층 전환` 커밋도 함께 운영 반영됐다.

## 2026-08-13 동일 주소·동일 층 별도매물 체크 완전 분리

- 매물 체크박스는 주소·층·호실·종류가 모두 같더라도 화면에 별도 카드로 존재하면 반드시 각각 독립적으로 선택되도록 수정했다.
- 기존 체크 키는 `propertyId`를 우선했지만, 리스트 가상 스크롤·카드 재사용·목록 구성 비교는 여전히 주소 기반 `item.key`를 사용하고 있었다. 이 혼용을 제거하고 `propertyId + D1 CSV 행 번호`를 카드 인스턴스 키로 사용한다.
- 비정상 데이터에서 `propertyId`까지 중복되더라도 행 번호가 다른 세 카드는 한 장씩만 선택된다. ID가 없으면 원본 링크와 행 번호, 마지막으로 기존 표시 키와 행 번호 순으로 안전하게 구분한다.
- 가상 스크롤 높이 캐시, 기존 카드 재사용 풀, 목록 변경 시그니처, 개별 체크·전체 선택·선택 인쇄·계약완료·임장완료가 모두 같은 카드 인스턴스 키를 사용한다.
- 운영 D1에서 같은 주소·같은 층으로 3개 이상 존재하는 그룹을 읽기 전용 점검했으며, 확인된 별도 카드들은 서로 다른 `property_id`를 보유했다. 운영 데이터는 수정하지 않았다.
- 브라우저 캐시 버전은 `script.js?v=6.5.81-card-instance-selection`으로 갱신했다.

### 테스트와 운영 반영

- 동일 주소·동일 1층 카드 3개 및 동일 `propertyId` 카드 3개 재현 테스트에서 한 카드만 선택됨을 확인했다.
- `pnpm test`: 248/248 통과
- `pnpm run test:cloudflare`: 120/120 통과
- `pnpm run cf:check`: Cloudflare 자산 126개 및 Wrangler 4.118.0 dry-run 통과
- 운영 HTML·JavaScript 응답과 브라우저 DOM에서 `6.5.81-card-instance-selection`, 행 단위 체크 키, 카드 인스턴스 DOM 식별자를 확인했다.
- 운영 Worker 버전: `77d7e544-2172-4ec0-a687-966fe85efb5b`.

## 2026-08-14 상세카드 원본 링크·합치기 작업 한 줄 정리

- 상세카드 하단 작업 그리드에 있던 `원본 링크 열기`를 선택한 당근·네이버·공실박스 출처 표시 오른쪽으로 이동해 `원본 링크 ↗` 보조 버튼으로 표시한다. 주소는 그 아래에 그대로 유지한다.
- 하단 작업 영역에는 `별도 매물 분리`, `원본 1개 합치기`, `대표 전체 합치기`만 남기고 동일 너비의 `flex-wrap: nowrap` 행으로 구성해 세 기능이 한 줄에 표시되도록 했다.
- 좁은 데스크톱 상세창에서도 세 버튼이 넘치지 않도록 문구·간격·글자 크기를 다듬었다. 원본 수와 대표 상태에 따른 기존 버튼 노출 조건, 분리·합치기 서버 로직과 데이터는 변경하지 않았다.
- 원본 링크는 공용 `openExternalLink`로 열며 HTTP(S)만 허용하고 `noopener,noreferrer` 및 잘못된 URL 디코딩 방어를 적용했다. 목록카드의 단일 원본 링크도 같은 안전 함수를 사용한다.
- 브라우저 캐시 버전은 `unified-listings-v8.js?v=8.1.27-detail-action-layout`, `unified-listings-v8.css?v=8.0.40-detail-action-layout`으로 갱신했다.

### 테스트와 운영 반영

- 상세카드 위치·한 줄 레이아웃·분리/합치기 노출·원본 링크 보안 집중 테스트 통과
- `pnpm test`: 248/248 통과
- `pnpm run test:cloudflare`: 120/120 통과
- `pnpm run cf:check`: Cloudflare 자산 126개 및 Wrangler 4.118.0 dry-run 통과
- 운영 HTML·JavaScript·CSS와 브라우저 DOM에서 새 자산 버전, 출처 옆 링크 구조, 세 버튼 문구 및 줄바꿈 금지 스타일을 확인했다.
- 운영 Worker 버전: `0347a4dd-7f12-4843-923a-f2312aa763cf`.

## 2026-08-14 상세카드 출처·원본 링크 간격 보정

- 선택한 원본의 출처명과 `원본 링크 ↗` 사이 간격을 8px에서 18px로 늘리고 자동 오른쪽 밀기를 제거해, 서로 붙어 보이지 않으면서 한 정보 묶음으로 읽히도록 조정했다.
- 상세카드의 세 분리·합치기 버튼 한 줄 구성과 기능 로직은 그대로 유지한다.
- CSS 캐시 버전은 `unified-listings-v8.css?v=8.0.41-source-link-spacing`으로 갱신했다.
- `pnpm test` 248/248, `pnpm run test:cloudflare` 120/120, `pnpm run cf:check`를 통과했다.
- 운영 배포 Worker 버전은 `2f814e32-7bb5-4754-9fd6-221411096821`이다.
- 앱 내 브라우저에는 승인 계정용 Google 로그인 버튼이 표시되지만 자동 브라우저 제어가 교차 출처 Google iframe 내부를 누를 수 없어, 로그인 후 실제 상세카드 캡처는 사용자 로그인 세션 연결이 필요하다.
