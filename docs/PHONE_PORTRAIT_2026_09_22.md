# 2026-09-22 스마트폰 세로 전용 화면

## 변경 범위

스마트폰으로 확인된 기기에만 `js-phone-app-v2`를 적용한다. 화면 폭만으로 판단하지 않고 기기 정보, 터치 입력, 실제 화면 크기를 함께 확인한다. iPad·Android 태블릿, 태블릿 분할 화면, 폭을 좁힌 PC는 기존 화면을 유지한다.

| 기존 스마트폰 | 변경 후 |
| --- | --- |
| 로고·빠른등록 및 지도/매물/임장/고객/더보기 메뉴 | 상단 검색창과 지도/매물/찜 3개 메뉴 |
| 여러 목록 필터가 상시 노출 | 목록에는 건수·정렬, 나머지는 상세 필터에 배치 |
| 목록에서 전화·내비 등 여러 버튼 노출 | 가격 중심 목록과 찜 버튼, 상세 하단 전화·길찾기·찜·원본 |
| 공통 찜 관리 창 | 폴더 목록 → 폴더 매물 → 상세 화면, 뒤로 가기와 탐색 위치 유지 |
| 작은 화면에서도 가로 UI 사용 | 가로에서는 세로 사용 안내, 회전 후 검색·매물 상태 유지 |

필터 변경은 적용 버튼을 눌렀을 때 반영하며 닫기는 취소한다. 소프트 키보드로 화면 높이만 줄어들어도 회전 안내를 띄우지 않는다. 수집기, 자동수집 일정, 운영 DB 및 계정 데이터는 변경하지 않았다.

## 소스 백업

- 기준 커밋: `61f7ce4d32ff7a2af724f6b900bc0f0ccbaf8122`
- 로컬 보관: `C:/Users/ITP/Documents/JS부동산 백업/스마트폰개편전-20260922-143519/source-61f7ce4.zip`
- SHA-256: `CFF132BD877900C37C5940347B5D369DB622DC5DD9DF3E13A51603410A4D578B`
- 내용 있는 파일 483개를 모두 읽어 압축을 검증했다. 같은 폴더에 복원 안내와 비교용 압축 해제본이 있다.
- Git 추적 소스 전체의 백업이다. 운영 DB, 비밀 설정, 미추적 로컬 파일은 포함하지 않는다.
- 복원 시 별도 빈 폴더에서 비교한 뒤 필요한 변경만 선택적으로 반영한다. 현재 작업 폴더를 통째로 덮어쓰지 않는다.

## 검증

- Node 회귀 테스트 904개, ESLint, 타입 검사 통과.
- 설치된 Chrome의 격리된 로컬 테스트 데이터로 브라우저 33개 통과. 프로젝트별 해당하지 않는 중복 실행 25개는 의도적으로 생략한다.
- 스마트폰 폭 360/390/430, 실제 화면 회전 모의, 키보드 높이 감소, 로그인 직후 회전, 검색 즉시 탭 전환, 찜→상세→뒤로 가기, 원본 복귀, 목록 스크롤 유지, 필터 취소/0원 조건을 검사했다.
- 태블릿·PC 5개 조건에서 백업본과 지도/목록/필터의 화면 배치를 비교했다. 목록 스크린샷도 동일했다.
- Cloudflare 자산 빌드 및 `wrangler deploy --dry-run` 통과. 이후 사용자 승인에 따른 실제 운영 배포는 아래에 별도 기록한다.
- 브라우저 검사는 기기 에뮬레이션이다. 실제 휴대폰 Safari/Samsung Internet 및 운영 지도 SDK의 실기기 검수는 별도다.

### 로컬 재검사

PowerShell에서 설치된 Chrome을 사용하는 예시:

```powershell
$env:JS_BROWSER_CHANNEL = 'chrome'
$env:JS_BROWSER_FIXTURE_PORT = '4184'
$env:JS_PHONE_BASELINE_ROOT = 'C:/Users/ITP/Documents/JS부동산 백업/스마트폰개편전-20260922-143519/reference'
pnpm exec playwright test
```

백업 경로 환경변수가 없으면 백업본과의 정밀 비교 5개만 추가 생략된다. 기본 브라우저 검사에는 실제 서비스 로그인이나 운영 데이터 변경이 없다.

## 운영 배포

- 사용자 배포 승인 후 2026-09-22 15:15 KST 확인 완료.
- 배포 소스: `2b21b39` (`codex/daangn-numeric-status`).
- Cloudflare 버전: `e4cb2fe5-aa6f-4371-a290-9c67603938e7`.
- 직전 운영 버전: `b88fdcf5-b565-49ab-81d6-7ff7682b4d1d`.
- 공개 자산 캐시 버전: `07236ef133a9a47ea321059c`.
- `js-map.com`과 `www.js-map.com` HTML이 HTTP 200으로 응답하고 새 스마트폰 CSS/JS를 참조함을 확인했다.
- 변경된 CSS/JS 및 서비스 워커 총 12개가 HTTP 200으로 응답하며 로컬 배포 자산과 SHA-256이 일치했다.
- 비로그인 `/api/session` 요청은 기존 인증 계약대로 HTTP 401로 차단됨을 확인했다. 인증 후 실기기 사용 검수와 구분한다.
- 운영 DB 마이그레이션, 수집기 설정/실행 변경, 로컬 미추적 파일 배포는 수행하지 않았다.

## 2026-09-23 접힌 폴드 실기기 판별 확인

- S25 Ultra는 새 화면을 표시하지만 폴드는 접힌 상태·삼성 인터넷 모바일 사이트 모드·새로고침 후에도 기존 화면이라는 사용자 제보가 있다. 당시 운영 자산 12개는 9월 22일 배포본과 일치했고 저장소 복원/롤백은 없었다.
- 실기기의 UA, mobile 힌트, screen 값을 확보하지 못한 상태이므로 원인을 특정하거나 태블릿 제외 조건을 완화하지 않는다. 정상 화면 갱신/PC 모드/오래된 screen 정보는 로컬에서 구분되지만 어느 경우인지 사용자 기기 확인이 필요하다.
- `https://js-map.com/?phoneCheck=1`은 로그인 후 기기 화면 확인 패널을 표시한다. 사용자가 접은 세로 상태에서 진단 내용을 복사하거나 캡처해 전달할 수 있다. 일반 접속에는 아무 UI도 추가하지 않는다.
- 패널은 화면·브라우저·모델 정보만 로컬에서 읽는다. 계정/매물/쿠키/토큰/저장소를 읽지 않으며 서버로 전송하거나 저장하지 않는다. 모델 조회도 진단 링크를 연 경우에만 한 번 실행한다.
- 이 단계는 실기기 확인 기능이며 **폴드 표시 오류 수정 완료를 의미하지 않는다**. 기존 분류 조건 및 태블릿·PC 화면은 유지한다.
- 진단 전용 브라우저 검사 4개, 기존 Node 검사 904개, lint/typecheck/Cloudflare dry-run 통과.
